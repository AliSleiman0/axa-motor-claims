# HANDOFF — read this first

**Project:** AXA Middle East — Mobile Application for Motor Claim Management
**Developer:** solo (Ali Sleiman)
**Commitment:** 2 months, $5,000 fixed, developer handles everything
**Status as of 2026-08-22 (slice 4.4 — P2, and the slice is done):** **weeks 1–3 are complete and committed, and so are 4.1–4.3** (`783f32e`). **Slice 4.4 is complete and uncommitted** — P1 and P2 together, awaiting the developer's test-diff review, so the working tree is 4.4 alone. **The box is ticked.** Only P3 remains and it is now one item, not two: the broker display-name snapshot column (pass-2 decision 3), which belongs with 5.2/5.3 rather than here. **279 web tests (+80 over the slice) and 503 xUnit (501 passed, 2 environment-skipped — Azurite and the NEXT3 sandbox).** **No existing test was edited anywhere in either half.**

**The browser pass found a bug that my own testability hack had planted, and it blanked the screen.** `ConfirmDialog` rendered `<dialog open>` so that jsdom — which has no `showModal` — would still show it to a test. In a real browser `open` puts the element into the **non-modal** state, `showModal()` then throws `InvalidStateError`, and because that happens inside an effect React tears down the whole tree: pressing **Deactivate** cleared the page to bare canvas. **All seven of its tests passed.** It is `showModal` if it exists and `open` only otherwise, never both; verified in Chrome with `:modal` and with focus landing on Cancel. **Writing markup to suit the test environment is how you ship a bug the test environment cannot see** — and it is the same shape as 2.5's PDF-in-an-`<img>` and 4.2's `<img src>` that would have logged an officer out, third time in three slices that the thing no test could reach was the thing that mattered.

**The second finding was arithmetic, and no test has a width.** O2's three columns give the left one about a third of 1180 px; the detail table's label column takes up to 200 px, so the *value* got 75 px and every field wrapped mid-word — "PLACEHOLDER Insured 88" rendered as PLAC/EHO/LDER/Insur/ed/88 and a plate number broke over three lines. A review column is phone-width, so it now gets the phone treatment at every width.

**Pass 2's open question about the 429 has an answer: yes, and as a header.** `AuthEndpoints.TooManyRequests` already sets `Retry-After`, so S1 and S2 *read* the resend countdown rather than counting down from `Auth:OtpResendSeconds` — which is what keeps it right across a reload, since the browser has no idea when the code was sent. It is also why both screens keep raw `fetch`: `ApiError` carries a status and a body and **no headers**. Proven live at 43 s ticking to 40 s, with a throttled request rendered as a countdown rather than an error.

**The five-attempt sentence is the screen counting its own tries, and that is the only honest version available.** §9 answers a wrong code, an expired code, a used code, an exhausted code and an unknown number with one identical empty 401 so that nothing can be probed by trying. The screen therefore says what it can defend — this code was not accepted, and separately that a code only gets five — and never which of the five just happened. Anything sharper is a deliberate decision about what the app will reveal, not a guess on the phone.

**S1 exists at last, and the invitation SMS carries a link.** `InviteService` keeps its old sentence verbatim and appends `{AppBaseUrl}/invite/{token}` — which is precisely why **no test moved**: `CapturingSmsSender` reads the token with `invite token is ([A-Za-z0-9_-]+)`, which stops at the full stop. `scripts/demo-reset.ps1` used the looser `token is (\S+)`, which *would* have swallowed the URL into the token, and is tightened to the same class. **The reset seeds through the real onboarding path, so a broken invite SMS fails the reset rather than the demo** — which is what it is for, and it still runs in 18 s. Walked end to end in Chrome: the link from a real SMS → *You have been invited* → code → *You are signed in* → **Go to My claims** → `/expert`, and the same link again gives the uniform *This invitation cannot be used*.

**The link's origin is configuration and must never be `Request.Host`.** The Host header is attacker-controlled and this URL carries a live credential into an SMS sent in AXA's name, so a poisoned one would send new users' activation codes elsewhere. New `Auth:AppBaseUrl`, PLACEHOLDER by default. **`--no-launch-profile` bit here:** `demo-reset.ps1` starts the API with it, so the `launchSettings.json` value never applied and the demo's invitations pointed at `https://PLACEHOLDER-app.example` — correct for a placeholder and useless to anyone who taps one. Set in the script beside `Blob__Mode`, for the same reason those are. Two server tests pin the rule: one that the link carries the configured origin, one that it does **not** carry the test server's host.

**One artboard word was deliberately not shipped, and it is worth knowing why.** `G3Waiting` draws *Queued* beside a garage's document, but §5.2 holds every garage document `deferred` with no outbox row until an officer approves, and pass 1's string is "Queued, **will send**" — a promise the app cannot keep, since a rejected declaration never pushes anything. A fourth indicator string would have been invented copy. The panel's heading and the "Waiting for AXA" paragraph above it already carry the meaning.

**O2's approve button names its three steps now**, because they are not equivalent: a failure at *Rendering* or *Uploading* leaves the declaration undecided, and only *Approving* commits. Additive state on `useDecision`; the ordering, the single latch across both buttons and the server's `409 approval_image_required` are untouched, and a rejection keeps "Working…" because it is one call. The test gates each step on a promise it resolves one at a time, so the label is read *while* that step runs — **verified by planting the flat "Working…", which turns two of three red**.

**What the pass re-proved behind all of it.** The whole §5.2 chain again: garage draft → capture → submit → officer three-column review → visa search → approve (both rows `sent` on attempt 1) → decided panel with **no controls at all**. A1's three row treatments with a real `invited` row, the modal confirmation, Escape and Cancel both closing cleanly. E1's no-match pair, E2's Arrival sentence, G3's "What was sent". Every button label `docs/demo-week4.md` names is unchanged.

**Status as of 2026-08-22 (slice 4.4 — P1):** **weeks 1–3 are complete and committed, and so are 4.1, 4.2 and 4.3** (`783f32e`). **Slice 4.4's P1 is complete and uncommitted**, awaiting the developer's test-diff review — so the working tree is 4.4 alone. **The 4.4 box is deliberately still unticked: P2 and P3 are not done** and carry to a follow-up session. **249 web tests (+50) and 501 xUnit (499 passed, 2 skipped)** — the two skips are Azurite and the NEXT3 sandbox, neither of which was running; **no server code changed at all**. The app now looks like the three `/design` canvases: the pass-1 tokens as a real stylesheet, self-hosted IBM Plex, a shared `ui/` component layer, the two net-new shells, and pass 3's one shipped defect fixed.

**No existing test was edited**, which is what the card asked for and was the main thing at risk in a slice that touches every screen. Four tests did go red and every one was worth reading rather than patching. **A test that goes red on a restyle is sometimes telling you the restyle is wrong.** Putting the media count in its own `<span>` broke `getByText('Insured car photos (3)')`, because that query reads an element's *direct* text children — one text node again. And making the whole worklist card an `<a>`, which is the obvious way to give a gloved thumb the full row, broke `links.map(l => l.textContent)` **correctly**: the link's accessible name became the entire card, so a screen reader would read six label/value pairs before naming the claim. It is a stretched-link overlay now — tappable edge to edge, announced as the visa number.

**The fourth was a real bug, and this toolchain does not call it an error.** `OfficerDeclarationPage` had a local `DocumentRow` and imported the new shared one under the same name. It resolved to the import, rendered rows with empty names and no children, and compiled and linted clean; the symptom looked like three broken queries. Renamed to `ReviewDocument`.

**Read the artboard, not the summary line: the per-bucket accessible name is an `aria-label`, not a new visible label.** The canvas draws "Take a photo" and *quotes* "Take a photo — insured car" as the accessible name, and those are different things. So the visible label stays the shipped string that `docs/demo-week4.md` names, the input carries the qualifier, and WCAG 2.5.3 wants exactly that containment. The predicted hazard — `getByLabelText` double-counting once a `<label>` and an `aria-label` both match — does not occur; testing-library dedupes the union, and E2's `/^Take a photo/` → 5 assertion is untouched with qualifiers live. Verified in the browser: **eight distinct accessible names on E2**, five "Take a photo — …" and three "Choose a file — …".

**One card item was dropped rather than built.** P3's "exclude `approval_image` from garage-facing media counts" (pass-2 decision 4) is answered the other way in `scope-decisions.md` by slice 4.3, whose argument is better: the approval image *is* the declaration's media under the same visa, and filtering it would put a §7.1 rule outside the table §7.1 is encoded in. Recorded as considered-and-not-taken. **Two review documents written the same day can disagree, and the playbook card inherited one of them.**

**The browser pass found four things, eighth slice running, and the first is the one no test could reach.** At 390 px the header measured **443 px against 386 of viewport** and pushed **Sign out** off the right edge — on the two shells that have no other way out, in the slice whose whole point was adding Sign out. The phone number gives way, keyed on the shell class rather than a media query, because pass 1's distinction is field-versus-office and because a viewport media query is untestable in a harness that pins the viewport. The other three: O2 inherited E4's `max-height: 60vh` on document previews, so the officer got one screen-height photograph per row (right for E4, where a person must look at the photo before it is sent; wrong for a list of every document on a claim); the "Failed pushes" tab was pixel-identical to the four real tabs and did nothing; and the diagram's 30 px wing columns ran their labels over the border into the bonnet.

**The diagram change alters what AXA receives, so it was proven rather than asserted.** Panel names are drawn into the exported PNG and the mark went `#cc0000` → `#f7c8c4` with a darker border, because a solid red hid the name of the one panel anybody cares about. Confirmed live: **fifteen names in a 1600×1200 PNG, Front bumper and Front left wing marked and still legible**, `passed` / `PLACEHOLDER-DOC-07` / 98,516 bytes, outbox `sent` on attempt 1. `<tspan>` with inline attributes and **never `foreignObject`** — a serialized SVG carries no page CSS and a foreignObject taints the canvas outright.

**`media/` importing `ui/` needed a rule, and a ban would have been the wrong one.** Most of `ui/` is presentational and safe for the Option 2 public page; `AppShell`, `AppHeader` and `useSignOut` read the session, the token store and the router — precisely what `reusability.test.ts` exists to keep out. So an **allow-list** of the safe modules, plus a second `describe` asserting `ui/` imports no role module at all, with the router deliberately *not* forbidden there because `DesktopNav` navigating is the job. **Both halves verified by planting**, one violation each.

**Fonts are self-hosted and the licence was checked first** (3.4's rule, second outing): `@fontsource/ibm-plex-sans` and `-mono` declare **OFL-1.1**, which permits embedding and puts no obligation on this codebase's own source — the thing HANDOFF §5's open IP conversation needs it not to do. Latin subset, five faces. Proven in the browser: all five served from the app's own origin, **zero requests to `fonts.googleapis.com`**.

**What the pass re-proved behind the restyle.** `demo-reset.ps1` ran in 22 s unchanged, and the whole §5.2 chain was driven again: garage draft → §7.1's **no file picker on Car photos**, live → E4 at **1600×1200, sharpness 33707** → Submit → officer inbox → visa search → **Approve rendered and uploaded the PNG and *then* approved** → both outbox rows `sent` on attempt 1 with `file_name` intact → both notifications on §8's email fallback. A duplicate phone on A1 now answers *"Somebody already has that phone number…"* instead of `Save failed (409): {"error":"duplicate_phone"}`. Every button label in the demo script is unchanged.

**One flake, written down rather than shrugged off.** `GarageDeclarationsPage`'s `findAllByRole` (1 s default) timed out under 26 parallel test files in two runs out of five; four consecutive full runs since are green. Not fixed, because fixing it means editing a test and it is a timeout rather than a wrong assertion — but the new `ui/` module graph plausibly added first-import latency to that worker.

**Status as of 2026-08-22 (slice 4.3):** **weeks 1–3 are complete and committed, and so are 4.1 and 4.2** (`ce82e31`). **Slice 4.3 is complete and uncommitted**, awaiting the developer's test-diff review. **500 xUnit tests green and 1 skipped; the web suite is 199 — both unchanged, because 4.3 ships no feature code and no test change.** It is the demo-prep slice: `docs/demo-week4.md` (the beat sheet), `scripts/demo-reset.ps1` + four companions, `docs/demo-fix-list.md` (17 findings, triaged), and **two full dry runs in Chrome**. The second dry run closed the gap 4.2 recorded: **both of §8's push rows are now proven against a real push service** — the expert's assignment popup and the garage's decision popup, each with the right deep link — alongside **Arrived on real GPS** and a real `MediaRecorder` voice note. **The client-comms gate is NOT met and it blocks the demo:** the scope letter and the Friday status are drafted (`docs/scope-letter.md`, `docs/status-2026-08-22.md`) and **unsent**, and `docs/next3-openapi.yaml` has been ready since 3.3 and is **unsent**. Sending is the developer's act.

**The card asked for something a browser cannot do, and that was the slice's real finding.** "Inline image previews via the content endpoint" implies an `<img src="/api/…/content">` — but the session is a bearer token in a header, an `<img>` sends none, the endpoint answers 401, and `api()`'s 401 branch hard-navigates to `/login`. **An officer would have been signed out by looking at a photograph.** So `apiBlob` joins `api<T>()` over one extracted `send()`, so the single refresh-and-retry cannot drift between them; the *fetch* points at the content endpoint and only the rendered `src` is a `blob:` URL. Images fetch as their row renders, everything else on a click, so a claim with a dozen photographs does not pull every megabyte first.

**A latch test built on `fireEvent` passes with the latch deleted, and planting is what found it.** `fireEvent` flushes React between events, so by the second click `isPending` is true and the `disabled` attribute swallows the rest — a real guard, but not the one under test. Three raw `.click()`s inside one `act` reproduce two taps in the same tick, and the plant then goes 1 → 3 requests. **A concurrency test that never puts two calls in one tick is testing the button, not the guard** — worth checking the other three latches against this the next time one is touched.

**The content endpoint needed two authorization checks and the obvious one is the wrong one.** Resolving the *declaration* for the caller is not enough: matching the document by `docId` alone lets a garage read any document in the system by quoting its id under a declaration of its own. That is a check which looks present and tests the wrong thing, which is worse than an absent one because nobody re-reads it. A test isolates it — it stays green when the declaration check is removed and goes red only for this.

**Two smaller traps worth knowing.** `Results.File`/`Results.Stream` with a download name emit `Content-Disposition: attachment`, which turns O2's inline preview into a download prompt per photograph — the response is a small `IResult` type instead, which is also where `nosniff` lives. And **the React lint rules reject `setState` in an effect body**, which is how the first preview hook was written; rebuilt on `useQuery`, which also caches the bytes across a remount.

**The PNG generalization cost no test churn at all.** `media/png.ts` took everything mechanical — serialize, rasterize, the injectable renderer, the error type — and `diagram/render.ts` kept only what is about diagrams, aliasing the renamed types. `DiagramPanel` and its 15 tests passed untouched. The new `media/approval/` builder lays the decision out as `<text>` lines and **never `foreignObject`** (a serialized SVG carries no page CSS, and foreignObject taints the canvas), escapes the officer's free text (an unescaped `<` in "damage < 500 AED" would fail to rasterize at all), and stamps the time in **explicit UTC** — it is the browser's clock, so the image says *prepared at* and `decided_at` stays authoritative.

**The manual Chrome pass ran, and found one real bug — sixth slice running.** **`PushPanel` was telling a garage the wrong thing about its own workflow.** Written for `ExpertLayout`, it promised a popup "the moment a claim is assigned to you", and that sentence then sat above every garage screen. A garage is never assigned a claim — §8's garage rows are decisions on work the garage filed. No test could see it: every push test rendered the panel with no role in mind and asserted the *button*, never the sentence. It is a `PushCopy` parameter now, defaulted to the expert wording so nothing that existed changed. (The constants live in `push/copy.ts` because `react-refresh/only-export-components` forbids a `.tsx` file exporting non-components — `TestQueryProvider`'s reason, met again.)

**What the pass proved live, against Azurite and the fake NEXT3.** `homePathFor` landed both new roles on their own screens. The garage's `Car photos` panel rendered **no file picker** while `Survey documents` did — §7.1 in a real browser. The clarity gate ran on a real capture at **1600×1200, sharpness 14984**, and a PDF reached the confirm screen **named, with no sharpness line**. Both documents stored `deferred` with **no outbox row**. **Three rapid taps on Submit produced exactly one UPDATE, one fan-out and zero 409s.** **The officer's image preview decoded at `naturalWidth: 1600, naturalHeight: 1200` from a `blob:` URL** — the finding that made this slice, since the card's literal `<img src="/api/…">` would have been 0 pixels and a logout. The PDF stayed a lazy button until clicked. Blank search was refused client-side; an unknown visa produced #16's hint verbatim. **Approve rendered a real 128 KB PNG, uploaded it, then approved — in that order** — with a comment containing `<` and `&`, which is the XML escaping proven against a real canvas rather than a stub. All three documents reached **`sent` in the *Survey* folder**, the approval under the distinct `push_approval` operation, and **`invoice.pdf` survived from the garage's upload into the push built at approval** — the entire argument for `file_name`, demonstrated. G3 then unlocked. Rejection left `visa_no` NULL, **0 pushed documents**, one stored comment — and **the garage saw status only while the officer saw the reason** (§1; the scope letter must warn about this).

**Two things deliberately not done, said plainly rather than implied.** Real browser *push* was not re-tested: permission was `default`, and Chrome's permission bubble is browser UI this harness cannot click without risking a blocked session — and slice 3.4 already proved the mechanism end to end against a real push service. Both notifications were instead observed taking §8's **email fallback**, which is the first live proof of 4.1's broad per-officer catch. And both roles shared one browser profile, so the sessions were sequential rather than simultaneous. **Noticed and not fixed:** G1's media count includes the officer's approval image (3, not the garage's 2) — defensible, but worth a decision in 4.3.

**Recorded rather than diverged from:** the approval PNG is uploaded through `uploadDocument`, not `useCapture`, so Approve stays one press — `useCapture` is a select→confirm→send machine and a Confirm button in the middle of a decision would also break §5.2's clean-abort ordering. The card's `photographic: false` has no meaning outside `useCapture.select`; the fixed 1600×1200 satisfies the **server's** §7.2 floor either way. design.md §5.2 gained the content endpoints, and scope-decisions.md five rows.

**Status as of 2026-08-22 (slice 4.1):** **weeks 1–3 are complete and committed** (3.4 is `34f7db7`). **Week 4 has started: slice 4.1 is complete and uncommitted**, awaiting the developer's test-diff review — so the working tree is 4.1 alone. **491 xUnit tests green (+56) and 1 skipped; the web suite is untouched at 155.** 4.1 is design.md §5.2's state machine: one `declaration` entity with a garage view and an officer view, the schema, the endpoints, the notifications both ways, and the media that finally **waits**. One migration, db-reviewed. **No UI and therefore no browser pass** — 4.2 is where the screens land, and it is also where the manual Chrome pass for this flow belongs.

**The slice's own idea: §5.2's transition table is a table, not a switch.** `DeclarationTransitions` encodes it as data the way `MediaBuckets` encodes §7.1, and `Declaration` keeps no second copy. That is what makes the *illegal* pairs testable by construction — the legal edges are five rows anybody would write; the illegal ones are **25** combinations nobody enumerates by hand, and the suite sweeps them straight from the enums. Verified by planting the resubmit edge §1 forbids: three tests go red.

**Push deferral is now structural rather than a policy.** `document.push_status` gains `deferred`, `BucketRule` gains `PushTiming`, and §5.2's three new buckets store a row with **no outbox row at all** until the officer approves. `CK_document_push_status_outbox` makes a deferred document with a queued push unrepresentable, so "nothing goes to NEXT3 before approval" is a property of the schema and not a rule the garage endpoint has to remember.

**The `db-reviewer`'s CRITICAL finding was wrong, and checking it cost five minutes.** It reported that `ContentDispositionHeaderValue.Name` keeps its quotes and that every real-browser upload had therefore been dead since slice 2.3. Three recorded Chrome passes said otherwise, and a throwaway probe settled it: the getters *do* strip quotes. **The same finding's second half was right and worse than it looked.** `Path.GetInvalidFileNameChars()` returns 41 characters on Windows and exactly ` ` and `/` on the Linux containers §10 deploys to — so the file-name sanitiser did almost nothing where it runs and everything where it is developed and tested. CR and LF surviving into the `Content-Disposition` that `RealNext3Client` writes is a header-injection primitive chosen by whoever picked the file. **Check the reviewer's findings the way it checks yours** — and note that seven migrations running, its worst finding has been in the adjacent code rather than the schema.

**Two authorization holes the card did not describe, both from that pass.** `approval_image` shares the `declaration` owner kind with the garage's own buckets, so without a per-caller allow-list a **garage could attach its own approval** and walk through the gate that exists to make an officer sign the decision — capture-only would not stop it, because `origin` is a claim the client makes. And an OnApproval bucket uploaded *after* the approve transition is a `deferred` row nothing will ever enqueue: never pushed, absent from A2, excluded from §7.3's sweep, blob retained for ever, and nothing thrown. Both are closed, and the allow-list refuses **before the file is read**, so a rejection costs no blob and leaves no row.

**The card's atomicity test could not be written as described, and saying so beat writing something that looked like it.** `WithNext3Down` sets `Fake:FailureRate = 1.0`, and one `FakeBehavior` serves NEXT3 *and* all three senders — so it fails the visa check rather than the transaction, and there is no seam over HTTP to fail a request part way through. Three tests instead: the 503 path writes nothing; **four parallel approvals** are the seam that does exist (each loser stages a comment row, three document flips and four outbox rows, and every one has to vanish); and a poison-row test against the mechanism directly. `state` as the EF concurrency token is load-bearing — **verified by removing it, all four concurrency tests go red**. The nastiest is approve-versus-reject: an approval landing after a rejection would give a disregarded claim a visa and push its documents to AXA.

**One column §4 did not list, for the third time in this table's history.** `document.file_name` — a deferred push is built at approval, in a different request, with the upload's `Content-Disposition` long gone, so a garage's `invoice.pdf` would have reached NEXT3's *Survey* folder as `019ab….pdf`. design.md §4, §5.2, §7.1, §7.3 and Appendix A were all updated rather than diverged from; **seventh slice running**.

**One gap left open deliberately, and it belongs to 7.2:** a **rejected** declaration — and an abandoned draft — retains its blobs for ever. Its documents stay `deferred` with no outbox row, so sweep 1 structurally excludes them and sweep 2 spares any blob a live row claims. Correct while a decision is pending, wrong for ever once rejection is terminal. It is written into design.md §7.3 rather than left as a code comment, because deleting a rejected claim's photographs needs a client answer (#4/#22), not a sweep added quietly.

**Status as of 2026-08-21 (slice 3.4):** **weeks 1 and 2 are complete and committed, and so are 3.1, 3.2 and 3.3** (`fa5c707`). **Week 3 is finished and committed** — 3.4 is `34f7db7`, working tree clean. **435 xUnit tests green (+40) and 1 skipped; the web suite is 155 (+21).** **Week-4 cards 4.1–4.3 expanded to verbatim prompts on 2026-08-22** (`docs/build-playbook.md`) with six decisions recorded at expansion: push deferral as a third `push_status` (`deferred`) driven by a `PushTiming` field on `BucketRule`; the garage declaration buckets in 4.1 rather than 5.1; approval as two calls (PNG upload, then approve); approve verifies the visa with `GetClaim`; an officer document-content endpoint; `state` as the concurrency token. **Week 4 is the client-demo week (30%)** — 4.3 gates the demo on sending the scope letter and `docs/next3-openapi.yaml` first. Next: slice 4.1. 3.4 makes the BRD's primary trigger real: an assignment now puts an actual notification on an actual browser, and clicking it opens E2. `IPushSender` becomes pluggable by config the way Blob and NEXT3 already are — third outing for that switch — and `push_subscription` joins §4 as a new table. **`Push:Mode` is `fake` everywhere and no VAPID key is committed**; CLAUDE.md carries the `npx web-push generate-vapid-keys` + `dotnet user-secrets` steps for a real browser pass.

**The card told me the library was MIT. It is not, and 2.3's rule is the only reason that was caught.** `WebPush` (web-push-libs) is **MPL-2.0** — weak copyleft — and pulls the deprecated `Portable.BouncyCastle`. This codebase is heading for handover with HANDOFF §5's IP conversation still open, and a copyleft dependency is not a question to answer under time pressure in week 8. Switched to **`Lib.Net.Http.WebPush` (MIT, v3.3.1)**, verified from both packages' own `.nuspec`, and it turned out to fit better anyway: it is built on `HttpClient`, so `PushServiceClient(HttpClient)` *is* the injectable transport the card asked for — reusing slice 3.3's `IHttpClientFactory` and `StubHttpMessageHandler` — and on net10.0 it drops BouncyCastle entirely, leaving one transitive dependency, also MIT.

**The db-reviewer found the worst bug for the sixth migration running, and it was the same bug as last slice.** `HttpClient` reports its own timeout as `TaskCanceledException`, which derives from `OperationCanceledException`, so a catch filter naming `TimeoutException` never matches. Slice 3.3 closed exactly this on `RealNext3Client` — and the lesson was not carried across to the second HTTP client written a day later. Here it was worse than in the outbox: the exception escapes the per-subscription loop, so the expert's other devices are never attempted, no `failed` row is written for §8 to show, and every revocation staged so far is discarded — **and then it escapes `AssignmentHandler`'s catch too**, whose filter also excludes `OperationCanceledException`, faulting the assignment-ingestion path. **A lesson learned in one adapter is not learned until it has been checked in the others.** Pinned now in both directions, in both places.

**Two findings that were security-shaped, and neither was reachable by a test that did not go looking.** The subscribe endpoint stores a URL **the API later POSTs to**, supplied by the caller — so unchecked it was a blind SSRF available to any authenticated user of any role: aim it at a host reachable only from inside the deployment and read the outcome out of the `notification` log. Slice 1.5's rate limiting covers `/public/*` only, and #21 puts an AXA Group InfoSec review on the critical path. There is now a host allow-list (real values rather than placeholders — these are the browsers' own push services, not client data, the same treatment as `Media:ImageContentTypes`) and a per-user subscription cap, the latter because the sender walks a user's devices serially on the ingestion path, so an unbounded set was an unbounded stall for *every* expert.

**Three smaller traps, each of which cost real time and each of which reads as obvious afterwards.** `Base64Url.TryDecodeFromChars` **throws** on a bad character despite the name — it returns false only for a small destination — so key validation answered 500 where it existed to answer 400; `Base64Url.IsValid` first. **A fixture can be well-formed and still not real:** the test `p256dh` was 65 correctly-shaped bytes that are not a point on the curve, and the library's ECDH import threw a platform exception that looked exactly like a broken sender until it was tracked down — real keys are generated per run now, for the browser side and the VAPID pair alike, and still never committed. And **minimal APIs will not infer a request body on DELETE**: without `[FromBody]` the entire application fails at startup, which turned a finding the reviewer had ranked "low" into a hard blocker.

**Two tests asserted an ordering the query never promised**, and one of them passed in isolation and failed in the full suite — the send order varies. Both now assert the property (one sent and one failed; exactly one revoked) rather than which device drew which response. **No assertion anywhere in the diff was weakened or removed**; the two that changed were wrong about a guarantee that does not exist.

**The manual Chrome pass ran, against a real push service, and found two bugs the suite was blind to — fifth slice running.**

**The first was not push-specific and was the more serious of the two: user secrets were being silently overridden by the placeholder file.** `WebApplication.CreateBuilder` adds user secrets, and `Program.cs` then appended `appsettings.Placeholders.json` *after* them — so anything set with `dotnet user-secrets` was accepted, stored, and then ignored. The symptom was the API refusing to boot on VAPID keys that were demonstrably set; the same trap was waiting for `Auth:Jwt:SigningKey` and for NEXT3's credentials the day #1 lands. Appendix A's whole premise is that a real value overrides a placeholder without a code change, and for the standard local-secrets mechanism it did the opposite. Env vars were unaffected, which is exactly why nobody had noticed. Fixed by re-adding user secrets after the placeholder file — **and that fix immediately produced a second-order problem worth knowing about**: with secrets in the chain, a Development test host reads whatever *that developer* has set, which turned a push test red on this machine and would have passed on every other. `ApiFixture` now runs the suite as **Production**, so it is hermetic; the app has exactly one `IsDevelopment()` branch, which is the line that was just added.

**The second was a screen telling an expert the opposite of the truth.** The panel offered "Enable notifications" on every page load even when this browser was already subscribed, because the hook never read the subscription that already existed. It now does, on mount — via `getRegistration()` rather than `ready`, which never resolves on a browser that has never registered a worker. Reading prompts for nothing, so it is safe on mount in a way that subscribing very much is not. Invisible to the suite because every test mounted with nothing subscribed; three tests now cover it.

**What the pass proved, live, end to end against a real push service.** The service worker registered at `/sw.js`. Two successive subscribes produced exactly **one** row and **zero** UPDATEs — EF saw identical values and issued no SQL at all, which is the upsert holding in the wild. An injected assignment logged `1 of 1 subscriptions accepted`, and Chrome displayed a real notification titled "New claim assigned" with `data.url = /expert/{assignmentId}`, `requireInteraction: true`, and the url as its tag; that URL loads E2 for the right claim. Unsubscribing the browser — a genuinely dead endpoint rather than a stub — then produced `410`, `Push subscription … revoked`, `0 of 1 subscriptions accepted`, and `Assignment … recorded, but notifying the expert failed`: slice 2.1's contract intact, with the assignment surviving and `notified_at` null. **Both refusal branches also fired against real Chrome** — a dismissed prompt as `denied`, and Chrome's *automatic block* after that dismissal as `blocked`. That distinction earned its keep within a minute of existing: pressing the button again genuinely cannot undo a block, and the panel said so instead of sending the expert round a loop.

**One thing genuinely left open, and it belongs on the week-6 device checklist.** When a browser rotates its push endpoint the old one 410s and is revoked correctly — but nothing re-registers it. The browser fires `pushsubscriptionchange`, and the service worker cannot act on it, because re-subscribing means calling an authenticated endpoint and a worker holds no token. So the expert silently stops receiving popups until they press "Enable notifications" again. Recorded rather than papered over; a real handset is the honest place to judge how often it happens.

**Status as of 2026-08-21 (slice 3.3):** **weeks 1 and 2 are complete and committed, and so are 3.1 and 3.2** (`a4be8ab`). **Week 3 is nearly done: slice 3.3 is complete and uncommitted**, awaiting the developer's test-diff review — so the working tree is 3.3 alone. **395 xUnit tests green (+39) and 1 skipped**; the web suite is untouched at 134. 3.3 built `RealNext3Client` against a contract we wrote ourselves, and wrote that contract: **`docs/next3-openapi.yaml` exists at last** — the artifact §8 item 3 has pointed at since week 0. **The fake to real swap did NOT happen and was never going to**: #1 is unanswered, there is no sandbox, and `Next3:Mode` is `fake` in every settings file. **The week-3 Friday status to the client must say so out loud** — "real client built and contract-tested, OpenAPI proposal ready to send, still running on the simulator, sandbox date unknown" — because if the sandbox is still absent at the week-4 demo, the day-for-day clock starts there.

**The slice's worst bug was in the card's own definition of done, and no single file could show it.** The card said 5xx, socket and timeout failures should propagate as `HttpRequestException`/`TimeoutException` — correct, except that `HttpClient` reports its *own* timeout as `TaskCanceledException`, which derives from `OperationCanceledException`, and `OutboxProcessor`'s catch filter excludes exactly that type. Left alone, a NEXT3 timeout would have escaped the worker mid-batch, abandoned the rows behind it, and stranded its own row in `processing` until the lease expired: never retried on §6.3's schedule, never listed on A2, with the only symptom a queue that quietly stops draining. The client converts it, using **the caller's cancellation token as the discriminator rather than the exception type**, and both directions are pinned — a genuine shutdown must stay a cancellation. **Reading the producer against the consumer is what found it. Neither file is wrong on its own.**

**`IBlobStore` had no way to read a blob back.** Put, Exists, Delete, List — and `UploadDocument` needs the bytes, because `DocumentPush` carries a blob key rather than a file (§7.3 keeps the binary in transit until the push is confirmed). Nothing had ever read one: the media pipeline writes and the cleanup job deletes, and both are satisfied by `Exists` and `List`. So the port gained `Open`, returning `Stream?` rather than an array — a 15 MB photo held in the outbox worker times a batch of ten is not a thing to do on purpose — and **absent is `null`, not an exception**, because §7.3 makes "these bytes were already cleaned up" terminal while "storage is unreachable" is worth retrying, and one exception for both would collapse the two. Both halves are in `BlobStoreContractTests`, so the Azurite adapter is held to the same contract.

**The predicted architecture failure fired, for a different reason, and the fix was better than the planned one.** The plan expected async state machines to trip rule 1's by-name exemption; NetArchTest filters those. What actually tripped it was four **hand-written nested records** — `RealNext3Client/Next3ClaimDto` is named `Next3ClaimDto`, so it is not exempt, and it carries a dependency on its declaring type. Moving them to namespace level satisfies the rule **as written**, so **no architecture rule was widened, weakened or touched**. All nine arch tests are green on the original rules.

**One production visibility change, stated plainly: `OutboxProcessor.IsTransient` went `private` to `internal`.** The classification is the load-bearing part of this slice — a 400 must not burn 26 h 36 m of retries and a 503 must not go `failed` on attempt 1 — and a test that copied the type list would only have proved the copy matched itself (2.4's lesson). The `[Theory]` over ten status codes now asks the real classifier. No behaviour changed and **no assertion was weakened or removed** anywhere in the diff.

**design.md was corrected in three places, the sixth slice running.** §6.3 promised that "until #32 is answered, the real client also keeps a sent-log check on our side" — **not built**, for three independent reasons: the outbox row's `sent` status already *is* the sent-log; a local check cannot cover the case it exists for, since the dangerous retry is the one after a **timeout** where nothing client-side knows whether NEXT3 accepted the push; and architecture rule 4 forbids `Api.Integrations.Next3` from referencing an outbox row at all, so it could not have been built where it was described. **The honest consequence belongs in the scope letter:** if NEXT3 does not dedupe on `clientRef`, duplicate documents under a visa are unavoidable on any timed-out retry, and nothing on our side prevents it. §6.1 absorbed the two endpoints the client document v0.5 had grown since it was written (`GET /experts/{expertId}/claims`, and master data for garages and claim officers as well as experts), so the OpenAPI file and the client document ask AXA for the same thing. And §12's #34 row claimed `IAssignmentSource` had "all three sources built" — it never did; webhook and poll still throw loudly if selected.

**No manual browser pass, and that is worth saying rather than leaving as a gap:** nothing user-facing changed. The equivalent blind spot here is the exception classification, which is why that test asserts through the outbox's own classifier, and why the timeout trap is pinned in both directions.

**Status as of 2026-08-21 (slice 3.2):** **weeks 1 and 2 are complete and committed**, and **3.1 is committed too** (`0244315`). **Week 3 is half done: slice 3.2 is complete and uncommitted**, awaiting the developer's test-diff review — so the working tree is 3.2 alone. **356 xUnit tests green (+18) and the web suite is 134 (+13).** 3.2 finished §5.1's expert surface: E1 filters the expert's **own** assignments by visa or cached plate, and E5's report upload is a panel on E2. **No migration and almost no new server code** — the search is one optional `q` parameter composed into the existing list statement, and E5 was one entry in `EXPERT_BUCKETS`, because 2.3 had already shipped `expert_report` with its doc type and PDF acceptance.

**The slice's real work was correcting design.md, not writing code.** §5.1 routed E1's search through `INext3Client.SearchClaims`, and two independent things made that unshippable. A NEXT3-wide search hands the expert claims they were never assigned — and the screen those results lead to carries a capture panel, so it is an invitation to attach photos to a stranger's visa, which is the precise failure this project exists to remove. And `ClaimSummary` has no policy number, no insured phone and no city, so a search hit **cannot** be upserted into the `claim` cache without inventing three fields NEXT3 owns. §5.1's search row and §6.1's claim-search row were both corrected (§6.1 said the operation "serves expert search and officer lookup", which would have contradicted §5.1 from the moment this shipped); `SearchClaims` stays on the port, still with no production caller, for the officer's lookup in 4.2. **Fifth slice running that design.md was corrected rather than quietly diverged from.** A recorded consequence: a cold cache has no plate to match, so such an assignment is findable by visa only — E1 says so on screen, and both halves are pinned by tests.

**The browser pass found the slice's only real bug, for the fourth slice running, and no test could have.** Search survived a reload exactly as the card asked — and was thrown away the instant the expert opened a claim, because E2's "← My claims" pointed at a bare `/expert`. An expert who searches a plate, opens the claim and comes back landed on their whole unfiltered history and had to retype it: on a phone, at a crash site, which is the entire situation the feature exists for. Nothing threw and every assertion passed, because **no test navigated**. E1 now carries `?q=` into each claim link and E2 hands it back, both pinned.

**Two more things worth knowing before reviewing.** **This slice adds the codebase's first hand-written analyzer suppression.** Case-insensitive search is `UPPER()` on both sides rather than a bare `Contains`, because nothing here configures a collation and a CS-collation deployment would otherwise change search behaviour with nothing going red — but CA1304/CA1311/CA1862 all assume the expression runs in .NET, and every fix they suggest (`ToUpper(CultureInfo)`, `Contains(string, StringComparison)`) has no SQL translation, so EF would throw on the expert's first search. Suppressed narrowly, at the statement, with the reason. Said honestly in the test: **it passes either way on LocalDB's default collation**, so the `UPPER()` is recorded, not proven. And **`expertKeys` gained a `lists()` prefix**: `invalidateQueries` matches by prefix, so once the search term is in the list key, invalidating `list()` no longer reaches `list('PLC-TEST')`. Verified by planting — with `list()` the new test goes red and every other web test stays green, which is exactly the failure mode. `useArrived` had the same bug and the card did not mention it.

**Verified in Chrome end to end against the fakes:** 11 keystrokes produced **1** request; `?q=` survived a reload *and* the claim round trip; expert Alpha searching Beta's plate and Beta's exact visa both returned nothing; whitespace-only returned the full list; 64 characters answered 200 and 65 answered `400 search_term_too_long`; `%` matched literally; and three report PDFs landed `not_applicable` / `PLACEHOLDER-DOC-05` / `application/pdf` / `.pdf` into *Expert documents* with `clientRef` = document id and outbox rows `sent` on attempt 1 — with the **filtered** list's media count going 2 → 3 without a reload, which is the `lists()` invalidation proven in a browser rather than only in jsdom.

**Status as of 2026-08-21 (earlier that day — slice 3.1):** **weeks 1 and 2 are complete and committed** (2.5 is `fb946a8`), and **week 3 has started: slice 3.1 is complete and uncommitted**, awaiting the developer's test-diff review — so the working tree is 3.1 alone. **338 xUnit tests green (+50) and the web suite is 121 (+49).** 3.1 put §5.1's last two expert artifacts through the pipeline 2.3 and 2.5 built: a **voice note** (record → `<audio controls>` playback-confirm → upload, §7.2 item 4) and a **damage diagram** (static SVG car, tap-to-mark, PNG via canvas — functional and unpolished, because §1 spent its polish on Option 2). Two new §7.1 buckets — `voice_note` under a new `MediaKind.Audio`, and `damage_diagram` registered as an *image* so the server's resolution floor applies to the export exactly as to a photograph — both `AllowUpload: false`, because both are produced in-app and there is no file to pick. One migration, db-reviewed. **Verified in Chrome end to end against the fakes:** the diagram landed `passed` / `PLACEHOLDER-DOC-07` / `.png` and the voice note `not_applicable` / `PLACEHOLDER-DOC-06` / `audio/webm` / 813,700 bytes, both into *Expert documents* with `clientRef` = document id and outbox rows reaching `sent` on attempt 1; all four cross-kind and picked-file refusals returned the right code and **wrote nothing** (exactly 2 documents after 4 refusals); Retake discarded without uploading; neither panel renders a file-picker control.

**Two things the browser pass could not settle, and one is a genuine gap.** **Playback was not verified.** The `<audio>` control renders, is bound to the right blob URL, and the recording is a real 813 KB WebM opening with the exact EBML magic the server sniffs — but this Chrome profile decodes no audio at all (a known-good 44-byte WAV stalls at `readyState: 0` identically), so *hearing* the note is the one thing left unproven. **It belongs on the week-6 device checklist.** Separately, a second recording failed loudly with "the recording did not complete" — which turned out to be the code working: the first recording's teardown had genuinely stopped the microphone tracks (`readyState: "ended"`), and the single-stream test stub handed back a dead stream where a real `getUserMedia` returns a fresh one.

**The playbook card and this file contradicted each other, and resolving it was the slice's design decision.** The card said route the diagram PNG through `useCapture.select`; §NEXT-SESSION said the diagram does *not* go through the clarity gate. `select` gates anything `image/*`, so both could not hold. HANDOFF won, via an explicit `select(file, origin, { photographic })` defaulted to true: a flat line drawing scored against `BlurVarianceThreshold` risks refusal with *"hold the phone still, let the camera focus on the damage"* — advice about a drawing, and 2.5's PDF-in-an-`<img>` bug in a new costume. **Deciding a file's treatment from its MIME type alone is the trap; the caller knows something the type does not.**

**The `db-reviewer` pass earned its keep for the fifth migration running, and again the finding was in the new code rather than the schema.** Every content-type comparison is case-insensitive, so `AUDIO/WEBM` is *accepted* — and the caller's spelling was then what got persisted, pushed to NEXT3, and matched **ordinally** by the blob-key extension map, filing a good voice note as `.bin` and hiding it from `WHERE content_type = 'audio/webm'`. Every accepting path now returns the **canonical** type the bytes were recognised as. **Accept loosely, store canonically.** The same pass caught that `Down()`'s loud failure — SQL Server validates a restored CHECK against existing rows — depends on the rollback running in a transaction: from a `--no-transaction` script the drop commits and the restore fails, leaving `document.bucket` unconstrained and every invented bucket string writable.

**A subfolder walked straight out of 2.5's reusability guard.** `diagram/` is the first nested folder in `media/`; the guard globbed `./*` with depth-fixed patterns, and a nested file reaches the expert module as `'../../expert/'`, which those patterns do not match at all. Both halves are hardened now and **verified by planting an import** — under the old pattern the violation passed silently, under the old glob the count went red. **A guard that quietly stops covering new code is worse than none, because it still reads green.**

**Status as of 2026-08-20 (slice 2.5):** **week 2 is complete.** Slices 2.1–2.4 are committed (`826e550`, `d099c93`, `79ff5d5`); slice 2.5 since committed as `fb946a8` — **weeks 1 and 2 are complete, working tree clean, 288 xUnit + 75 web tests green** (re-verified 2026-08-20). **Week-3 cards 3.1–3.4 expanded to verbatim prompts the same day** (`docs/build-playbook.md`); two decisions at expansion: E1 search is local to the expert's own assignments (design.md §5.1 corrected in 3.2 — a NEXT3-wide search would let an expert attach photos to claims never assigned to them), and 3.3's sandbox gate is closed, so it builds `RealNext3Client` against our own `docs/next3-openapi.yaml` and the mode stays `fake`. Next: slice 3.1. 2.5 built the capture component and clarity gate — the reusable pair that garage (5.1) and the Option 2 public page (5.3/6.1) must take unchanged, which is part of what design.md §11 spent to hold 8 weeks. E3 now sits under E2's Arrived panel with §7.1's four expert buckets, each running §7.2's resolution floor and Laplacian blur check before the E4 confirm screen, then posting through 2.3's streamed endpoint. **Verified in Chrome end to end against the fakes, across all four buckets:** sharp 1600×1200 PNGs passed at sharpness 19312 and drove `document` row → outbox row → dequeue → `sent`, with `size_bytes` matching the file on disk byte for byte and E1's media count reaching 4; a blurry one and an undersized one were both refused client-side with **no request leaving**; `origin` landed `captured` for the two car-photo buckets and `uploaded` for the two file-picked ones; a PDF stored `clarity_result = not_applicable` while a file-picked *image* stored `passed` (§7.2's "uploads included", holding in practice); Retake cleared the candidate and uploaded nothing; and the two car-photo buckets render **no file-picker control at all**. **Arrived was never pressed in the entire pass** — §5.1's ungated-capture interpretation demonstrated rather than asserted.

**The browser pass found the slice's only real bug, and the suite could not see it.** A PDF rendered into E4's `<img>` gives a broken-image icon on a black bar, over alt text reading *"The photo about to be sent to AXA"* — an expert asked to confirm a document they cannot see, miscalled a photo, on the one screen that exists so that a person looks at the file. Nothing throws: `naturalWidth` is simply 0, so every assertion still passed. E4 now names the file instead, pinned by a test. **A component test that never asks what the pixels did cannot see a broken image** — the same shape of gap as 2.4's timestamp bug, where the code's own arithmetic was asserted back to itself.

**The slice needed server code the playbook card did not mention, for a reason worth keeping:** a client-side gate needs client-side values, and CLAUDE.md's placeholder rule names *thresholds* explicitly — so 1024/768/100 could not be written into TypeScript, and a browser cannot read `appsettings.Placeholders.json`. Hence a new **anonymous `GET /api/config/media`**. Anonymous is the decision, not the shortcut: 5.3's public page runs this identical gate with no token. It sits outside `/public/*`, so §9.1's rate limits do not cover it — accepted because it touches no database and no user, and **recorded rather than assumed**. The same reasoning added one placeholder key: **a blur threshold is meaningless without the scale it was measured at**, since Laplacian variance scales with resolution and a bare `100` would pass and fail the same photo depending on the handset. `Clarity.BlurAnalysisMaxEdge` therefore lives beside the threshold in Appendix A, and design.md §7.2 was updated rather than diverged from — the 2.2 `visa_no` / 2.3 `outbox_message_id` precedent, now three for three.

**The 2.5 review point:** *the resolution floor had to be measured at native size, and that forced the API's shape.* Decoding a 48 MP photo into one RGBA array is ~192 MB — enough to kill the tab on the handset of an expert standing at a crash site — so the decoder returns native dimensions **plus** a bounded analysis buffer, and `assessClarity` takes the two separately. Had the floor been checked on the bounded buffer, a 1.1 MP photo would be refused as "too low-resolution": wrong, and unfixable from where the expert is standing. Also third time running for 1.5's lesson — the double-Confirm guard is a `useRef` latch, and removing it was verified to turn one upload into two, i.e. a duplicate photo under the visa and a second outbox row.

**Status as of 2026-08-20 (earlier that day — slice 2.4):** slices 2.2 and 2.3 committed (`d099c93`); 2.4 complete, since committed as `79ff5d5`. 283 xUnit tests green plus a **then-new 20-test web suite** (`npm test` in `src/Web` — Vitest + jsdom, stood up by 2.4 because the geolocation-denied path and the double-press guard are browser facts `dotnet test` cannot reach, and 2.5's clarity gate is entirely client-side). 2.4 is the first expert-facing screen work and the first *user action* to put a row in the outbox: E1 lists the expert's claims newest-first with media counts, E2 shows the claim and the **Arrived** button, and pressing it stamps `arrived_at` + coordinates and queues an `update_arrival` push in one transaction. Verified in the browser end to end against the fakes — arrival landed `sent` in `next3_outbox`; with location denied, the screen shows the blocking explanation and **no request leaves at all** (zero outbox rows, zero audit rows for that assignment). No migration: 2.1 created the arrival columns and 2.2 created `EnqueueArrival`.

**The `db-reviewer` pass earned its keep for the third slice running, and this time the worst finding was one the tests had *pinned*.** `ArrivalInfo` carried `DateOnly` + `TimeOnly` — §6.1's own words, written in 1.4 when nothing filled it. The moment 2.4 filled it from a UTC instant, the offset was discarded **at capture**: an expert arriving at 01:30 GST would be pushed to NEXT3 as arriving the previous calendar day, on the single field a claims dispute turns on (§9), in a row that can sit in the queue for 26 hours and that cannot be repaired from its own payload. It is a `DateTimeOffset` now; the split moved to `RealNext3Client` under a new `Next3:ArrivalTimeZone` placeholder, and design.md §12/Appendix A were updated rather than diverged from. The bitter part: the web layer had caught the *same* hazard on the display side that morning (bare timestamps read as local, "a four-hour error in GST that nobody would spot") and nobody connected it to the push side. **A lossy conversion belongs at the edge that knows the target format, never at the producer** — and a test that asserts the code's own arithmetic back to itself can never go red. The same pass also found that the new explicit transaction had to run inside `Database.CreateExecutionStrategy()`, or the day someone enables `EnableRetryOnFailure` (the standard Azure SQL fix, and §10 puts production there) Arrived breaks on *every* press with nothing in the suite to catch it; and that `ApiFixture` was proving both of this project's concurrency guarantees on LocalDB with `READ_COMMITTED_SNAPSHOT` **off**, while Azure SQL has it **on** — it is switched on now, all 283 pass under it, and the arrival guard was re-verified to discriminate under it.

**The 2.4 review point:** *"idempotent" had to be enforced in the `UPDATE`'s `WHERE` clause*, not by an `if`. Four simultaneous presses all pass a read-then-write check, and the consequence is not cosmetic — two `arrived_at` values and two pushes tell NEXT3 the expert arrived twice, at two different times. `WHERE arrived_at IS NULL` settles it; removing the clause was verified to turn the concurrency test from 1 arrival into 4. Because `ExecuteUpdate` runs outside `SaveChanges`, the endpoint opens the codebase's **only explicit transaction** so §4's one-transaction rule holds. A concurrency token on `ArrivedAt` — the 1.5/2.2 idiom — was rejected: EF applies a token to every write of the entity, so a concurrent E2 first-open would fail its `opened_at` save. **A guard that belongs to one transition should not be pinned to the whole row.** The same class of bug appears one layer up in the browser: `mutation.isPending` cannot stop a double tap in the same tick, because state has not landed yet — only a `useRef` latch does, and swapping it back was verified to turn one request into two.

**Status as of 2026-08-20 (earlier):** **Week 1 complete and committed** (1.1 scaffold + arch tests, 1.2 phone-OTP auth, 1.3 admin CRUD ×4 + append-only audit log + browser admin pages, 1.4 the five ports + fakes, 1.5 Option 2 schema + public-surface skeleton — all in git through commit `71023c9`). **Week 2: slice 2.1 (claim cache + expert assignments) is committed (`826e550`); slices 2.2 (the transactional outbox) and 2.3 (the server media pipeline) are complete and uncommitted, awaiting the developer's test-diff review.** 263 tests green; app boots on pure fakes. 2.2 is the load-bearing piece: every NEXT3 write now commits in the same transaction as the domain row that caused it, and a worker drains the queue with a `READPAST` stored procedure, retrying on §6.3's schedule and landing `sent` or `failed`. 2.3 is what finally puts a *file* into that queue — an expert streams a photo to `POST /api/expert/assignments/{id}/documents`, it lands in Blob, and a `document` row plus its outbox row commit together — and it makes §7.3's central rule real: **no blob is deleted before its outbox row is `sent`**, enforced by a structural join rather than by convention. **No client answers received; §8 communications still unsent.**

> ## ⏭️ NEXT SESSION: **send the three documents, run the demo** — then slice 5.1
> **There is no more week-4 code.** Weeks 1–3 are ☑ and committed, 4.1–4.3 are ☑ and committed (`783f32e`), and **4.4 is ☑ and uncommitted**. The next unticked slice is **5.1** (repair flow G4), and P3's one remaining item — the broker display-name snapshot column (pass-2 decision 3) — belongs with **5.2/5.3**, which own `broker_request`, rather than being back-fitted here.
>
> **Three things are due before any of that, and none of them is code.** `docs/scope-letter.md` and `docs/status-2026-08-22.md` are drafted and unsent; `docs/next3-openapi.yaml` has been unsent since 3.3. **All three go to Ramy before the demo** — the demo is where AXA forms its idea of what was agreed, and the letter is what stops that idea forming by itself (§8). The status keeps its two unsoftened sentences: the sandbox gate (#1) is still closed, and the day-for-day clock starts at the demo if it stays closed.
>
> **The scope letter gains thirteen rows from 4.4** (`scope-decisions.md`, all tagged slice 4.4). The ones a client will actually react to: **Sign out clears this device only** and no server-side revoke exists; **the damage diagram now names panels inside the image AXA receives**; **the invitation SMS carries a link** as well as the token; **a garage still sees the officer's approval image in its media count**; and **a rejected declaration still shows the garage no reason** — that last one is not new, but it is the one that will surprise users.
>
> **Then run the demo from `docs/demo-week4.md`.** It was rehearsed twice in 4.3 and re-driven in full during both of 4.4's browser passes; every button label in it is unchanged and `demo-reset.ps1` still runs in 18 s. Read its pre-flight properly — three Chrome profiles (load-bearing, not tidy), three permissions granted by hand in the expert profile, and a sign-in throttled for 60 s straight after the reset.
>
> **One real bug is still carried to 7.2** (`docs/demo-fix-list.md` #16): a browser holds **one push subscription per profile, not per user**, so the second user to sign in on a profile is told *"Notifications are on"* while the server has no row for them. 4.4 did not touch it. Note that `useSignOut` now clears the query cache, which fixes the *data* half of the shared-profile problem and not the subscription half.
>
> **Review 4.4's P2 with these seven in mind** (all in the playbook's 4.4 Notes and scope-decisions.md). **`ConfirmDialog` opens imperatively and never via an `open` attribute** — the reason is in its doc comment and it is the finding of the slice. **`useDecision` gained a `step`**, which is the only change to a hook in either half, and it is additive: the ordering and the latch are untouched. **`InviteService`, `AuthOptions`, `appsettings.Placeholders.json`, `launchSettings.json` and `demo-reset.ps1` are the entire server-and-scripts diff**, and `CapturingSmsSender` gained one accessor with no assertion changed. **`api/session.ts` is untouched** — `ui/roles.ts` gained a `home` label beside each shell's `name`, because a button that names a destination and a route that points at one are different things. **Two new unauthenticated routes** (`/invite` and `/invite/:token`) sit beside `/login`, outside every layout; the path is a contract with the SMS in the way `/garage/:id` is one with §8's push URLs, so renaming it breaks up to seven days of invitations already sent. **`auth/otp.ts` and `auth/useCountdown.ts` are shared by S1 and S2** — the two screens only look alike, so the shared parts are a module rather than a component. And **`LoginPage` and `InvitePage` deliberately use raw `fetch`**: they need a response *header*, which `api<T>()` throws away.
>
> **5.1 inherits from 4.4:** `ui/` is complete enough that G4's repair panels are `CapturePanel` with two more buckets and a `DocumentRow` list — G3's "What was sent" is the shape to copy. `PushIndicator` renders nothing for `deferred` on purpose (see scope-decisions), so a repair document that is genuinely `queued` will be the first thing to show "Queued, will send" on a garage screen. And §5.2's terminal chip string **"Repair documents sent"** already exists in `garage/labels.ts` and in `ui/tones.ts`'s map — 5.1 has to reach that state, not name it.
>
> ## ⏭️ PREVIOUS SESSION: 4.4's P2 — the screens, and S1 at last
> Build proceeds per `docs/build-playbook.md` — weeks 1–3 are ☑ and committed, 4.1–4.3 are ☑ and committed (`783f32e`). **4.4 is P1-complete and uncommitted, and its box stays unticked.** The next code is **4.4's P2**: screen polish from the canvases (E1/E2 states, G1–G3's five states, O2's three-column review with the approve-sequence labels, A1's row treatments and deactivate confirmation, login as two styled steps with a single OTP field + `autocomplete="one-time-code"`, and the S1 invite screens — which need the invite SMS to gain a URL and a route, pass-2 decision 2). P3 is one item, not two: the broker display-name snapshot column. **The other P3 item was dropped** — see the status above.
>
> **Three things are still due before any more code, and none of them is code.** `docs/scope-letter.md` and `docs/status-2026-08-22.md` are drafted and unsent; `docs/next3-openapi.yaml` has been unsent since 3.3. **All three go to Ramy before the demo** — the demo is where AXA forms its idea of what was agreed, and the letter is what stops that idea forming by itself (§8). The status must keep its two unsoftened sentences: the sandbox gate (#1) is still closed, and the day-for-day clock starts at the demo if it stays closed. **The scope letter gains five rows from 4.4** (`scope-decisions.md`): Sign out clears this device only, there is no phone bottom navigation, the damage diagram now names panels in the image AXA receives, the app self-hosts its fonts, and the garage's media count still includes the officer's approval image.
>
> **Then run the demo from `docs/demo-week4.md`.** It was rehearsed twice end to end and re-driven in full during 4.4's browser pass — every button label in it is unchanged and `demo-reset.ps1` still runs in 22 s. Read its pre-flight properly: three Chrome profiles (load-bearing, not tidy), three permissions granted by hand in the expert profile, and a sign-in throttled for 60 s straight after the reset.
>
> **One real bug is still carried to 7.2** (`docs/demo-fix-list.md` #16): a browser holds **one push subscription per profile, not per user**, so the second user to sign in on a profile is told *"Notifications are on"* while the server has no row for them. 4.4 did not touch it. Note that `useSignOut` now clears the query cache, which fixes the *data* half of the shared-profile problem but not the subscription half.
>
> **Review 4.4's P1 with these six in mind** (all in the playbook's 4.4 Notes and scope-decisions.md). **No existing test was edited anywhere in the diff** — the 50 new web tests are Sign out and `DesktopNav` (`ui/AppHeader.test.tsx`), the A1 mapping (`admin/saveErrors.test.ts`), the per-bucket accessible names (`media/CaptureNames.test.tsx`), and the two new guards in `media/reusability.test.ts`. **`car.tsx` changes the artefact NEXT3 receives** and is the one file here with a client-visible consequence. **`officer/api.ts` only re-exports now** — `Me`/`getMe` moved to `api/me.ts` so `AppHeader` could use them without `ui/` importing a role module, and `useOfficer.ts` changed one inline `['auth','me']` literal to the shared `meKey`, which is the same value. **`OfficerDeclarationPage`'s local `DocumentRow` is `ReviewDocument`** because of the name collision described above. **`index.css` is now four `@import`s** and two npm dependencies were added, both OFL-1.1. And **nothing under `src/Api` changed**, so the 501 xUnit tests are a regression check rather than coverage of anything new.
>
> **P2 inherits from P1:** `ui/` exists with Button, the four field components, StatusChip, the two banners, DetailTable, Worklist/WorklistCard, DocumentRow/PushIndicator, AppHeader, DesktopNav and AppShell, and the guard that keeps them role-agnostic. The tokens are in `styles/tokens.css` with the accent ramp as the only brand-swappable slot, and **dark-mode tokens are defined behind an unreachable selector** — turning them on is one selector, and it is still out of scope. **`PushIndicator` is wired into O2's document rows** — `DocumentDto` already carried `pushStatus` and `blobRetained`, so it is a real component rather than one shipped ahead of a caller (2.3's lesson: a boundary with nothing on the other side of it is untested). Wiring it meant deleting the duplicate swept-blob sentence that had been the row's body, because two places saying the same thing is the mildest form of the bug this codebase keeps naming. The login screen is styled but is still two steps with a plain code input — the single OTP field is P2's.
>
> ## ⏭️ EARLIER: 4.4 P1 — the design system, and the shells
> Build proceeds per `docs/build-playbook.md` — weeks 1–3 are ☑ and committed, 4.1–4.3 are ☑ and committed (`783f32e`). **New card 4.4 (2026-08-22): implement the design system from the three `/design` canvases** — `docs/design-prompts/pass-1-summary.md` is the contract, `pass-2-review.md` / `pass-3-review.md` carry the decisions; prioritised P1/P2/P3 with a stop line so one session lands a demo-ready result. Visual design was never in the BRD; the canvases are mockups and changed nothing in `src/Web` — 4.4 is where the app starts looking like them. After 4.4 the next unticked slice is **5.1** (repair flow G4).
>
> **Three things are due before any more code, and none of them is code.** `docs/scope-letter.md` and `docs/status-2026-08-22.md` are drafted and unsent; `docs/next3-openapi.yaml` has been unsent since 3.3. **All three go to Ramy before the demo** — the demo is where AXA forms its idea of what was agreed, and the letter is what stops that idea forming by itself (§8). The status must keep its two unsoftened sentences: the sandbox gate (#1) is still closed, and the day-for-day clock starts at the demo if it stays closed.
>
> **Then run the demo from `docs/demo-week4.md`.** It was rehearsed twice end to end; follow it rather than improvising, and read its pre-flight properly — three Chrome profiles (load-bearing, not tidy: see the push finding below), three permissions granted by hand in the expert profile, and a sign-in throttled for 60 s straight after the reset. `.\scripts\demo-reset.ps1` rebuilds the whole environment in 18 seconds if anything goes wrong mid-demo.
>
> **One real bug is carried to 7.2 rather than fixed here** (`docs/demo-fix-list.md` #16). A browser holds **one push subscription per profile, not per user**: the second user to sign in on a profile is told *"Notifications are on"* while the server has no row for them, and receives nothing. design.md §4 anticipated exactly this when it scoped the unique index to `(user_id, endpoint_hash)`; the client defeats it by reading the browser's subscription on mount and never POSTing it. It breaks no demo beat and the fix is a mount-time change in a component 5.2, 5.3 and 6.1 all reuse — so it is written down, not smuggled into a verification slice.
>
> **Review 4.2 with these six in mind** (all in the playbook's 4.2 Notes and scope-decisions.md). **`api/client.ts` gained `apiBlob` and an extracted `send()`** — the only change to a file every screen depends on, and the reason is in the doc comment. **`homePathFor` is a map now**, and its two new keys are a contract with slice 4.1's push URLs rather than a naming choice. **`media/diagram/render.ts` is a thin shim over the new `media/png.ts`** — the type aliases exist so `DiagramPanel` and its tests needed no edit at all. **The test diff touches three existing files**: `media/upload.ts` (a `fileName` field and 4.1's two refusal codes), `expert/api.ts` (the same field, additive), and `media/reusability.test.ts`, whose forbidden list gained `garage/` and `officer/`, whose floor was raised, and which now asserts it reaches `approval/` as well as `diagram/`. **No assertion was weakened or removed anywhere in the diff** — the only one that changed shape is G3's latch test, which was *strengthened* from `fireEvent` to three clicks in one `act` after planting showed the `fireEvent` version passed with the latch deleted.
>
> **4.3 inherits from 4.2:** every screen in beat 4 now exists, so the demo script can be written against real routes — `/garage`, `/garage/new`, `/garage/:id`, `/officer`, `/officer/:id`. `demo-reset.ps1` needs a garage and an officer seeded through the admin API alongside the expert, and both roles need their own browser profile (the officer's screens have no `PushPanel`, deliberately — §2 puts them on a desktop and §8 gives them email as well).

> ## ⏭️ EARLIER: slice 4.2 — **week 4 is the client demo**
> Build proceeds per `docs/build-playbook.md` — weeks 1–3 are ☑ and committed (3.4 is `34f7db7`); **4.1 is ☑ and uncommitted**, awaiting the test-diff review. The next unticked slice is **4.2**, the garage and officer screens. Paste the standing preamble + the 4.2 prompt. 4.3 is the demo-prep session and it **gates the demo on sending the scope letter and `docs/next3-openapi.yaml` first** — see §8.
>
> **Review 4.1 with these seven in mind** (all in the playbook's 4.1 Notes and scope-decisions.md). **`document` gained a third column §4 never listed** — `file_name`, because a deferred push is built in a different request from the upload that produced it. **Two DTOs moved out of `Api.Modules.Expert`** (`DocumentDto`, `ClaimDto`) so the declaration module does not reference the expert module to describe a document or a claim; `ExpertDocumentEndpoints` and `ExpertEndpoints` changed only to follow them. **`MediaUploadService.Upload` gained a fourth parameter** (`allowedBuckets`, defaulted null, so every existing caller is unchanged) and **`MediaUploadTarget.VisaNo` became nullable** — both load-bearing, both explained in the Notes. **`SafeFileName` stopped using `Path.GetInvalidFileNameChars()`**, which is a genuine production bug fix and not a refactor. **The test diff touches three existing files**: `MediaFlows` (a path overload for `Upload`, and `FileName` added to `DocumentBodyDto` — the field nothing had ever asserted), and `MediaBucketTests`, where `EveryExpertBucketLandsInTheExpertDocumentsFolder` is **scoped to assignment rows**. That last one is the only assertion in the diff that narrowed: it swept `AllRules` unfiltered and was right to until §5.2's buckets existed, and it is paired with a new test that pins the *Survey* buckets just as tightly plus a non-vacuity guard that fails if a third owner kind ever escapes both. **No assertion was weakened or removed anywhere in the diff.**
>
> **4.2 inherits from 4.1:** the whole server side of §5.2 is built and green, so 4.2 is screens plus the two **content endpoints** (nothing yet serves blob bytes over HTTP; `IBlobStore.Open` has existed since 3.3 with no caller). `DocumentDto` now carries `FileName`, which is exactly what O2's "named links for PDF/audio" needs — it would otherwise have had to be added there. Note the **approve sequence is already enforced server-side**: `409 approval_image_required` without the PNG, so 4.2's render → upload → approve ordering is pinned from both ends. `GET /api/config/media` now returns three more buckets, so `useCapture` will find the garage rows without a config change — but they still need a `MediaBuckets` entry *and* the migration, which 4.1 shipped, so 3.1's "not configured" ladder should stay quiet. And the officer's visa search is live at `GET /api/officer/claims/search`: `INext3Client.SearchClaims` finally has a production caller, five slices after it was written.
>
> **Two things are due at the week-3 boundary and neither is code.** The **Friday check** (playbook line 181): the fake→real NEXT3 swap did **not** happen, because the gate (#1) was closed all week. The status to the client must say so in as many words — "RealNext3Client built and contract-tested, OpenAPI proposal written and ready to send, still running on the simulator, sandbox date unknown" — and if the sandbox is still absent at the week-4 demo, **the day-for-day clock starts there**. And **§8's communications are still unsent**: the blocking questions, the scope letter, and `docs/next3-openapi.yaml`, which has existed since slice 3.3 and is the single highest-leverage thing not yet done. Week 4 ends in a client demo and a 30% payment; the scope letter must precede real client exposure.
>
> **Review 3.4 with these six in mind** (all in the playbook's 3.4 Notes and scope-decisions.md). **The NuGet package is not the one the card named** — `WebPush` is MPL-2.0, so this uses MIT-licensed `Lib.Net.Http.WebPush`; both licences were checked from the `.nuspec`. **`IPushSender.Send` changed shape** — a `PushMessage(Title, Body, Url)` record, because the payload needed a click target and §8 has four more push events coming that will each want one; `AssignmentHandler`'s `NotifiedAt` semantics are untouched. **The subscribe endpoint gained an SSRF allow-list and a per-user cap** that the card did not ask for, both from the db-reviewer pass. **A push row's `recipient_address` is the subscription id**, because the column is nvarchar(320) and endpoints are longer. **`eslint.config.js` gained a block** so `public/sw.js` is actually linted. **`Program.cs` and `ApiFixture` both changed for a reason that outlives this slice** — user secrets were being overridden by the placeholder file, and fixing that made the test host non-hermetic until it was pinned to Production. And **the test diff touches three existing files**: `SenderNotificationTests` for the new `Send` shape (with one assertion *added*, not changed), `ApiFixture` for a `PushOptions` monitor plus the Production pin, and `PortSelectionTests` for the mode switch — **no assertion was weakened or removed**.
>
> **4.1 inherits from 3.4:** the mode-gated `ValidateOnStart` pattern now has two instances (`Next3OptionsValidator`, `PushOptionsValidator`) and is the house shape for any live-mode credential. `PushMessage` is what §5.2's four notifications will carry — declaration submitted to officers, approved and rejected to the garage — and each is one call, but note the *recipients* are plural and unassigned (§5.2: "no per-officer routing — all officers"), so the sender's one-row-per-subscription logging will multiply. **1.5's lesson is now five slices deep** and 4.1's declaration transitions are the next place for it: an illegal transition must be unrepresentable, and "this may only happen once" belongs in the schema, not in an `if`.
>
> **Review 3.3 with these six in mind** (all in the playbook's 3.3 Notes and scope-decisions.md). **`docs/next3-openapi.yaml` is a client-facing deliverable and should be read as one** — it is the file §8 item 3 says to send, and it deliberately follows client doc v0.5's endpoint list rather than design.md §6.1's original seven, with §6.1 corrected to match. **`IBlobStore` gained a method** (`Open`), because the push was the first thing in the codebase that ever needed to read a blob back. **`OutboxProcessor.IsTransient` is `internal` now**, so the classification test can ask the real classifier instead of a copy of its list. **design.md changed in three places** — §6.3's sent-log promise reversed, §6.1's operation table widened, §12's #34 row corrected — and the §6.3 reversal has a consequence for the scope letter: without NEXT3-side dedupe, timed-out retries duplicate documents and we cannot prevent it. **No architecture rule was touched** even though one went red mid-slice; the fix was to move four nested DTOs out, not to widen the exemption. And **the test diff touches two existing files**: `BlobStoreContractTests` gained three `Open` assertions inside the shared `AssertContract`, and `OutboxProcessor`'s doc comment gained a paragraph — **no assertion was weakened or removed**.
>
> **3.4 inherits from 3.3:** `AddHttpClient` now exists in `AddPorts`, so `WebPushSender` is not the first `IHttpClientFactory` consumer and can follow the named-client shape. `Push:Mode = fake | webpush` should mirror the `Blob:Mode`/`Next3:Mode` switch that is now used three times. **The mode-gated `ValidateOnStart` pattern is built** (`Next3OptionsValidator`) and is exactly what 3.4's "`ValidateOnStart` when the mode is `webpush`" wants — note the two things that make it work: validation lives in `IValidateOptions` rather than a constructor, so `PortSelectionTests` keeps resolving implementations from a bare container; and it is registered only in the live mode, or the placeholder file stops the app booting. A **`StubHttpMessageHandler`** now exists in `Api.Tests.Integrations` for any outbound-HTTP test. And 3.4's `push_subscription` upsert is the fifth outing for 1.5's lesson — the once-only guard belongs in the unique index, not in a read-then-write.
>
> **Review 3.2 with these five in mind** (all in the playbook's 3.2 Notes and scope-decisions.md). **design.md changed in two places** — §5.1's search row and §6.1's claim-search row — because a NEXT3-wide expert search is an authorization hole and `ClaimSummary` cannot be cached; that is the slice's actual deliverable and the code is small. **The first hand-written analyzer suppression in the repo** sits on the search predicate (CA1304/CA1311/CA1862), because the analyzers' suggested fixes are untranslatable by EF — narrow, commented, and the CI-collation reasoning is recorded rather than proven. **`expertKeys.list()` became `lists()` + `list(q)`**, and both invalidation sites moved (`useRefreshAfterCapture` *and* `useArrived`, which the card did not name); the guard was verified by planting. **`ExpertAssignmentsPage` gained an optional `debounceMs` prop** — fake timers deadlock against @testing-library, so the debounce is tested the way `media/` tests its browser APIs, by injection. And **the test diff touches two existing files**: `ExpertAssignmentPage.test.tsx`'s `MEDIA_CONFIG` fixture gained an `expert_report` row (without it the new panel renders 2.5's "not configured" alert), and two capture-count assertions moved from 4/2 to 5/3 because E5 adds a panel with both controls — **no assertion was weakened or removed**, only the arithmetic. `ExpertFlows.SeedClaim` gained an optional `plateNo`, additively.
>
> **3.3 inherits from 3.2:** `INext3Client.SearchClaims` still has **no production caller** and now never will on the expert side — 4.2's officer lookup is its first one, so 3.3's contract tests are the only thing exercising it. The `document`/outbox path is unchanged. One known gap left open deliberately: **five capture panels label their controls identically** ("Take a photo" ×5, "Choose a file" ×3), which is a real screen-reader problem on E2 — not fixed here because `CapturePanel` is what 5.1 and 5.3/6.1 must reuse unchanged, and it is on the **week-6 device checklist**.
>
> **Review 3.1 with these five in mind** (all in the playbook's 3.1 Notes and scope-decisions.md). **`MediaValidation.CheckContent` gained an `out` parameter**, so ten existing call sites in `MediaValidationTests` grew an `out _` — the largest hunk in the test diff, and **no assertion was weakened or removed**. The parameter exists so that *what was validated is what gets stored*: a browser sends `audio/webm;codecs=opus`, and normalising in two places would give two answers that can disagree. **One placeholder key was added** — `Media.AudioContentTypes` — which is also the list the browser's recorder picks its format from, so no audio format literal exists in TypeScript; design.md §7.2 + Appendix A updated rather than diverged from, now four for four. **`useCapture.select` gained a third parameter**, defaulted so every existing caller and 5.1/5.3/6.1's future ones are unchanged. **`reusability.test.ts` was widened, not weakened** — glob to `./**/*`, patterns to depth-agnostic, plus a non-vacuity count and a "reaches into the subfolders" assertion; both halves were verified by planting an import. And **the migration is one check constraint**, regenerated from `MediaBuckets.All`, with a `Down()` caveat about non-transactional rollback written into the file.
>
> **3.2 inherits from 3.1:** the media pipeline now carries three kinds (image, document, audio) and `MediaKind` decides the allow-list. **E5 is nearly free** — `expert_report` already exists as a bucket with its doc type and PDF acceptance from 2.3, so it is one entry in `EXPERT_BUCKETS`. Note `CaptureSection` is no longer just that array: it also renders `VoicePanel` and `DiagramPanel`, so a change to the section's layout touches three call sites. **The media count helper is `countFor(bucket)`** now, shared by all three. Search adds `q` to `expertKeys.list`, and `useRefreshAfterCapture` is what all three panels invalidate through — if the key changes shape, capture stops refreshing the list it is looking at.
>
> > **One trap disarmed for 5.1:** `useCapture` reports `configLoaded` and `ready` as **two** flags. `findBucket` returns undefined both while the config is in flight and when a bucket is absent from §7.1's registry — collapsed, the second case renders "Loading photo settings…" for ever, with a clean network tab and an empty console. The garage buckets 5.1 adds need a `MediaBuckets` entry **and a migration** (`bucket` is check-constrained), so wiring the screen first is the likely order; the panel now says the section is unconfigured instead. **3.1 added a fourth rung to that ladder** for the diagram: a resolution floor the render cannot clear is said out loud, because otherwise a config change silently refuses every diagram as `image_too_small` and the drawing takes the blame.
>
> **Review 2.5 with these four in mind** (all in the playbook's 2.5 Notes and scope-decisions.md). **A new anonymous endpoint exists** (`GET /api/config/media`) — it is how the placeholder rule is honoured on the client, and its anonymity is 5.3's requirement pinned by a test, not an oversight; it is outside `/public/*` and therefore un-rate-limited, which is written down rather than assumed. **One placeholder key was added** (`Clarity.BlurAnalysisMaxEdge`) because a blur threshold without its measurement scale means a different thing on every handset; design.md §7.2 + Appendix A updated. **`withBearer` changed** — it now omits `Content-Type` for a `FormData` body, which is what lets the upload keep the refresh-on-401 retry instead of a second fetch path silently losing it. And **the test diff touches existing files**: `ExpertAssignmentPage.test.tsx`'s blanket `fetch` stub is now route-aware because E2 issues three GETs, and `ApiFixture` gained a mutable `ClarityOptions` monitor — no assertion was weakened or removed.
>
> **What 2.5 inherited from 2.4:** a **web test runner** (`npm test` — Vitest + jsdom + @testing-library; cleanup is wired by hand in `src/testing/setup.ts`, because with `globals: false` the library's own auto-cleanup never registers and every render stacks into the same document). **TanStack Query** is the server-state layer — logic in hooks under `src/Web/src/expert/`, components render, and `src/api/queryClient.ts` holds the defaults. E2 (`ExpertAssignmentPage.tsx`) is where the capture UI goes, and it must stay **ungated on arrival** (§5.1's recorded interpretation, asserted server-side by `ArrivedIsNotAPreconditionForCapture`). The upload endpoint and `MediaBuckets` from 2.3 are what the capture component posts to; note its multipart contract requires the metadata parts **before** the file part. `navigator.geolocation` and, by the same trick, any browser API can be stubbed with `Object.defineProperty` in both jsdom and a real Chrome tab — which is how the denied path was driven without a permission prompt blocking the automation.
>
> **Review 2.3 with these four in mind** (all in the playbook's 2.3 Notes and scope-decisions.md). **A stray `AsNoTracking()` in a composed subquery silently disarmed the retention sweep** — `OutboxSentQuery` projected to a `Guid` and looked free to mark no-tracking, but EF applies the tracking behaviour of the *combined* expression tree, so the outer query came back untracked, `blob_deleted_at` was never written, and `SaveChanges` reported success having done nothing: the bytes were deleted while the row still said they were held. Only the test asserting the *flag* caught it. **Architecture rule 4 had made `OutboxWriter` uncallable** — a called method's return type is part of the calling type's IL, so returning `Next3OutboxMessage` put every producer in violation; it returns `Guid` now, and 2.2 only looked fine because it shipped with no producers. **The `db-reviewer` pass again found more than the tests, and its worst finding was in my fix rather than the design**: the retention sweep deleted up to 500 blobs and committed once, so one undeletable blob discarded the `blob_deleted_at` and audit rows already staged for bytes that were physically gone — and the deterministic batch then stalled retention behind it for ever. It is one document per transaction now. And **`document` has two columns §4 did not list** — `outbox_message_id` (the structural join key §7.3 demands; no FK, because rule 4 keeps `Next3OutboxMessage` out of this module) and `blob_deleted_at` — with design.md §4 updated rather than quietly diverged from, exactly as 2.2 did with `visa_no`.
>
> **2.4 inherits:** the upload endpoint and `MediaBuckets` (§7.1 as data) are what 2.5's capture component posts to; E1 already returns `mediaCount`; `Retention__CleanupEnabled=false` joins `Outbox__WorkerEnabled=false` in `ApiFixture`, and the cleanup sweeps are driven by `CleanupRunner.RunOnce`. Local dev now wants **Azurite** (`Blob:Mode=azure` via launchSettings; `--skipApiVersionCheck` is mandatory — see CLAUDE.md), but `dotnet test` still needs nothing running.
>
> **Review 2.2 with these five in mind** (all in the playbook's 2.2 Notes and scope-decisions.md). **The test diff includes one deletion**: `FailureInjectionHarnessTests.cs`, 1.4's throwaway harness, whose own doc comment scheduled it for removal here. **§4's `claim_id uniqueidentifier` was unimplementable** — no uniqueidentifier claim id exists in the model and this table may hold no FK to the disposable `claim` cache — so it is `visa_no nvarchar(50)` and **design.md §4 was corrected**, not quietly diverged from. **The dequeue procedure gained `@now` and `@lease_seconds`**: without the first, a frozen `FakeTimeProvider` against `SYSUTCDATETIME()` makes every backoff timing untestable; without the second, a worker that dies mid-push strands its row in `processing` for ever, never retried and absent from A2's list. **"No duplicates" does not test READPAST** — two concurrent claims are disjoint either way, the second one just blocks — so the real test holds a transaction open and gives the other connection a short timeout; removing the hint was verified to turn it red. And **`OutboxWorker` was already a registered hosted service**, so the loop is disabled in tests (`Outbox__WorkerEnabled=false`) and `OutboxProcessor.RunOnce` is driven explicitly — the same trap waits for 2.3's cleanup job.
>
> **The `db-reviewer` pass found a real defect the tests had missed, and should now run on every migration.** The lease created a two-owner window — a reclaimed row is held by the new worker *and* the original, and without an arbitrator the slow one wins by arriving last, rewriting a `sent` row to `failed` or giving a `failed` row a `sent_at`. Since §7.3 deletes blobs on `sent` and A2 lists `failed`, both would then act on a status NEXT3 never agreed to. **`attempts` is the concurrency token now** — a reclaim increments it, so it is the claim's generation counter, and §4 needed no new column. It also caught that a row which keeps killing its worker would be reclaimed for ever (the give-up rule runs only when a worker survives), so the dequeue retires those itself via `@max_attempts`. Lesson worth keeping: **a fix for one hole often digs another beside it — audit the fix, not just the original design.**
>
> **What 2.3 inherited from here, and did:** the `document` row is written through `OutboxWriter` in the caller's transaction (architecture **rule 4** forbids any type outside `Api.Outbox` from touching `Next3OutboxMessage`, so there is no other way — which is also how the return-type problem above surfaced), and §7.3's **"no blob is deleted before its outbox row is `sent`"** is now enforced by a structural join. 2.3 also inherited the queue-clearing discipline: the dequeue claims across the whole table and cannot be scoped to one test's rows.
>
> Carried from 1.5: `TokenHashing` in `Api.Infrastructure` backs invites, refresh tokens and public links; architecture rule 2 keeps `Api.Modules.PublicSurface` clear of `Users` and `Next3`, and a `Rule2_IsNotVacuous` test stops that rule passing on an empty namespace. Note rule 2 is scoped to the public module only — `Api.Modules.Expert` may and does reference both. Carried from 2.1: **`Time.Advance` beyond 15 minutes expires the access token** (`ClockSkew` zero), surfacing as a 401 rather than an assertion failure — outbox tests dodge it only because they drive the processor directly instead of over HTTP.
> Still outstanding and getting more urgent as week 1 burns down: **§7A** (Capacitor research → `docs/research-capacitor.md`, validates the provisional Capacitor decision — needed before week 6, ideally sooner) and **§8** (blocking questions, scope letter, OpenAPI proposal — all still unsent; the scope letter must precede real client exposure).
>
> **Schedule decision 2026-08-19 (design.md §11):** Broker Option 2 stays IN scope; the calendar **holds at 8 weeks** — paid for by dropping **damage-diagram polish** and the **second UAT round**, plus pipeline reuse. No descope lever remains; any further slip moves the date day-for-day. `scope-decisions.md` and `estimate-and-plan.md` were reconciled to match the same day. Do not re-litigate Option 2 or the re-cut.

---

## 1. What this project is

AXA Middle East sent a BRD (`docs/source/`). Today their motor-claim photos travel by **email**: experts photograph accident damage at the roadside and email it in; one staff member ("Joanna") manually downloads thousands of emails and re-uploads each photo under the right visa number in **NEXT3** (AXA's claims core system). Expert reports take **2+ weeks**, so the claims team can't make a preliminary assessment or answer the insured / third party in the meantime.

The app replaces that email path: field users capture photos in-app, and the app pushes them into NEXT3 under the correct visa number.

Four profiles: **Expert**, **Garage**, **Claim Officer**, **Broker**.

Source material: `docs/BRD-extracted-text.md` (full text), `docs/diagrams/` (their four flowcharts), `docs/source/` (original .docx).

---

## 2. Stack — DECIDED

Changed from the initial proposal (was Next.js + Postgres). Current stack is the developer's own, and it is **the better choice for this client**: he is fast in it, and AXA is an Azure / Microsoft / almost-certainly-SQL-Server shop that will own and support this software after handover.

| Layer | Choice |
|---|---|
| API | **.NET 10** (LTS), containerized |
| Data | **Azure SQL Database**, **EF Core** + targeted stored procedures |
| Worker | .NET background service or Container Apps Job (outbox processor) |
| Web | **React + TypeScript**, built as a PWA |
| Mobile | **Same React app wrapped in Capacitor** — Flutter deferred, see §4 |
| Hosting | **Azure Container Apps**, `minReplicas: 1` |
| File storage | **Azure Blob** (transit buffer only) |

**Stored procedures: use surgically, not everywhere.** EF Core migrations + LINQ for the app's own domain — 27 open questions means requirements will move in weeks 3–6 and iteration speed matters more than purity. Reserve stored procs for three places that earn them: the outbox dequeue (locking semantics), any direct writes into NEXT3's own database, and bulk/reporting queries.

SQL Server outbox dequeue — `READPAST` is the equivalent of Postgres `SKIP LOCKED`:

```sql
UPDATE TOP (10) next3_outbox WITH (UPDLOCK, READPAST, ROWLOCK)
SET status = 'processing', attempts = attempts + 1
OUTPUT inserted.*
WHERE status = 'pending' AND next_retry_at <= SYSUTCDATETIME();
```

---

## 3. Architecture decisions — DECIDED

| Decision | Reasoning |
|---|---|
| **NEXT3 is the system of record for photos; the app holds files for days only** | Storage stays ~11 GB flat forever instead of growing ~68 GB/month. Requires a cleanup job and, critically, **never delete a blob before the NEXT3 push is confirmed sent**. |
| **Azure Blob, not Cloudflare R2** | R2 was recommended for zero egress fees — that mattered only while the app might be the permanent archive. As a days-only transit buffer, monthly egress (~68 GB) sits under Azure's 100 GB free allowance, so **R2's advantage is $0**. Meanwhile R2 would add a second vendor to clear through AXA procurement, a separate data-residency answer, and a second bill. *Lesson recorded: an architecture decision is only right relative to your other decisions — re-run the reasoning when an input changes.* |
| **Async processing via a transactional outbox** — not synchronous | Users are at accident scenes on bad connections; NEXT3 is a legacy core system with outages and maintenance windows. Sync means **NEXT3 down = experts cannot work**. Async means the queue drains on recovery and nobody notices. |
| **Container Apps with `minReplicas: 1`** | Scale-to-zero causes multi-second cold starts — unacceptable when an expert taps a claim notification at a crash site. ~$20–30/month, still well under App Service. Keep scale-to-zero for dev/UAT. |
| **Single tenant** | One client, he owns the software. No tenant isolation, no tenant-scoped queries, config in env vars not the database. Take this simplification everywhere. |

### The outbox pattern (core of the integration)

```
next3_outbox                       -- as built, slice 2.2; design.md §4 is the current contract
  id            uniqueidentifier
  visa_no       nvarchar(50)    -- was claim_id uniqueidentifier; there is no such id in the model
  operation     'upload_document' | 'update_arrival' | 'push_approval'
  payload       nvarchar(max)   -- JSON: blob keys, field values
  status        'pending' | 'processing' | 'sent' | 'failed'
  attempts      int
  last_error    nvarchar(max)
  next_retry_at datetime2
  created_at    datetime2
  sent_at       datetime2
```

Write the document row and the outbox row **in one transaction** — if they can't commit together you get documents never pushed, or pushes for documents that don't exist. Both surface weeks later as "AXA is missing photos", i.e. the exact problem this project exists to solve.

Worker loop: pick `pending` where `next_retry_at <= now` → push → success marks `sent`; failure increments `attempts`, records `last_error`, backs off (1min, 5min, 30min, 2hr…), and after ~8 attempts marks `failed`.

**Two things that are the classic bugs here:**
- **Make the push idempotent.** A retry after a timeout that actually succeeded must not duplicate the document in NEXT3. Send a stable `clientRef` with every push (open question — does NEXT3 dedupe on it?).
- **Never delete a blob before `status='sent'`.** Obvious, and reliably broken during a week-6 "cleanup" refactor.

**Build one admin screen listing failed pushes with a Retry button.** ~4 hours. It's the safety net, the debugging tool, the support answer, and a feature AXA will value more than half the BRD.

---

## 4. Frontend: PWA + Capacitor — the open research item

**The BRD asks for a mobile app, explicitly and four times:**

> *"a mobile application that can be **installed on mobile** or logged in PC"*
> *"Ability to use the user in a **mobile application or in browser**"*
> *"a mobile application that will be **installed on AXA experts network mobile**"*
> *"an invitation link is to be sent to his mobile number to **install the application**"*

**Why not a bare PWA.** A PWA is genuinely installable (home-screen icon, fullscreen, own splash) and skipping the app stores is arguably an advantage for a controlled internal network. But on **iOS**: Add-to-Home-Screen is a manual Safari-only three-tap flow that non-technical field users will not reliably complete, **and iOS web push only works after that install**. The BRD's entire expert flow is triggered by *"a popup message will show on the expert mobile"* — so an iPhone user who never installs simply never gets claims. That's a functional failure of the primary requirement, not a cosmetic gap.

**Why not Flutter.** React web + Flutter mobile = three codebases (.NET API, React, Flutter) and four languages, with every profile built twice. Adds an estimated **3–4 weeks** — turning 8 weeks into 11–12, unpaid at a fixed $5,000. Its real advantages (camera control, background push reliability) don't justify that here.

**Decision: React PWA wrapped in Capacitor.** One React codebase serves installed mobile app, desktop browser, and PWA fallback. Gets real store presence, native APNs/FCM push, and native camera/geolocation, for an estimated **1–1.5 weeks** instead of 3–4.

| Approach | Extra time | Codebases | Store presence | iOS push |
|---|---|---|---|---|
| PWA only | 0 | 1 | No | Fragile |
| **PWA + Capacitor** | **~1–1.5 wks** | **1 (+ thin shell)** | **Yes** | **Native, reliable** |
| Flutter native | 3–4 wks | 2 | Yes | Native, reliable |

Flutter is **deferred, not rejected** — revisit with evidence at the week-6 device-testing checkpoint if push or camera prove inadequate.

Known remaining costs: Apple Developer account ($99/yr, AXA's), Google Play ($25 one-off), and store review cycles. **The Mac question is largely answered (2026-08-18): Codemagic gives 500 free macOS M2 build minutes/month** — enough for this project — but *only on a personal account*, not a Team account, so the CI account stays in the developer's name while the store accounts are AXA's. Overflow is $0.10/min. Developer may also acquire a MacBook, and **owns an iPhone 17 Pro Max**, so real-device iOS testing at the week-6 checkpoint is covered at zero cost. §7 item 7 is therefore de-risked, not eliminated — still confirm the Capacitor iOS build actually runs on Codemagic's free tier before relying on it. **Not mentioned to the client: this costs AXA nothing.**

---

## 5. Commercial position

- **$5k / 2 months is already committed.** Independent estimates for the full BRD: ~300–430 person-days with a team, or 9–13 months solo. The commitment stands; the strategy is **scope control, not renegotiation** — don't spend the user's time re-deriving that gap.
- Roughly **60–70% of the BRD** fits the timeframe. Exclusions in `docs/scope-decisions.md` must be agreed **in writing before code starts**.
- **2026-08-19: Broker Option 2 moved INTO scope** by the developer, knowing it costs ~1.5–2.5 weeks and was the primary negotiating chip. The chip is spent — the remaining give is offline mode (already out), damage-diagram polish, and the second UAT round. Say so early if the date moves.
- **IP ownership is unresolved and worth money.** "He owns the software" would transfer full IP for $5,000. Propose instead: AXA gets full source, a perpetual unlimited licence, and the right to modify — developer retains the right to reuse **generic, non-AXA-specific components** (auth, upload pipeline, outbox) in future work. AXA loses nothing they care about; that reuse is worth more than this contract. If they insist on full transfer, price it rather than give it away silently.
- Payment: **40% up front / 30% at week-4 demo / 30% at handover.**
- ~~Two UAT rounds~~ **One UAT round** (2026-08-19 — the second round was spent to fund Broker Option 2; see design.md §11), tightly defined and capped in writing.
- Define "handover complete": source in their repo, deployment runbook, account ownership transferred, one training session. Otherwise "he owns it" becomes unpaid support forever.
- All third-party costs (SMS, hosting, domain, developer accounts) on **AXA accounts, AXA card**.

---

## 6. NEXT3 — what we actually know

**Searched the web on 2026-08-18: NEXT3 has no meaningful public presence as a commercial insurance product.** Results returned generic core-system vendors (Majesco, Sapiens, FINEOS) and the unrelated US insurer "NEXT Insurance". Not proof — a small regional vendor can have near-zero footprint — but the BRD's own wording is stronger evidence:

> *"To have **the possibility** to integrate the pictures … in NEXT3"* — they list as a prerequisite whether integration is even *possible*
> *"To **identify the directory** to upload photos and **insert records**"* — file share + direct DB writes, not a product API
> *"To have **visibility on fields** that will be updated"* — schema-level thinking
> *"To **extract** the data of experts … and **provide this list**"* — a manual export

**Working conclusion: bespoke or heavily customised, no API-first design.** So *"they will provide API endpoints"* most likely means **someone will build endpoints for you** — which makes NEXT3 availability the **#1 schedule risk, above InfoSec**.

**Highest-leverage action available: write the API contract yourself.** Don't wait to receive a spec. Send an OpenAPI document defining the ~8 endpoints needed:

| Endpoint | Purpose |
|---|---|
| `POST /auth/token` | Service authentication |
| `GET /claims/{visaNo}` | Claim details (visa, policy, plate, insured name + phone, car make/model, city, accident date) |
| `GET /claims/search?plateNo=&visaNo=` | Expert's "search previous claims" |
| `POST /claims/{visaNo}/arrival` | Arrival date, time, lat/lng |
| `POST /claims/{visaNo}/documents` | Multipart + `docType`, `folder` (Expert documents / Survey), **`clientRef` for idempotency** |
| `GET /experts` | Master-data sync (id, name, phone, active) |
| `GET /visas/search` | Claim officer's visa lookup |
| **Assignment delivery** | **Genuine hole in the BRD** — it says a *"back-office engine triggers the process"* without saying how that reaches the app. Webhook from NEXT3 (preferred) or the app polls. Decide it. |

Sending this converts a vague promise into a reviewable, datable deliverable; gets the endpoints built to fit the app; and makes any variance visibly theirs.

**Architectural protection already in place:** the NEXT3 client sits behind an interface with a working fake implementation, so all four modules can be built while AXA is still deciding. That covers roughly through week 3 — beyond that the date needs renegotiating, and say so early.

---

## 7. ✅ DONE — the design document

`docs/design.md` was written from the brief that used to live here, and is the build's reference; `docs/build-playbook.md` carries the slice-by-slice cards. **The live next-session pointer is the banner at the top of this file, not this section.**

Two items came out of the manager's review of client doc v0.4 on 2026-08-19. Both are checked against `design.md` below rather than assumed:

- **Authorization scoping** (open question #42). The manager's sharpest point: how does the app know which claims an expert may see, and which declarations a garage may see? Largely already handled — `design.md` §9 enforces role plus resource-level checks server-side, and slice 3.2's expansion corrected §5.1 so expert search is **local to the expert's own assignments**, precisely because a NEXT3-wide search would let an expert attach photos to claims never assigned to them. **The residual dependency is on AXA:** if NEXT3 never exposes the expert-to-claim linkage, the app's own assignment records are the only source of truth for it, and that limitation belongs in the scope letter.
- **Offline capture** (open question #45). `design.md` keeps it out of scope, and §11 states plainly that there is **no remaining descope lever** — diagram polish and UAT round 2 are already spent holding 8 weeks with Broker Option 2 in. Client doc **v0.5 Q13 offers it to AXA at roughly one extra week**. If they say yes, there is nowhere for that week to come from. **Resolve this before the document is sent** — either withdraw the offer, or price it as a paid extension rather than absorbing it.

Two more from the same review, for the record: **image fraud/tamper detection appears nowhere in `design.md`** and is correctly excluded in v0.5 Q9 — keep it that way unless separately quoted. And **the clarity gate was already built in slice 2.5** (resolution floor + Laplacian blur + confirm screen, voice = playback-confirm only), so v0.5 Q14 asks the client to confirm something that already exists in code; if AXA answers "manual review queue" it is rework, not a choice.

---

## 7A. Deferred — research brief: PWA + Capacitor

**Goal:** validate or kill the PWA + Capacitor decision in §4 **before** week 1 coding starts, and produce a concrete build/distribution plan. **Deferred behind the design doc (§7), but still needed before coding** — the design doc should record the Capacitor choice as provisional and flag anything that depends on it.

### Must answer

1. **Capacitor + React current setup** — current major version, project structure alongside a React/Vite app, how the web build feeds the native shell, what the dev loop looks like (live reload on device).
2. **iOS push via Capacitor** — APNs certificate/key provisioning, what an Apple Developer account must have configured, whether it works reliably when the app is backgrounded or killed. **This is the single most important question** — the BRD's expert flow depends entirely on the popup arriving.
3. **Android push** — FCM setup, and specifically **whether aggressive-battery-saver OEMs (Xiaomi, Huawei, Oppo, Samsung) kill background delivery.** Common in MENA. Find the known mitigations.
4. **Camera: forcing capture, blocking gallery upload.** The BRD requires car photos be **capture-only** (`Insured Car Photo`, `TP Car Photo`, garage car photos) while document buckets allow both. Confirm the Capacitor Camera plugin can enforce camera-only, on both platforms.
5. **Voice recording** — which plugin, what formats, file sizes, iOS permission behaviour.
6. **Geolocation** for the "Arrived" button — accuracy, permission prompts, behaviour when denied.
7. **Building iOS without a Mac** — evaluate Codemagic, Bitrise, Ionic Appflow. Free tiers, paid tiers, monthly cost, setup effort. **This is a hard blocker if unsolved.**
8. **Distribution** — public App Store / Play Store vs enterprise/MDM internal distribution. Which suits a controlled network of experts and garages? What does each require from AXA?
9. **Client-side image quality check** — a resolution + blur-variance (Laplacian) check in the browser/webview, to implement the BRD's *"image visibility must be ensured"*. Find a workable approach and a sane threshold. **Explicitly not ML.**
10. **Sanity-check the alternatives** — is Capacitor still the right pick in 2026 versus a bare PWA or a React Native rewrite? Look for anything that has changed.

### Also confirm

- PWA fallback quality for the desktop/browser users (claim officer, broker, admin) from the same codebase
- Offline behaviour available "for free" — deferred as scope, but worth knowing what comes at no cost
- Realistic revised estimate for the Capacitor wrapper: is 1–1.5 weeks right?

### Deliverable

Write findings to **`docs/research-capacitor.md`**, ending with a clear **go / no-go** on PWA + Capacitor and, if go, a concrete build-and-distribution plan slotted into week 6 of the plan in `docs/estimate-and-plan.md`.

### Blocked on the client for

Questions **28, 29, 30** in `docs/open-questions.md` — device mix (iOS vs Android share across the expert/garage network), store vs MDM distribution, and whether AXA already holds an Apple Developer account. **If the network is 80%+ Android** (plausible in MENA), the iOS risk shrinks sharply and Android-only Capacitor needs no Mac at all — which would materially simplify everything above. **Chase these answers in parallel; don't block the research on them.**

---

## 8. Immediate next actions (non-coding, still outstanding)

1. **Send the blocking questions** — the 8 in `docs/open-questions.md` § Blocking, plus #21 (InfoSec/pen test), #23 (PWA acceptable), #28–30 (devices). Prioritise **#1, #2, #21, #31** — those decide whether 2 months is real.
2. **Send the one-page scope letter.** **✅ DRAFTED — `docs/scope-letter.md` (slice 4.3, 2026-08-22). Unsent.** Email acknowledgement is sufficient. It states that **Broker Option 2 IS included** and what that cost (diagram polish and the second UAT round), lists the written exclusions, names the dated dependencies, and puts three decisions in front of AXA before they are discovered: rejection shows the garage no reason, rejection is final, and **§6.3's duplicate caveat** — without NEXT3-side dedupe on `clientRef`, a timed-out retry can file a document twice and nothing on our side prevents it. `docs/status-2026-08-22.md` is drafted alongside it and carries the sandbox gate and the day-for-day clock. **Sending is the developer's act; nothing has been sent from a build session, and slice 4.3's gate says the demo does not happen until all three are out.**
3. **Send the NEXT3 OpenAPI proposal** (§6). Fastest way to force clarity on the biggest unknown. **✅ WRITTEN — `docs/next3-openapi.yaml` (slice 3.3, 2026-08-21). It is finished and STILL UNSENT as of 4.3; send that file with the scope letter.** It specifies the nine calls plus the inbound assignment callback, makes `clientRef` a *required* parameter with the dedupe stated as a requirement on NEXT3 (#32), asks for a sandbox server (#1), and surfaces #2/#5/#6/#8/#12/#34/#42 in the description text so an AXA reviewer meets them. It validates clean under `redocly lint`. Note it deliberately matches the **client document v0.5 §3.1 endpoint list**, not design.md §6.1's original seven — §6.1 was corrected to match, so the two documents AXA receives agree.
4. **Agree payment milestones and the IP position** (§5).
5. **Confirm all infra accounts are in AXA's name, on AXA's card.**
6. Then scaffold — week 1 of `docs/estimate-and-plan.md`.

**Say out loud to the client now, not later:** every day the NEXT3 sandbox credentials slip, the delivery date moves day-for-day.

---

## 9. Context an AI session won't infer

- **No client answers have been received.** Every TBC is genuinely unknown — **do not invent** insurance types, email routing recipients, NEXT3 field names, or document-type codes.
- Client contact who last edited the BRD: **HADDAD Ramy**, AXA Middle East. Document created 2026-08-05.
- The three things most likely to break the 2 months: **NEXT3 endpoints not existing yet**, **AXA Group InfoSec review / pen test**, and **UAT scope creep in weeks 7–8**.
- The developer is deliberately building cost-estimation skill — cost reasoning is welcome, and decisions should be explained in terms of unit economics (cost per claim) and total cost of ownership including his own time (~$30/hr effective on this contract), not sticker price.

---

## 10. Repo map

```
docs/
  BRD-extracted-text.md     Full text extracted from the client .docx
  source/                   Original client .docx (unmodified)
  diagrams/                 The client's 4 flowcharts as PNG
  scope-decisions.md        In scope / out of scope / simplifications
  open-questions.md         40 questions for the client, prioritised
  estimate-and-plan.md      8-week plan, hosting options, monthly cost
  client-doc-src.html       SOURCE of the client Word document - edit content HERE
  build-docx.ps1            Regenerates the .docx from that HTML. Never hand-edit the .docx:
                            LibreOffice's HTML import breaks table widths, margins and the
                            Word-version stamp, and this script patches all of it. Re-run as
                            `pwsh -File docs\build-docx.ps1 -Version 0.3`
  AXA-Motor-Claims-Questions-and-Costs-v0.5.docx   The client document (6pp), NOT yet sent
                            v0.5 = v0.4 + the manager review of 2026-08-19: authorization scoping,
                            photo rules/fraud, master data for all user types, and a new §5 asking
                            mobile-vs-PC parity, offline behaviour and clarity-check automation.
                            v0.4 = client's own heavy cut of v0.3, folded back into the HTML
                            source. Now covers ONLY: purpose, what's needed at a glance, NEXT3
                            endpoints + 9 questions, and hosting/SMS/mobile cost. Client deleted
                            the title block, the schedule statement, connectivity options, the
                            OpenAPI note, the SMS provider options, and all of security /
                            mobile delivery / remaining questions / next steps. Those cuts are
                            deliberate - do not restore them without asking.
  client-email-2026-08-18.md  Covering email to Ramy, NOT yet sent
  design.md                 Internal engineering design (§7) — WRITTEN 2026-08-19. The build contract.
  build-playbook.md         ~25 ordered build slices with per-session prompts + milestone checklists — written 2026-08-19.
                            Progress ticks + per-slice Notes live here (1.1–1.3 done as of 2026-08-19)
  next3-openapi.yaml        The NEXT3 API contract WE wrote (§6, §8 item 3) — written 2026-08-21, slice 3.3.
                            NOT yet sent. The nine calls + the inbound assignment callback; clientRef
                            required; every open question surfaced in the description text.
  research-claude-workflow.md  How to drive the build with Claude Code — written 2026-08-19
  design-prompts/           Three `/design` briefs (foundations → phone screens → desktop screens), written 2026-08-22.
                            Visual design was never in the BRD; run pass 1 before the demo, implement as a styling slice.
  research-capacitor.md     ← still unwritten (§7A). Validates the Capacitor decision; needed before week 6
AxaMotorClaims.sln          Week-1 solution (slices 1.1–1.3)
src/
  Api/                      .NET 10 minimal API. Modules/Users (auth, profiles, admin CRUD),
                            Modules/Audit (append-only audit_log + AuditWriter), Modules/Notifications
                            (notification log + NotificationLog writer), Modules/Claims (the `claim`
                            NEXT3 cache + ClaimCache, §4's refresh-on-open rule — shared with the
                            officer's lookup in 4.2), Modules/Expert (expert_assignment, the single
                            idempotent AssignmentHandler + its startup subscription, E1/E2 read
                            endpoints, the Arrived endpoint — the codebase's only explicit
                            transaction, because ExecuteUpdate's WHERE clause is what makes an
                            arrival happen exactly once — admin dev-injection endpoint, and E1's
                            local `q` search: no NEXT3 call, design.md §5.1 corrected in 3.2),
                            Modules/Broker (broker_request
                            + B3 create-link), Modules/PublicSurface (the ONLY unauthenticated surface:
                            public_link_token, /public/* endpoints, chained per-IP/per-token rate
                            limiter, body-size cap — may not reference Users or Next3, arch rule 2),
                            Integrations/ (the 5 ports + fakes: Next3 incl. IAssignmentSource, Email,
                            Push, Sms; FakeBehavior = shared latency/failure injection; and since
                            slice 3.3 the REAL NEXT3 client — RealNext3Client + Next3TokenProvider +
                            Next3RejectedException + Next3OptionsValidator, built against our own
                            docs/next3-openapi.yaml because #1 is unanswered. Next3:Mode is still
                            `fake` everywhere; the client exists so the swap is a config value),
                            Modules/Media (§7's pipeline: the `document` table, MediaBuckets = §7.1's
                            matrix as data, ImageHeader + AudioHeader + MediaContentType (parameter
                            stripping; validated type = stored type) + MediaValidation = §7.2 item 5's
                            server re-validation, MediaUploadService = the streamed multipart endpoint's
                            blob-then-rows ordering, MediaBlobCleanupTask = §7.3's two sweeps,
                            MediaConfigEndpoints = the anonymous GET /api/config/media that serves
                            §7.2's thresholds + §7.1's bucket rows to the browser, so no client
                            value is ever written into TypeScript),
                            Infrastructure/Cleanup (ICleanupTask + CleanupRunner + CleanupWorker,
                            schedule split from behaviour like the outbox worker),
                            Integrations/Push (IPushSender + FakePushSender + the real WebPushSender,
                            push_subscription, PushOptions/PushOptionsValidator; Push:Mode =
                            fake | webpush, fake everywhere, VAPID keys in user-secrets only),
                            Modules/Push (the three subscribe/unsubscribe/vapid-key endpoints;
                            Api.csproj gained a UserSecretsId so CLAUDE.md's commands work),
                            Integrations/Blob (the sixth port: IBlobStore + InMemoryBlobStore fake +
                            AzureBlobStore; Blob:Mode = fake | azure, fake everywhere by default;
                            gained Open() in slice 3.3 — the push is the first thing that ever read
                            a blob back, and absent is null rather than a throw),
                            Outbox/ (next3_outbox + the READPAST dequeue proc, OutboxWriter = the
                            one-transaction enqueue every producer uses, OutboxProcessor = the §6.3
                            retry/backoff loop body, OutboxWorker = its schedule; the ONLY namespace
                            that may call INext3Client push ops or touch outbox rows, arch rules 3+4),
                            appsettings.Placeholders.json (Appendix A — ALL client-value placeholders)
  Api.Tests/                xUnit: NetArchTest boundary rules (with planted-violation self-tests) +
                            integration tests on LocalDB via WebApplicationFactory (356 tests;
                            READ_COMMITTED_SNAPSHOT is switched on to match Azure SQL).
                            Media/ holds TestImages (synthetic JPEG/PNG/PDF headers), TestAudio
                            (WebM/MP4/Ogg container headers, slice 3.1) and the
                            IBlobStore contract tests, whose Azurite half self-skips when the
                            emulator is not listening — so the suite needs no Docker
  Web/                      React 19 + TS + Vite. Router, localStorage JWT + refresh-on-401 client,
                            admin pages (login, per-kind profile list/form). Dev proxy → API :5180.
                            Slice 2.4 added: TanStack Query (api/queryClient.ts), role-based
                            post-login routing (api/session.ts — navigation only, server still
                            enforces), UTC normalisation for bare API timestamps (api/datetime.ts),
                            expert/ (api + query hooks + geolocation + useArrived — all E1/E2 logic),
                            pages/ExpertAssignments*.tsx (E1/E2), and a Vitest suite (`npm test`;
                            testing/setup.ts registers @testing-library's cleanup by hand).
                            Slice 2.5 added: media/ (§7.2's gate + §7.1's capture UI — clarity.ts is
                            the pure core, decode.ts wraps the browser's canvas behind an injectable
                            parameter, useCapture + CapturePanel + ClarityConfirm are E3/E4, upload.ts
                            builds the ordered multipart). media/ imports NOTHING from expert/, pages/,
                            admin/, session, tokens or the router — asserted by reusability.test.ts,
                            because 5.1's garage flow and 5.3's public page must reuse it unchanged.
                            Slice 3.1 added: media/recorder.ts + useVoiceNote.ts + VoicePanel.tsx
                            (§7.2 item 4's playback-confirm; the recorder is injected, never stubbed,
                            and takes its formats from the server) and media/diagram/ (regions.ts =
                            the car as data, marks.ts = pure tap state, car.tsx = the SVG that IS
                            what gets exported, render.ts = injectable canvas renderer at a fixed
                            size above §7.2's floor, DiagramPanel.tsx). reusability.test.ts now
                            globs `./**/*` with depth-agnostic patterns, so the subfolder is covered.
                            Slice 3.2 added: expert/useAssignmentSearch.ts (E1's search box state —
                            the URL is the source of truth, `?q=` survives a reload and the trip into
                            a claim and back; the debounce delay is injectable, because fake timers
                            deadlock against @testing-library). expertKeys grew a `lists()` prefix:
                            once the search term is in the list key, invalidating `list()` no longer
                            reaches the list on screen. E5 is one entry in EXPERT_BUCKETS.
                            Slice 3.4 added: push/ (subscription.ts = the injectable serviceWorker/
                            pushManager/Notification seam in the geolocation idiom, urlBase64.ts,
                            usePushSubscription + PushPanel) and public/sw.js (plain service worker,
                            no vite-plugin-pwa; linted via an eslint.config.js block, not type-checked).
                            152 web tests total
.claude/
  settings.json             Hook registrations (post-edit format check, on-stop test run)
  hooks/                    Guarded PowerShell hooks — no-op until the solution exists in week 1
  skills/scaffold-module/   Skill: scaffold an API module per design.md §5 pattern
  skills/add-ef-migration/  Skill: add + safety-review + apply an EF Core migration
  agents/db-reviewer.md     Read-only subagent auditing EF migrations
CLAUDE.md                   Domain glossary + conventions + design.md import (the per-session contract)
HANDOFF.md                  This file
```
