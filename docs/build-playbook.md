# build-playbook.md — session-by-session build plan

**What this is:** the ordered list of ~25 build slices for the 8-week plan in `design.md` §11, one slice per Claude Code session, each with its prompt. The spec lives in `design.md`; this file only points at it.
**Status:** written 2026-08-19, before week 1. Weeks 1–2 carry verbatim prompts; weeks 3–8 are outline cards marked *expand when reached*.

---

## How to use it

**The session ritual:**
1. Pick the first unticked slice (they are dependency-ordered — don't skip ahead).
2. Start a Claude Code session in **plan mode** and paste the slice's prompt (prepend the standing preamble below if the session is fresh).
3. Review the plan Claude proposes — check it against the design.md §§ the slice cites — then approve and let it execute.
4. Run the slice's **definition of done**. Green = tick the box, write a one-line learning in *Notes* (what surprised you; the week-upgrade session feeds on these).
5. Review the **test diff** yourself before committing. Claude stops before commit by instruction.

**The drift rule (why prompts look thin):** prompts reference `design.md` §N and never restate its content — no transition tables, no column lists, no config values in this file. When a client answer lands, update `design.md` + the placeholder config; this playbook stays untouched. One source of truth.

**The fidelity rule:** at the start of each week from week 3 on, run one short session: *"Upgrade the week-N cards in build-playbook.md to verbatim prompts, using the Notes from completed slices."* Don't expand weeks early — what week 2 teaches changes how week 6's prompts should read.

**The Friday check:** every Friday, compare ticked slices against the week's row. A week that didn't finish is a **day-for-day slip signal** — per `design.md` §11 there is no descope lever left, so the slip is said to the client immediately, not absorbed silently.

**Slice anatomy:** `☐` status · goal · design.md refs · prompt (or outline) · definition of done (DoD) · test focus · notes.

---

## Standing session preamble (paste at the top of a fresh session's first prompt)

> Work on the slice below from docs/build-playbook.md. Start in plan mode: read the design.md sections it cites **before** proposing anything, propose the file list + edge cases + test list, and wait for approval. Rules: placeholder discipline (design.md Appendix A — no client literals outside the placeholder config); never weaken or delete a test to make it pass without saying so; definition of done includes `dotnet test` green and the architecture tests; stop before committing so I can review the test diff.

---

## Week 1 — Foundation

### ☑ 1.1 Solution scaffold + guardrails
**Goal:** the repo compiles, tests run, and the architecture rules are executable.
**Refs:** CLAUDE.md (source layout), design.md §3 (boundaries), §10 (build conventions).
**Prompt:**
> Slice 1.1 — scaffold the solution per CLAUDE.md's source layout: `AxaMotorClaims.sln`, `src/Api` (.NET 10 minimal API, vertical-slice module folders), `src/Api.Tests` (xUnit), `src/Web` (Vite + React + TypeScript strict). Wire EF Core to LocalDB (`Server=(localdb)\MSSQLLocalDB;Database=AxaMotorClaims;Integrated Security=true`) with a first empty migration. Turn on: `nullable enable`, `TreatWarningsAsErrors`, .editorconfig + analyzers, ESLint as a build error. Add a NetArchTest test project encoding design.md §3's boundaries: (1) no feature module references `RealNext3Client` directly — only the DI registration may; (2) nothing in the public module namespace references `INext3Client` or profile/user types; (3) only the outbox worker namespace calls push operations on `INext3Client`. The boundary tests may be red-green'd with placeholder namespaces now and enforced for real as modules appear.
**DoD:** `dotnet build` zero warnings, `dotnet test` green (including arch tests), `npm run build` succeeds in `src/Web`, week-0 hooks now activate (edit a .cs file → format hook runs).
**Test focus:** the three architecture rules; nothing else yet.
**Notes:** _

### ☑ 1.2 Auth — phone OTP, JWT, roles
**Goal:** all five roles can log in; the §2 policy skeleton exists.
**Refs:** design.md §4 (`app_user`, `otp_challenge`, `invite`), §9 (auth model), §2 (role matrix).
**Prompt:**
> Slice 1.2 — implement authentication per design.md §9: the `app_user`, `otp_challenge`, `invite` tables exactly as §4 specifies, phone-OTP login (hashed codes, TTL and max attempts from the placeholder config), JWT with role claim + refresh token, token invalidation on user deactivation, and the invite flow (S1: link → register → OTP verify → active). Create one ASP.NET authorization policy per role matching the §2 matrix, applied at endpoint-group level. Fake `ISmsSender` logs the codes for dev.
**DoD:** register→login→refresh→deactivate-blocks-login works end-to-end via the API; `dotnet test` green.
**Test focus:** OTP expiry, max-attempt lockout, replay/consumed-code rejection, deactivated-user token invalidation, role policy denies cross-role access.
**Notes:** _

### ☑ 1.3 Admin CRUD + audit log
**Goal:** the four profile CRUDs and the append-only audit trail.
**Refs:** design.md §5.4 (A1), §4 (profile tables, `audit_log`), §9 (audit events).
**Prompt:**
> Slice 1.3 — implement Admin A1 per design.md §5.4: CRUD for the four profile types with the §4 field sets, create-issues-invite (via slice 1.2's flow), deactivate semantics as §5.4 specifies. Add the `audit_log` table per §4 and a small append-only audit writer; wire the §9 audit events that exist so far (login success/failure, admin CRUD). Admin screens are browser-only React pages — functional, no styling effort.
**DoD:** create→invite→deactivate round-trip for each profile type; audit rows written; `dotnet test` green.
**Test focus:** deactivation leaves in-flight data untouched; audit rows are append-only (no update/delete path); NEXT3-ID uniqueness on expert/garage profiles.
**Notes:** done 2026-08-19 (60 tests). Manual browser pass caught two bugs the suite missed: GET-by-id 500'd on every profile type (EF can't translate a filter composed *after* a client-evaluated projection — filter before projecting; GET list/detail tests added) and System.Text.Json's default encoder escapes `+` to `\u002B`, breaking audit-detail phone searches (relaxed encoder in `AuditWriter`). Lesson: the round-trip DoD tests exercised POST but never GET — spec the read side explicitly in future slice test plans. Registration (S1) has no web UI yet, so browser round-trips need API-side activation. Extras recorded in scope-decisions: reissue-invite endpoint, no reactivate/hard-delete, phone immutable, `entity_id` nullable, DB trigger (not an arch rule) enforces append-only. Web gained router + localStorage-JWT/refresh client — 2.4 reuses it.

### ☑ 1.4 The five ports + fakes
**Goal:** every external dependency behind its interface with a working, failure-injectable fake.
**Refs:** design.md §6.2 (interface + fake contract), §3 (senders), §8 (notification log).
**Prompt:**
> Slice 1.4 — create the five ports per design.md §6.2 and §3: `INext3Client` (§6.2 signature verbatim), `IAssignmentSource`, `IEmailSender`, `IPushSender`, `ISmsSender`. Implement fakes for all five: seeded claim data for the NEXT3 fake, config-driven failure injection (`Fake:FailureRate`, `Fake:LatencyMs`), senders logging to the `notification` table (§4). Config selection per §6.2 (`Next3:Mode`, `Next3:AssignmentSource`). No real implementations yet. Register in DI so the arch tests from 1.1 now bind against real namespaces.
**DoD:** app boots fully on fakes; failure injection demonstrably causes retries downstream (assert via a throwaway harness test); arch tests green.
**Test focus:** fake NEXT3 honors `clientRef` dedupe (so outbox tests in 2.2 have a realistic target); notification rows logged per send.
**Notes:** done 2026-08-19 (85 tests, +25). Three things worth carrying forward. (1) **Dedupe order is load-bearing:** the fake checks the `clientRef` sent-log *before* rolling the injected failure, and a *failed* push does not consume the ref. Get that backwards and a transient error either loses the document forever or duplicates it — 2.2's outbox is written against exactly this. (2) **Senders can't hold a DbContext:** they are singletons, `AppDbContext` is scoped, so `NotificationLog` creates its own scope per call and commits its own transaction (unlike `AuditWriter`, which joins the caller's — a send already happened externally and must not vanish on rollback). The plan's `AddDbContextFactory` swap was dropped as unnecessary risk to the 1.2/1.3 suite. (3) **SMS bodies are never logged:** every SMS carries a live credential (OTP code, invite token) that §9 deliberately hashes, so `notification.payload` stays null for SMS — recorded in §4. Also: CA1716 rejects `template` and `to` as interface parameter names (use `templateName`/`recipient`), and `ISmsSender.Send` gaining `templateName` + `recipientUserId` touched OtpService, InviteService and the `CapturingSmsSender` double — that double writes no rows, so send-log tests must use the real fake.

### ☑ 1.5 Option 2 schema + public-surface skeleton
**Goal:** the public surface exists on day one — cheap now, expensive to retrofit.
**Refs:** design.md §4 (`broker_request`, `public_link_token`), §9.1 (token + abuse controls), §3 (public-surface boundary).
**Prompt:**
> Slice 1.5 — create the `broker_request` and `public_link_token` tables per design.md §4 and the `/public/*` endpoint group skeleton per §9.1: token generation (raw shown once, hash stored), the uniform-404 rule for invalid/expired/locked tokens, per-IP and per-token rate limiting middleware, and upload size/count caps from the placeholder config. No UI yet — endpoints + tests only. The §3 boundary holds: this module touches only its own tables and blob prefix; the 1.1 arch test for the public namespace must now bind for real.
**DoD:** token issue→open→lock lifecycle works via API tests; invalid/expired/locked are byte-identical 404s; rate limiter demonstrably throttles; arch test green.
**Test focus:** §9.1 token lifecycle table (reuse-until-locked, expiry, lock-on-submit), enumeration resistance, rate-limit thresholds from config.
**Notes:** done 2026-08-19; reviewed and closed 2026-08-20 (133 tests, +48). Five things worth carrying forward. (1) **Arch rule 2 dictated the module split.** `Api.Modules.PublicSurface` may not reference `Api.Modules.Users`, and `AuthPolicies` lives there — so the public module physically cannot host a `RequireAuthorization` call. B3 therefore sits in a new `Api.Modules.Broker` (which owns `broker_request`), and PublicSurface owns only the token, the endpoints and the limits. A new `Rule2_IsNotVacuous` test now asserts the namespace is non-empty: the rule sat green through 1.1–1.4 purely because there was nothing in it to check. (2) **An endpoint filter is the wrong place for a body-size cap** — minimal-API filters run *after* model binding, so the oversize test failed with 400 (malformed JSON) instead of 413; the whole body had already been read, which is exactly what a cap exists to prevent. It is middleware now, and it also lowers `IHttpMaxRequestBodySizeFeature` so a lying `Content-Length` is cut off by the server. (3) **Rate-limit options must be read per request, not captured at startup**, or they cannot be tested without a second host — `MutableOptionsMonitor` from 1.4 was reused, and since the limiter caches one limiter per partition key, a test that lowers a limit must also use a fresh token *and* a fresh IP. TestServer leaves `RemoteIpAddress` null, so a test-only `IStartupFilter` reads `X-Test-Ip`. (4) **EF defaults a required FK to Cascade** — `broker_request → app_user` came out cascading on the first generated migration and had to be pinned to `Restrict`; the db-reviewer pass caught nothing else. Also: `option` is a reserved SQL keyword, harmless via EF (always bracketed) but it needs `[option]` in any hand-written support query or stored proc. (5) **"Exactly one submission" was not enforced anywhere** — found in the 2026-08-20 review. Submit read `locked_at`, decided, then wrote it; two simultaneous POSTs both passed the read and both returned 200. A `rowversion` on `public_link_token` now makes the database the arbiter, the loser is caught and rendered as the same uniform 404, and its field values and audit row roll back with the batch. The test fires 4 parallel submits and asserts one 200 / three 404s / one audit row; removing the catch turns three of them into 500s, so the race is real rather than theoretical. **The general lesson for 2.2 and 5.2: a read-then-write on a state column is not a state machine — every "this may only happen once" transition needs the guard in the schema.** The 1.5 migration was regenerated (not stacked) since it was still uncommitted; the body-size cap's one-file ceiling is now recorded in scope-decisions as something 5.3/6.1 must raise for multipart.

---

## Week 2 — Expert core

### ☑ 2.1 Claim cache + assignments
**Goal:** an assignment arrives (from the fake) and an expert sees their claim.
**Refs:** design.md §4 (`claim`, `expert_assignment`), §5.1 (flow + writes), §6.2 (`IAssignmentSource` handler).
**Prompt:**
> Slice 2.1 — implement the assignment path per design.md §5.1 and §6.2: the `claim` cache and `expert_assignment` tables per §4, the single idempotent `AssignmentReceived` handler (dedupe per §6.2), claim-cache refresh rule per §4's `claim` row, and the fake assignment source injecting synthetic assignments on demand. API: expert claim list + claim detail. Push notification via the fake `IPushSender`.
**DoD:** injecting a fake assignment produces the row, the cache fetch, the notification log entry, and it appears in that expert's list only; `dotnet test` green.
**Test focus:** duplicate/replayed assignment is a no-op; stale-cache banner path when the fake NEXT3 is "down"; assignment invisible to other experts.
**Notes:** done 2026-08-20 (167 tests, +34). Four things worth carrying forward. (1) **`IAssignmentSource.Subscribe` hands back no result**, so the demo endpoint could not both use the real delivery path and report an outcome. Split cleanly: the endpoint calls `AssignmentHandler` directly for its typed 200/422, and the tests call `FakeAssignmentSource.Inject` through DI so the startup subscription is what they exercise — same handler either way, which is §6.2's single-funnel guarantee. Resist widening the 1.4 interface for this. (2) **Ordering is the design, not an accident:** the assignment row commits alone, then the cache fetch and the push happen best-effort on top. Both failure paths are tested (`WhenNext3IsDown_TheAssignmentIsStillRecorded`, `WhenThePushFails_...`), because an outage that loses an assignment is the exact bug this project exists to fix. `notified_at` stays null on a failed push — the `notification` row is the record. (3) **`ApiFixture` now swaps `IOptionsMonitor<FakeOptions>` too**, copying the 1.5 `PublicLinkOptions` trick — before this there was no way to make the *booted* app's NEXT3 fail, only a hand-built client. One `FakeBehavior` is shared by NEXT3 and all three senders and the collection is serialized, so `WithNext3Down` restores the old value in a `finally`; forget that and every later test breaks. (4) **The clock is 15 minutes wide.** Two tests failed as 401s, not assertion failures: `Time.Advance(1 hour)` outran `Auth.Jwt.AccessTokenMinutes` with `ClockSkew` zero. Any test travelling further than that must re-login first. Also: `expert_assignment` has **no FK to `claim`** (the cache must stay disposable, or a NEXT3 outage costs assignments), the arrival columns are created now and written in 2.4, and an unmapped `next3_id` is audited and dropped rather than retried — a poller would otherwise loop on data the app cannot fix (#8).

### ☑ 2.2 The outbox
**Goal:** the load-bearing piece, test-first.
**Refs:** design.md §6.3 (everything), §4 (`next3_outbox` — contractual schema).
**Prompt:**
> Slice 2.2 — implement the transactional outbox per design.md §6.3, test-first: the `next3_outbox` table exactly per §4, the one-transaction write helper every producer will use, the `READPAST` dequeue as the sanctioned stored procedure, the worker loop with §6.3's backoff schedule and `failed` terminal state, and `clientRef` idempotency against the fake. Write the failing tests from §6.3's rules first, then implement.
**DoD:** with `Fake:FailureRate` raised, rows visibly retry on schedule and land `sent` or `failed`; atomicity test proves document row + outbox row commit or roll back together; `dotnet test` green.
**Test focus:** atomicity; retry-after-timeout does not duplicate (fake's `clientRef` check); backoff timings; two concurrent workers never double-process (READPAST test).
**Notes:** done 2026-08-20 (197 tests, +34, minus 1.4's throwaway harness which its own comment scheduled for deletion here). Six things worth carrying forward. (1) **§4's `claim_id uniqueidentifier` was unimplementable** — no uniqueidentifier claim id exists anywhere in the model, `claim` is keyed on `visa_no`, and this table must hold no FK to that disposable cache. It is `visa_no nvarchar(50)` now and design.md §4 was *corrected*, not diverged from: the `add-ef-migration` skill and the `db-reviewer` agent both treat outbox drift as a finding, so quiet divergence would have been caught later and cost more. **Read a contractual schema against the model before implementing it, not while generating the migration.** (2) **"No duplicates" does not test READPAST.** Two concurrent `UPDATE TOP (n) … WHERE status='pending'` statements produce disjoint sets with or without the hint — without it the second one simply *blocks* first. The discriminating test holds a transaction open on one connection and gives the second a short command timeout; removing `READPAST` turns it from a pass into a timeout, which was verified by actually removing it. A concurrency test that would pass against the broken version is decoration. (3) **A frozen clock and `SYSUTCDATETIME()` cannot coexist.** `FakeTimeProvider` sits at 2026-08-19 while the database is on real time, so a row whose `next_retry_at` the app wrote was always already due. The procedure takes `@now` from the worker; without it not one backoff timing is testable. (4) **`OutboxWorker` was already registered as a hosted service since 1.4** — turning the shell into a real loop would have had it tick every 30 s against the database the whole serialized collection shares, draining rows other tests were mid-assertion on. Split scheduling (`OutboxWorker`) from behaviour (`OutboxProcessor`), disable the loop with `Outbox__WorkerEnabled=false`, and drive `RunOnce` from tests. The same trap waits for 2.3's cleanup job and the `otp_challenge` purge. (5) **The dequeue claims a batch across the whole table and cannot be scoped to one test's rows** — a batch full of another test's leftovers leaves your row unclaimed and your assertions failing for unrelated reasons. `ClearQueue()` first; safe only because the collection is serialized. (6) **`FromSql` turns every interpolation hole into a parameter**, so the procedure name must be a literal, and nothing may be composed onto the query or EF wraps it in a subquery around `EXEC`. Also: the table must stay trigger-free (`OUTPUT inserted.*`, now asserted against `sys.triggers` rather than only commented), and the full 8-attempt schedule takes **26 h 36 m** — pinned by a test, because it is how long a doomed push takes to reach A2. (7) **The `db-reviewer` pass earned its keep and should run on every migration from here.** It found, and the tests had missed, that **the lease I added introduced a two-owner window**: a reclaimed row is held by both the new worker and the original, and with no arbitrator the slow one wins by arriving last — rewriting a `sent` row to `failed`, or giving a `failed` row a `sent_at`, both of which §7.3's blob deletion and A2's retry list then act on. `attempts` is the concurrency token now (a reclaim increments it, so it *is* the claim's generation counter — no new column, §4 untouched). It also caught that a row which keeps killing its worker would be reclaimed for ever, because the give-up rule sits in code that only runs when a worker survives; the dequeue retires those itself via `@max_attempts`. **A feature that fixes one hole often digs another one next to it — audit the fix, not just the original design.** (8) **FIFO draining and READPAST are in tension.** A CTE with `ORDER BY created_at` makes the sort materialise the candidate set and lock exactly the rows READPAST exists to skip; the non-blocking test went red. Ordering lost. The same bite recurred in miniature when the new abandon statement was written without the hints — one unhinted `UPDATE` anywhere in the procedure makes the whole thing blocking. **Every statement in that proc needs `WITH (UPDLOCK, READPAST, ROWLOCK)`, not just the claim.** (9) **`IsConcurrencyToken()` is invisible to `has-pending-model-changes`** — it reports "no changes" while the snapshot genuinely lacks it, because the annotation emits no DDL. The migration had to be regenerated (safe, still uncommitted) to get it into the snapshot; check the snapshot by eye after adding a token.

### ☑ 2.3 Media pipeline — server side
**Goal:** a file becomes a `document` row + blob + outbox push, with the lifecycle rules enforced structurally.
**Refs:** design.md §7 (all), §4 (`document`).
**Prompt:**
> Slice 2.3 — implement the server media pipeline per design.md §7: the `document` table per §4, a **streamed** multipart upload endpoint (no buffering into memory — §7.3's failure order: blob PUT first, then the one-transaction rows via 2.2's helper), bucket rules per §7.1 with the `origin` flag, server-side re-validation per §7.2 item 5, and the cleanup job whose delete query structurally joins on outbox `sent` per §7.3. Use Azurite (Docker) or the Blob emulator for local dev — document the choice in CLAUDE.md's commands section.
**DoD:** upload→document row→outbox row→fake push→`sent`→cleanup-eligible, verified by test; a `failed` push's blob survives cleanup; `dotnet test` green.
**Test focus:** §7.3 lifecycle rules (especially never-delete-before-sent), capture-only bucket rejection, oversize/wrong-type rejection.
**Notes:** done 2026-08-20 (263 tests, +66). Eight things worth carrying forward. (1) **A stray `AsNoTracking()` in a composed subquery silently disarmed the retention sweep.** `OutboxSentQuery` projected to a `Guid` and looked free to mark no-tracking; EF applies the tracking behaviour of the *combined* expression tree, so the outer `Documents` query came back untracked, `blob_deleted_at` was never written, and `SaveChanges` reported success having done nothing. The blob was deleted, the row still said it was held, and the same rows were re-swept on every pass. Two tests passed either way — only the one asserting the *flag* caught it. **When you compose an IQueryable from another class, tracking is part of what you inherited.** (2) **Architecture rule 4 made `OutboxWriter` uncallable and nobody noticed**, because 2.2 shipped with no producers: a called method's return type is part of the calling type's IL, so returning `Next3OutboxMessage` put every future producer in violation. It returns `Guid` now. **A boundary rule with nothing on the other side of it is untested — the first real consumer is the test.** (3) **The `db-reviewer` pass again found what the tests missed, and the worst finding was in my fix, not the design** — the retention sweep deleted up to 500 blobs and committed once, so one undeletable blob discarded the `blob_deleted_at` and audit rows already staged *for bytes that were physically gone*, and the deterministic batch then stalled retention behind it for ever. Also: the unique index on `outbox_message_id` is a safety property (one confirmed push must license exactly one deletion), the `queued ⇔ outbox row exists` check constraint, a missing `blob_key` index, `ValueGeneratedNever`, and a doc-type lookup sitting *after* the blob PUT so a config typo leaked a blob per upload. (4) **`Down()` on this table is destructive beyond it**: drop `document` and 24 hours later the orphan sweep deletes every blob in the container, including bytes named by `pending`/`failed` outbox rows — warning written into the migration, where someone will be standing when it matters. (5) **No image library.** ~90 lines of JPEG SOFn / PNG IHDR parsing beats a licence question at handover (ImageSharp v3's split licence) and a Windows-only API (`System.Drawing`); it doubles as the magic-byte check. A 128 KB bounded peek is needed because phone EXIF pushes the frame header tens of kB in — and `unreadable_image` must stay distinct from `content_type_mismatch`, or the answer points at an attacker who is not there. (6) **Streaming costs an ordering contract:** metadata parts must precede the file part, because a bucket learned after the bytes reached storage cannot apply §7.1's capture-only rule. Refused with a named code rather than silently buffered. (7) **Azurite lags the SDK**: Azure.Storage.Blobs 12.29 sends API version 2026-06-06, Azurite 3.36 rejects it, so `--skipApiVersionCheck` is mandatory and is in CLAUDE.md. The npm package is `azurite` with a binary named `azurite-blob` (`npx -p azurite azurite-blob`), which matters when Docker Desktop is not running. (8) **A contract test that only feeds a `MemoryStream` proves less than it looks**: the upload path hands the store a non-seekable, unknown-length stream, so the contract test now builds the same `PrefixedStream`+`LimitedStream` composition. A skippable `[AzuriteFact]` keeps `dotnet test` Docker-free while still exercising the real adapter when the emulator is up — the shape 3.3 wants for fake-vs-sandbox.

### ☑ 2.4 Expert screens E1/E2 + Arrived
**Goal:** the expert's daily flow in the browser.
**Refs:** design.md §5.1 (E1/E2 rows), §2 (screen gating).
**Prompt:**
> Slice 2.4 — build E1 (claim list, newest-first with media counts) and E2 (claim detail with the Arrived button) per design.md §5.1: Arrived writes per its table row (timestamps + coordinates + outbox `update_arrival`), disabled after first press, blocking explanation on geolocation denial, and Arrived does NOT gate capture (§5.1's recorded interpretation). TanStack Query for server state; logic in hooks, components render.
**DoD:** full flow works in the browser against the fake; Arrived visible in outbox as `sent`; `npm run build` + tests green.
**Test focus:** Arrived idempotence (double-press), geolocation-denied path, list scoped to the logged-in expert.
**Notes:** done 2026-08-20 (283 xUnit +20, and a new web suite of 20). **No migration and almost no new server code** — 2.1 created the arrival columns and 2.2 created `EnqueueArrival`, so the slice was one endpoint plus the whole web layer. Seven things worth carrying forward. (1) **"Idempotent" needed the guard in the WHERE clause, not in an `if`.** The sequential double-press that the DoD names is the easy half; four simultaneous presses all pass a read-then-write check, and the damage is real — two `arrived_at` values and two `update_arrival` pushes tell NEXT3 the expert arrived twice at two different times. `WHERE arrived_at IS NULL` on an `ExecuteUpdate` settles it; removing the clause was verified to turn the concurrency test from 1 arrival into 4. This is the third slice in a row where 1.5's lesson was the load-bearing one. (2) **`ExecuteUpdate` costs an explicit transaction.** It runs its own statement immediately, outside `SaveChanges`, so without `BeginTransactionAsync` the outbox row would commit separately from the row that caused it — §4's one-transaction rule broken by the first endpoint that actually depends on it. Only place in the codebase with an explicit transaction; commented as such. (3) **A concurrency token on `ArrivedAt` was the wrong tool**, though it is the 1.5/2.2 idiom: EF applies a token to *every* write of the entity, so a concurrent E2 first-open would fail its `opened_at` save with `DbUpdateConcurrencyException`. A guard that belongs to one transition should not be pinned to the whole row. (4) **The double-press latch cannot be `mutation.isPending`.** Two taps in the same tick both run before React re-renders, so `isPending` is still false for the second — swapping the `useRef` latch for `isPending` was verified to turn one request into two. Same class of bug as (1), one layer up. (5) **Timestamps arrive without a `Z`.** A value the server just computed serialises as UTC; the same value read back out of SQL is `DateTimeKind.Unspecified` and comes through bare, which the browser reads as local — a four-hour error in GST on the one screen whose job is recording when the expert arrived. Normalised in the web layer (`toUtcDate`), not by reconfiguring EF/JSON, which would have put the 282 exact-timestamp assertions at risk. (6) **Vitest without `globals` means `@testing-library`'s auto-cleanup never registers** — it looks for a global `afterEach`. Three page tests failed with "found multiple elements" before `src/testing/setup.ts` existed; every render had been stacking into the same document. (7) **`@(...)` around a PowerShell pipeline result**: a single matching line is a *string*, and `[-1]` on a string is its last character, so the manual-pass script read an OTP of "6". (8) **The `db-reviewer` pass found the worst bug in the slice, and the tests had pinned it.** `ArrivalInfo` carried `DateOnly` + `TimeOnly` — a faithful reading of §6.1, written in 1.4 when nothing filled it. The moment 2.4 filled it from a UTC instant, the offset was thrown away *at capture*: an expert arriving at 01:30 GST would be pushed to NEXT3 as arriving the previous calendar day, on the one field a claims dispute turns on, in a row that is durable for up to 26 hours and cannot be repaired from its own payload. It is a `DateTimeOffset` now, with the split moved to `RealNext3Client` and a new `Next3:ArrivalTimeZone` placeholder (#6, design.md updated). The web layer had *already* caught the same hazard on the display side that morning (`toUtcDate`) and nobody connected it to the push side. **A lossy conversion belongs at the edge that knows the target format, never at the producer** — and a test that asserts the code's own arithmetic back to itself (`DateOnly.FromDateTime(pressedAt)` where `pressedAt` was UTC) can never go red. (9) **A hand-rolled transaction needs `CreateExecutionStrategy`.** Enabling `EnableRetryOnFailure` — the standard Azure SQL fix, and §10 puts production there — makes EF throw on *every* user-initiated transaction outside a strategy. Latent today, a 100% outage of Arrived on the day someone hardens the connection, and no test would have caught it. `ChangeTracker.Clear()` goes first in the delegate, or a retry inserts the outbox and audit rows twice. (10) **`ApiFixture` now turns on `READ_COMMITTED_SNAPSHOT`.** Azure SQL has it on by default, LocalDB has it off, so both concurrency guarantees this project leans on were being proved under a protocol production will not run. All 283 pass under RCSI, and the arrival guard was re-verified to discriminate under it. Also: `navigator.geolocation` is stubbable via `Object.defineProperty` in both jsdom and the real browser, which is how the denied path was exercised without a Chrome permission prompt (a prompt would have blocked the automation). The manual pass needs a visa the fake NEXT3 seeds (`PLACEHOLDER-VISA-0001`..`0005`) or E1/E2 render a cold cache; `Blob__Mode=fake` is enough, since 2.4 uploads nothing. **Open for week 6:** E1 is unbounded and only grows (no expert-side "done" state, §1) — decided on a real handset at the device checkpoint, not truncated silently now.

### ☑ 2.5 Capture + clarity gate (the shared hook)
**Goal:** the capture/clarity component that Garage and the public page will reuse — the reuse that pays for Option 2.
**Refs:** design.md §7.1 (bucket matrix), §7.2 (gate), §5.1 (E3/E4).
**Prompt:**
> Slice 2.5 — build the capture component + clarity gate per design.md §7.2 as a **reusable hook + component pair** (it must later serve Garage and the Option 2 public page unchanged): camera capture vs file pick per the §7.1 bucket matrix, resolution floor + Laplacian blur-variance from the placeholder config, the E4 confirm screen, retry loop, then upload via 2.3's endpoint. Buckets configurable per §7.1. Browser `capture` attribute is the hint; native enforcement is Capacitor's job later (§7.1 provisional note).
**DoD:** expert can fill all four buckets end-to-end; blurry/small images are rejected client-side and (bypassing the client) server-side; tests green.
**Test focus:** Laplacian threshold behavior at the config boundary, capture-only bucket refuses file pick, hook reusability (no expert-specific coupling). **2.4 left you the runner** (`npm test`, Vitest + jsdom + @testing-library, cleanup wired in `src/testing/setup.ts`) — the clarity gate is the first thing in this project that could not be tested any other way.
**Notes:** done 2026-08-20 (288 xUnit +5, and the web suite 20 → 72). **The slice needed server code the card did not mention.** Seven things worth carrying forward. (1) **A client-side gate needs client-side values, and CLAUDE.md's placeholder rule names *thresholds* explicitly** — so hardcoding 1024/768/100 in TypeScript was not an option, and a browser cannot read `appsettings.Placeholders.json`. Hence a new anonymous `GET /api/config/media`. Anonymous is a decision, not a shortcut: 5.3's public page runs this identical gate with no token, so gating it now buys a second endpoint later. It is **outside `/public/*`, so §9.1's rate limits do not cover it** — acceptable only because it touches no database and no user, and recorded rather than assumed. (2) **A blur threshold is meaningless without the scale it was measured at.** Laplacian variance scales with resolution, so `100` means one thing on a 12 MP handset and another on a 48 MP one — across an expert network that is a gate that passes and fails the same photo depending on the phone. `Clarity.BlurAnalysisMaxEdge` (512) joins the threshold in Appendix A so the pair moves together when #9 lands; design.md §7.2 updated, not diverged from, as 2.2 and 2.3 did. (3) **The resolution floor must be measured at native size, and that forced the API's shape.** Decoding a 48 MP photo to one RGBA array is ~192 MB — enough to kill the tab on the handset of an expert standing at a crash site — so the decoder returns native dimensions *plus* a bounded buffer and `assessClarity` takes the two separately. Checking the floor on the bounded buffer would tell an expert their 1.1 MP photo is too low-resolution: wrong, and unfixable from where they are standing. One test pins it. (4) **jsdom has no canvas and no `canvas` package is installed**, which is a feature: it forced the gate into a pure function over pixel buffers with the browser decode behind an injectable parameter (the `requestPosition` idiom). The variance test is worked longhand to 425 on a 4×4 — 2.4's lesson that a test asserting the code's own arithmetic back to itself can never go red. (5) **The upload could not use a second fetch helper.** `withBearer` hardcoded `Content-Type: application/json`, and multipart needs the browser to set its own boundary — but the refresh-on-401 retry lives *inside* `api<T>()`, so a parallel path would have silently dropped it and the failure would look like a random logout halfway through filling a bucket. One conditional in `withBearer` instead. (6) **The double-Confirm latch is `useRef`, not `isPending`** — third slice running for 1.5's lesson; verified by removing it, which turns one upload into two, i.e. a duplicate photo under the visa and a second outbox row. (7) **Reusability needed a test, because nothing else would notice its loss.** If `media/` imports an expert query key, garage (5.1) and the public page (5.3/6.1) can no longer reuse it and §11's saving evaporates — while every expert test still passes. A test reads every file in the folder and forbids imports of `expert/`, `pages/`, `admin/`, the session helpers, the token store and the router; verified by planting one. The endpoint is a `path` prop and invalidation is handed back via `onUploaded`. (8) **The browser pass found the slice's only real bug, and it was invisible to the suite.** A PDF rendered into E4's `<img>` gives a broken-image icon on a black bar, over alt text reading "The photo about to be sent to AXA" — an expert asked to confirm a document they cannot see, miscalled a photo, on the one screen that exists so a person looks at the file. Nothing throws; `naturalWidth` is just 0, so every assertion passed. E4 now names the file instead, with a test. **A component test that never asks what the pixels did is a component test that cannot see a broken image.** (9) **`configLoaded` and `ready` are deliberately two flags.** `findBucket` returns undefined both while the config is in flight and when the bucket is not in §7.1's registry; collapsed into one "ready", the second case renders "Loading photo settings…" for ever, with a clean network tab and an empty console. **5.1 will hit this** the first time a garage bucket reaches a screen before its `MediaBuckets` entry and migration land — a permanent lie that points at the query instead of at the registry. Also: **the manual browser pass is where the real decode path lives** — jsdom cannot run `createImageBitmap`, so the E4 screen, the 1600×1200 verdict, both refusals and the real multipart were only ever proven in Chrome (against real PNGs generated with Node's zlib, since the suite's synthetic JPEGs are header-only and Chrome will not decode them). Full chain confirmed live across **all four buckets**: capture → gate → upload → `document` + outbox row → dequeue → `sent`, with `sizeBytes` matching the file on disk byte for byte, E1's media count reaching 4, and `origin` landing `captured` for the two car-photo buckets and `uploaded` for the two file-picked ones. The PDF stored `clarity_result = not_applicable`; the file-picked *image* stored `passed`, which is §7.2's "uploads included" holding in practice. Retake was verified to clear the candidate and upload nothing. **Arrived was never pressed in the whole pass** — §5.1's ungated-capture interpretation, demonstrated rather than asserted. Two small traps: passing `undefined` to a defaulted parameter re-selects the default (a "no config" test that silently tested the config), and E2's blanket `fetch` stub had to become route-aware now that the page issues three GETs. **No migration, so no `db-reviewer` pass** — the only slice since 2.1 without one.

---

## Week 3 — Expert rest + real NEXT3 client

*Expanded 2026-08-20 from the week-1/2 Notes. Two decisions taken at expansion: **E1 search is local** (the expert's own assignments — a NEXT3-wide search would let an expert attach photos to claims never assigned to them; design.md §5.1 is corrected in 3.2), and **3.3's gate is closed** (#1 sandbox still absent, all 41 questions TBC), so 3.3 builds the real client against our own contract and the swap stays `fake`.*

### ☑ 3.1 Voice note + damage diagram
**Goal:** the two remaining E3 artifacts flow through the existing pipeline; the server learns audio and two buckets; the diagram stays reusable for 6.1's side selector.
**Refs:** design.md §5.1 (diagram + voice rows), §7.1 (expert "voice / diagram" row), §7.2 item 4 and the PDF `not_applicable` exception, §1 simplifications (static SVG tap-to-mark → PNG; voice = playback-confirm), Appendix A (`Next3.DocTypes.VoiceNote/DamageDiagram` — already present and unconsumed; `Media.*`).
**Prompt:**
> Slice 3.1 — add the voice note and the damage diagram per design.md §5.1 and §7.2.
> **Server.** Add `MediaKind.Audio` and a `Media.AudioContentTypes` list (Appendix A addition, recorded as realized; MediaRecorder emits `audio/webm` on Chrome and `audio/mp4` on Safari — include `audio/ogg` too). Normalise the declared content type by stripping `;codecs=…` before the allow-list compare. Extend the sniffer beside `ImageHeader` for EBML, `ftyp`, and `OggS`; audio stores `clarity_result = not_applicable` (the value `ClarityResults` already documents for it); extend the blob-key extension map. Add two `MediaBuckets` rows, both `OwnerKind assignment`, `Next3Folders.ExpertDocuments`, **`AllowUpload false`** (both are produced in-app — smaller interpretation, record it in scope-decisions): `voice_note` (Audio, `DocTypeKey VoiceNote`) and `damage_diagram` (Image, `DocTypeKey DamageDiagram`). A new bucket is a migration (`CK_document_bucket` is generated from `MediaBuckets.All`) — use the `add-ef-migration` skill, db-reviewer pass mandatory. `GET /api/config/media` picks the buckets up by itself. No duration cap beyond `Media.MaxFileMb` (record).
> **Web, all inside `src/Web/src/media/`** so `reusability.test.ts` keeps passing — 6.1 reuses the diagram as the Option 2 side selector. `recorder.ts`: an injectable `MediaRecorder` factory in the `decodeImage`/`requestPosition` idiom (jsdom has no MediaRecorder — inject, never stub), and a `useVoiceNote` hook that produces a `File` and hands it to `useCapture.select(file, 'captured')`; `select()`'s non-image short-circuit already routes it to the confirm stage with `verdict: null`. Give `ClarityConfirm`'s non-image branch an `<audio controls src={previewUrl}>` when `file.type.startsWith('audio/')` — that *is* §7.2 item 4's playback-confirm. `diagram/`: a static SVG car with named hotspot regions, tap-to-toggle marks in pure state, rendered to PNG through an injectable canvas renderer **at 1600×1200 — above the resolution floor** so `CheckContent` passes it legitimately; add a web test asserting the render size is ≥ the floor from `/api/config/media`, so a raised floor fails loudly instead of silently rejecting every diagram; then `useCapture.select(png, 'captured')`. E2's `CaptureSection` gains a voice panel and a diagram panel with the same render ladder as `CapturePanel` — keep `configLoaded` and `ready` apart (2.5's trap).
**DoD:** both artifacts land `document` + outbox → `sent` through the fake; the server refuses audio into an image bucket and an image into `voice_note`; migration db-reviewed; `dotnet test` + `npm test` green; **manual Chrome pass**: a real MediaRecorder note recorded, played back, confirmed, `sent` with `clarity_result = not_applicable`; a diagram drawn and landed `passed`.
**Test focus:** server — sniff per container, `;codecs=` stripping, cross-kind rejection codes, `upload_not_allowed_for_bucket` on both new buckets, `TestAudio.Webm/Mp4/Ogg` header builders in the `TestImages` style; web — recorder hook with an injected fake recorder (start → stop → `File`), diagram state (toggle/clear), renderer injection, the `ClarityConfirm` audio branch, and `reusability.test.ts` still green with its non-vacuity count raised.
**Notes:** done 2026-08-21 (338 xUnit, +50; web 72 → 121). **The card and HANDOFF contradicted each other, and resolving it was the design decision of the slice.** The card said `useCapture.select(png, 'captured')`; HANDOFF §NEXT-SESSION said the diagram does not go through the clarity gate. `select` routes anything `image/*` into the blur pass, so both could not hold. HANDOFF won, through an explicit `select(file, origin, { photographic })` defaulted to **true**: a flat line drawing scored against `BlurVarianceThreshold` risks refusal with *"hold the phone still, let the camera focus on the damage"* — advice about a drawing, which is 2.5's PDF-in-an-`<img>` bug in a new costume. **Deciding a file's treatment from its MIME type alone is the trap; the caller knows something the type does not.** Nine more things worth carrying forward. (1) **A subfolder walked straight out of the 2.5 reusability guard.** `diagram/` is the first nested folder in `media/`, and the guard globbed `./*` with depth-fixed patterns — a nested file reaches the expert module as `'../../expert/'`, which `'\.\./expert/` does not match at all. Both halves were **verified by planting an import**: under the old pattern the violation passed silently; under the old glob the raised non-vacuity count and a new "reaches into the subfolders" assertion both went red. **A guard that quietly stops covering new code is worse than none, because it still reads green** — and only a count notices. (2) **`MediaRecorder` emits `audio/webm;codecs=opus`, and a raw allow-list compare refuses every voice note in the application.** Normalisation had to exist — and had to happen *once*: `CheckContent` hands the normalised type back through a new `out` parameter, so **what was validated is what gets stored**, on the row, the blob and the NEXT3 payload alike. Two normalisation points are two answers that can disagree, which is §7.3's lesson relocated. Cost: an `out _` at ten existing call sites, the largest hunk in the test diff, nothing weakened. (3) **The `db-reviewer` pass found a real defect for the fifth migration running — and again it was in the new code, not the schema.** Every comparison in the validator is case-insensitive, so `AUDIO/WEBM` is *accepted*; the caller's spelling was then persisted, pushed to NEXT3, and matched **ordinally** by the blob-key extension map, filing a good voice note as `.bin` and hiding it from `WHERE content_type = 'audio/webm'`. Every accepting path now returns the **canonical** type the bytes were recognised as. Pre-existing for images; 3.1 only added a second path onto it. **Accept loosely, store canonically.** (4) It also caught that `Down()`'s loud failure — SQL Server validates a restored CHECK against existing rows — **depends on the rollback running in a transaction**. From a `--no-transaction` script the `DROP` commits, the `ADD` fails, and `document.bucket` is left with *no* constraint, every invented bucket string writable. Now in the migration comment. (5) **Containers are verified, codecs are not, deliberately** — EBML and `ftyp` are shared with video, nothing here decodes a stream, and #10 has not said what NEXT3 takes. (6) **The recorder's format candidates come from the server** (`Media.AudioContentTypes`), so no audio format literal exists in TypeScript — 2.5's placeholder precedent applied to a capability list rather than a threshold. (7) **The floor-vs-render-size guard needed both suites.** A jsdom test cannot read `appsettings.Placeholders.json`, so the pairing is pinned in **xUnit** (`TheClarityFloorAdmitsADamageDiagram`) and the web half is a runtime rung that says so on screen — otherwise a config change silently refuses every diagram as `image_too_small` and the drawing takes the blame. (8) **What you see is what gets exported:** the PNG comes from serializing the live `<svg>`, not a second drawing routine, so there is no geometry to keep in step and the expert cannot mark one thing and send another. Inline styles, not CSS classes — a serialized SVG carries its attributes but not the page's stylesheet, so a class-styled diagram arrives at AXA blank. (9) **The browser pass again saw what the suite could not, twice.** A second recording failed with *"the recording did not complete"* — **not a bug**: the first recording's teardown had genuinely stopped the microphone tracks (`readyState: "ended"`) and the single-stream test stub handed back a dead stream where a real `getUserMedia` returns a fresh one. That accidentally *proved* the mic-release the code exists for. And **playback could not be verified**: the `<audio>` renders, is bound to the right blob URL, and the bytes are a genuine 813 KB WebM opening with the exact EBML magic the server sniffs — but this Chrome profile decodes no audio at all (a known-good 44-byte WAV stalls at `readyState: 0` identically), so *hearing* the note is the one thing this pass could not prove. **On the week-6 device checklist.** Everything else confirmed live: diagram → `passed`, `PLACEHOLDER-DOC-07`, `.png`, outbox `sent`; voice note → `not_applicable`, `PLACEHOLDER-DOC-06`, `audio/webm`, `.webm`, 813,700 bytes, outbox `sent`; both in *Expert documents* with `clientRef` = document id, audited with the origin flag; all four cross-kind and picked-file refusals returned the right code and **wrote nothing** (exactly 2 documents after 4 refusals); Retake discarded without uploading; neither panel renders a file picker. **One honest gap:** because the allow-list holds bare types, Chrome echoes the bare type back, so **the codec-stripping path never reached the wire in the browser** — it is defensive, exercised only by the integration test, and becomes load-bearing the day #10 answers with a codec-qualified type.

### ☑ 3.2 Search + expert report
**Goal:** E1 finds previously assigned claims by visa or plate; E5 uploads the report.
**Refs:** design.md §5.1 (search row — **corrected to local in this slice**; report row), §2 (E1/E5), §7.1 (`expert_report`, upload-allowed).
**Prompt:**
> Slice 3.2 — search and the expert report per design.md §5.1.
> **Search is local.** Extend `GET /api/expert/assignments` with an optional `q` (trimmed, max 64) matching `VisaNo` or the cached `PlateNo` (`Contains`, case-insensitive) **within the caller's own assignments only**, in the same GroupJoin + `MediaCount` statement; empty `q` returns the full list. No NEXT3 call — `INext3Client.SearchClaims` stays for the officer's visa lookup in 4.2. Correct design.md §5.1's search row as realized, with the reason (a NEXT3-wide search lets an expert open and attach photos to claims never assigned to them, and `ClaimSummary` cannot be upserted into the `claim` cache without corrupting it), and add the scope-decisions row. No new endpoint, no migration.
> **Web.** A debounced search input on E1 with its state in the URL (`?q=`) so a reload keeps it; extend `expertKeys.list(q)` — keys are the invalidation contract, so `useRefreshAfterCapture` must still hit the active list. Show a cold-cache hint ("plate unknown until the claim has loaded"). **E5** = add `{ bucket: 'expert_report', label: 'Expert report' }` to `EXPERT_BUCKETS` in `ExpertAssignmentPage.tsx` — the server side (bucket, doc type, PDF allowed) shipped in 2.3; no separate route (smaller interpretation).
**DoD:** visa and plate searches return only the caller's assignments; another expert's matching claim never appears; a report PDF lands `not_applicable` → `sent`; manual Chrome pass of both.
**Test focus:** server — cross-expert isolation, plate match via the cache, cold cache still matches by visa, `q` length cap; web — invalidation with an active `q`, URL round-trip, the E5 panel renders its upload input.
**Notes:** done 2026-08-21 (356 xUnit, +18; web 121 → 134). **No migration and almost no new server code** — `q` is one optional parameter and one composed `Where`, and E5 was one line, because 2.3 had already shipped `expert_report` with its doc type and PDF acceptance. Nine things worth carrying forward. (1) **The correction was the slice.** §5.1 routed E1's search through `INext3Client.SearchClaims`, and two independent things made that unshippable: a NEXT3-wide search hands the expert claims they were never assigned, and the screen those results lead to has a capture panel on it; and `ClaimSummary` has no policy number, insured phone or city, so a hit **cannot** be upserted into the `claim` cache without inventing three fields NEXT3 owns. design.md §5.1 *and* §6.1's claim-search row were corrected — §6.1 said the operation "serves expert search and officer lookup", which would have contradicted §5.1 from the moment this shipped. Fifth slice running that design.md was corrected rather than diverged from. **Read the row you are about to implement against the types it names.** (2) **The analyzers actively push toward code EF cannot translate.** `ToUpper()` trips CA1304/CA1311 and `Contains` trips CA1862, and every fix they suggest — `ToUpper(CultureInfo)`, `Contains(string, StringComparison)` — has no SQL mapping, so EF would throw at runtime on the expert's first search. This is the codebase's **first hand-written suppression**: narrow, at the statement, with the reason. The alternative was to rely on the database's default collation for case-insensitivity, which is a behaviour that would change silently on a CS-collation deployment. Said honestly in the test: **it passes either way on LocalDB**, so the `UPPER()` is recorded, not proven. (3) **`lists()` versus `list()` is the whole invalidation trap**, and it was verified by planting: with `list()` the new test goes red and *every other web test stays green*, which is exactly the failure mode — an upload during an active search silently stops refreshing the count. `useArrived` had the same bug and the card did not mention it. (4) **Fake timers deadlock against @testing-library's async helpers** — two tests hung to a 5 s timeout. The fallback was the house injectable idiom (`decode`, `render`, `start`): an optional `debounceMs` prop, 30 ms in tests. Zero would have made the "nothing committed yet" assertion vacuous. Verified by planting a synchronous commit: the request list goes from `[null,'PLC']` to `[null,'P','PL','PLC']`. Note a 0 ms `setTimeout` still coalesces under `userEvent`, so the test discriminates *debounced* from *not debounced*, not 30 ms from 0. (5) **E1's early returns had to be dismantled.** `if (isPending) return …` sat above the table, so the first refetch after a keystroke would unmount the input and take the caret with it. The input is hoisted above every branch and `keepPreviousData` stops the table blanking. Neither is visible in a test that only reads text; one test now pins the mounting. (6) **"No claims assigned yet" became a lie** the moment search existed — an expert with a full worklist who mistypes a plate was told they have no claims. Two messages now. (7) **The browser pass found the slice's only real bug, again.** Search survived a reload, as the card asked — and was thrown away the moment the expert opened a claim, because E2's "← My claims" pointed at a bare `/expert`. Retyping the plate after every claim is most of the value of having searched, on a phone, at a crash site. Nothing threw, nothing failed: **no test navigated**. E1 now carries `?q=` into each claim link and E2 hands it back. (8) **`SeedClaim` gained a `plateNo`** so two experts can genuinely collide on one plate — before this, a cross-expert search test would have passed by returning the only match in the database rather than by scoping to the caller. (9) **Verified live against the fakes:** 11 keystrokes → **1** request; `?q=` survives reload *and* the claim round trip; Alpha searching Beta's plate and Beta's exact visa both return nothing; whitespace-only returns the full list; 64 chars 200 / 65 chars `400 search_term_too_long`; `%` is literal; and three report PDFs landed `not_applicable` / `PLACEHOLDER-DOC-05` / `application/pdf` / `.pdf` into *Expert documents* with `clientRef` = document id, outbox `sent` on attempt 1 — with the filtered list's media count going 2 → 3 without a reload, which is the `lists()` invalidation proven in the browser and not only in jsdom. **Also recorded:** five capture panels now label their controls identically ("Take a photo" ×5, "Choose a file" ×3) — a real a11y gap left for the week-6 device checkpoint, because fixing it means changing a component 5.1 and 5.3/6.1 must reuse unchanged.

### ☑ 3.3 RealNext3Client + the contract
**Goal:** a real HTTP client that is unit-tested against a stubbed transport, passes the same contract tests as the fake, and waits for a sandbox URL — plus the OpenAPI proposal AXA still has not received.
**Refs:** design.md §6.1 (operations; paths illustrative), §6.2 (interface, `Next3:Mode`), §6.3 (transient allow-list), §12 rows #1/#6/#32, Appendix A `Next3.*`. **Gate status: #1 sandbox absent — mode stays `fake` everywhere; say so in the Friday status.**
**Prompt:**
> Slice 3.3 — the real NEXT3 client per design.md §6, against our own contract.
> **Write `docs/next3-openapi.yaml` from §6.1**: the seven operations; `clientRef` a *required* parameter on the two writes; arrival as an instant plus a `timeZone`; the folder enum; 404-on-unknown-visa; the assignment notification as an inbound callback. This is HANDOFF §8 item 3's artifact — the thing to send.
> **Bind `Next3Options` fully** (today it binds only `DocTypes`): `BaseUrl`, `AuthMode` (`apikey` | `oauth`; mTLS config-ready via a certificate path but not exercised — record), `ApiKey`, `OAuth.{TokenUrl, ClientId, ClientSecret}`, `ArrivalTimeZone`, `TimeoutSeconds` — Appendix A additions, all `PLACEHOLDER-*`. `ValidateOnStart` **only when `Next3:Mode=real`**; fake mode must keep booting on placeholders (`PortSelectionTests.Next3Mode_Real_ResolvesRealClient` supplies valid-shaped options).
> **Client.** `AddHttpClient("next3")` in `ServiceRegistration.AddPorts` — the app's first `IHttpClientFactory`; `RealNext3Client` takes the factory, `IOptions<Next3Options>`, `TimeProvider` (arch rule 1: helpers live in `Api.Integrations.Next3`, referenced through the interface). Mapping: 404 on `GetClaim` → `null`; 5xx, socket and timeout → **let `HttpRequestException` / `TimeoutException` propagate** (they are the outbox's transient list); other 4xx → a new non-transient `Next3RejectedException` → `failed` on attempt 1, visible on A2. Split `ArrivalInfo.OccurredAt` into date + time in `ArrivalTimeZone` **at this edge only** (`TimeZoneInfo.FindSystemTimeZoneById`; IANA ids work cross-platform). Cache the OAuth token until expiry minus skew. **No extra client-side sent-log**: the outbox's `sent` status already is the sent-log and nothing client-side can know whether a timed-out push was accepted — correct §6.3's sentence to that effect; #32 is NEXT3's obligation and the OpenAPI file says so.
> **Tests.** `Next3ClientContractTests` mirroring `BlobStoreContractTests`: one `AssertContract(INext3Client)`, a `[Fact]` against `FakeNext3Client`, a `[SandboxFact]` against `RealNext3Client` that skips with a message unless `NEXT3_SANDBOX_URL` is set (the `[AzuriteFact]` shape with a config probe instead of a TCP probe). Contract: unknown visa → null; blank search → empty; a replayed `clientRef` is accepted without a duplicate; arrival and document round-trip. `RealNext3ClientTests` against a stub `HttpMessageHandler`: auth header per mode, `clientRef` placement, status → exception classification, the 01:30 GST zone-split case from 2.4's notes, token refresh.
**DoD:** `Next3:Mode=real` boots against a stub base URL in a test host and fails fast on placeholders; the contract suite is green on the fake and *skipped with a message* for the sandbox; `docs/next3-openapi.yaml` exists and HANDOFF §8 item 3 points at it; **mode is still `fake` everywhere**.
**Test focus:** exception classification is load-bearing — a 400 must not burn 26 h 36 m of retries and a 503 must not go `failed` on attempt 1; the zone split; arch rule 1 still green.
**Notes:** done 2026-08-21 (395 xUnit, +39, plus 1 skipped; web untouched at 134). **No migration, so no `db-reviewer` pass** — the second slice since 2.1 without one. Nine things worth carrying forward. (1) **The card's own DoD contained the slice's worst bug, and neither the card nor either file could show it alone.** `OutboxProcessor`'s catch is `when (ex is not OperationCanceledException)`, and `HttpClient` reports its *own timeout* as `TaskCanceledException`, which derives from exactly that. So "let `TimeoutException` propagate" would have propagated the wrong type: a NEXT3 timeout escaping the worker mid-batch, abandoning the rows behind it, and stranding its own row in `processing` until the lease expired — never retried on §6.3's schedule, never listed on A2, and the only symptom a queue that quietly stops. The client converts it, with **`ct` as the discriminator rather than the exception type**, and both directions are pinned because a genuine shutdown must stay a cancellation. **Reading the producer against the consumer is what found it; reading either alone would not have.** (2) **`IBlobStore` had no read method.** Put, Exists, Delete, List — and `UploadDocument` needs bytes. Nothing had ever read a blob back, because the media pipeline writes and the cleanup job deletes. A sixth method, `Open`, returning `Stream?` rather than an array so a 15 MB photo is not held in the worker times a batch of ten; **absent is `null`, not a throw**, because §7.3 makes "the bytes are gone" terminal while "storage is unreachable" is worth retrying, and one exception for both collapses them. Both halves went into `BlobStoreContractTests`, so Azurite is held to it too. **A port with no reader is a port with an unnoticed hole in it.** (3) **The predicted arch-rule-1 failure fired, for the wrong reason, and the right fix was not the predicted one.** The plan expected async state machines to trip rule 1's `DoNotHaveName` exemption; NetArchTest filters those. What tripped it was four **hand-written nested records** — `RealNext3Client/Next3ClaimDto` is named `Next3ClaimDto`, so it is not exempt, and it depends on its declaring type. Moving them to namespace level satisfied the rule **as written**: no exemption widened, no guard loosened, which is a better outcome than the planned rule change. **Predict the failure, then let the failure tell you its actual cause.** (4) **The classification test asks the real classifier.** `OutboxProcessor.IsTransient` went `private` → `internal` so a `[Theory]` over ten statuses can assert the *disposition* rather than a copy of the mapping table — 2.4's lesson that a test asserting the code's own arithmetic back to itself can never go red. (5) **The contract's unit is the disposition, not the exception type.** The fake throws `InvalidOperationException` for an unknown visa and the real client throws `Next3RejectedException`; both are right, and what must agree is that neither burns 26 h 36 m on a claim that does not exist. (6) **The sandbox half cannot prove "no duplicate", and says so.** NEXT3 exposes no read-back, so the replay assertion is `RecordedDocuments` on the fake only; the guarantee is *requested* of NEXT3 in the YAML (#32). `[SandboxFact]` therefore requires `NEXT3_SANDBOX_VISA` as well as the URL — **skipping for want of data is honest; passing because every assertion happened to be about absent data would not be.** (7) **`ValidateOnStart` needed its own test or it was decorative.** Every options test resolves `IOptions<T>.Value`, which runs the validator whether or not `ValidateOnStart` is registered — so dropping it would have left the suite green while moving the failure from the deployment log to the first expert who presses Arrived, hours later, as a `failed` row. One test starts a real host. The mode gate is the mirror image and matters as much: an always-on validator stops the whole application booting on the placeholder file every environment runs. (8) **The 401 retry re-opens the blob, and that is the point of rebuilding the request rather than resending it** — the first attempt consumed the stream, so a resent one carries an empty file: a photograph arriving as zero bytes, under the right visa, reported as sent. Pinned by counting blob opens. (9) **design.md was corrected in three places, sixth slice running.** §6.3's promised client-side sent-log was **not built**: the outbox's `sent` status already is the sent-log, a local check cannot see whether a *timed-out* push was accepted (the only case it would exist for), and architecture rule 4 forbids this namespace from touching an outbox row anyway — so it could not have been built where it was described. §6.1 absorbed the two endpoints client doc v0.5 had grown since (`/experts/{expertId}/claims`, garage and claim-officer master data) so the two documents AXA receives agree. And §12's #34 row claimed `IAssignmentSource` had "all three sources built", which was never true — webhook and poll still throw loudly. **Also recorded:** `Next3:Mode` is still `fake` everywhere and the week-3 Friday status must say so; the OpenAPI file validates clean under `redocly lint` (one style warning, a missing `license` field, ignored deliberately — this is a proposal to one client, not a published API).

### ☑ 3.4 Web push + notification log
**Goal:** the assignment popup reaches a browser for real; `IPushSender` becomes pluggable like Blob and NEXT3.
**Refs:** design.md §8 (push rows; web push in browser/PWA; Capacitor provisional), §4 (`notification`; **`push_subscription` is a new table — add it to §4 as realized**), §5.1 (assignment → push), Appendix A (new `Push.*`).
**Prompt:**
> Slice 3.4 — web push per design.md §8.
> **Server.** A `push_subscription` table (`user_id` FK Restrict; the endpoint unique *via a hash column* — endpoints exceed index key limits; `p256dh`, `auth`, timestamps, `revoked_at`) — migration via the skill, db-reviewer mandatory. Authenticated endpoints: `POST /api/push/subscriptions` (upsert by endpoint for the caller — **the once-only guard is the unique index, not a read-then-write**), `DELETE /api/push/subscriptions`, `GET /api/push/vapid-public-key`. `WebPushSender : IPushSender` using the MIT-licensed `WebPush` NuGet (licence checked, 2.3's rule) behind an injectable transport so tests need no network; it sends to every active subscription of the user, revokes on 404/410, and writes **one `notification` row per send attempt** — `sent` if at least one subscription accepted, `failed` otherwise, so `AssignmentHandler`'s `NotifiedAt` semantics are unchanged. Add a `Push:Mode = fake | webpush` switch in `AddPorts` mirroring `Blob:Mode`; `Push:Vapid:{Subject, PublicKey, PrivateKey}` are **placeholders only** — real dev keys live in user-secrets/env (write the `dotnet user-secrets` steps into CLAUDE.md), never in the repo; `ValidateOnStart` when the mode is `webpush`. Payload `{title, body, url}` with `url = /expert/{assignmentId}`.
> **Web.** `public/sw.js` as a plain service worker (no vite-plugin-pwa — smaller): `push` → `showNotification`, `notificationclick` → focus or open `data.url`. A `push/usePushSubscription` hook with injectable `serviceWorker` / `pushManager` / `Notification` (the idiom), `urlBase64ToUint8Array`, and an explicit **"Enable notifications" button in `ExpertLayout`** — permission needs a user gesture — that subscribes and POSTs. Capacitor native push stays in 6.3.
**DoD:** `POST /api/admin/dev/assignments` for a subscribed expert shows a desktop Chrome notification whose click opens E2; `notification` rows `sent`; a 410 from the stub transport revokes the subscription; an unsubscribed user yields a `failed` row with `NotifiedAt` null (2.1's path preserved); manual Chrome pass including DevTools → Application → Push.
**Test focus:** server — upsert idempotence under four parallel POSTs (1.5 / 2.2 / 2.4's lesson, fourth time), revoke on 410, per-attempt logging, mode switch and startup validation; web — the hook with injected stubs (denied / granted / unsupported), button states, no subscription attempt without a gesture.
**Notes:** done 2026-08-21 (435 xUnit, +40, plus 1 skipped; web 134 → 152). One migration, db-reviewed. Ten things worth carrying forward. (1) **The card's licence claim was wrong, and 2.3's rule is why that mattered.** `WebPush` is **MPL-2.0**, not MIT, and pulls the deprecated `Portable.BouncyCastle` — a copyleft dependency in a codebase heading for handover with HANDOFF §5's IP conversation still open. Switched to **`Lib.Net.Http.WebPush` (MIT)**, which also fits better: `PushServiceClient(HttpClient)` *is* the injectable transport the card asked for, reusing 3.3's `IHttpClientFactory` and `StubHttpMessageHandler`, and on net10.0 it drops BouncyCastle entirely. **Check the licence from the `.nuspec`, not from the card that told you it was fine.** (2) **The `db-reviewer` found the worst bug for the sixth migration running, and it was the same bug as last slice.** `HttpClient` reports its own timeout as `TaskCanceledException`, which derives from `OperationCanceledException` — so a filter naming `TimeoutException` never matches. Slice 3.3 closed exactly this on `RealNext3Client`; it was not carried across, and here it was worse: the exception escapes the per-subscription loop (the expert's other devices never attempted, no `failed` row for §8, staged revocations discarded) *and then* escapes `AssignmentHandler`'s catch, whose filter also excludes `OperationCanceledException`, faulting the ingestion path. **A lesson learned in one adapter is not learned until it is checked in the others.** (3) **`IBlobStore` needed a method last slice; `IPushSender` needed a parameter this one.** The payload must carry `url = /expert/{id}` and the interface had nowhere to put it — now a `PushMessage` record, which is also the JSON `sw.js` reads. (4) **`notification.recipient_address` is nvarchar(320)** and push endpoints are longer, so a push row records the **subscription id**; the endpoint lives on the row it joins to. Sized for email in 1.4 and nobody had reason to notice until a third channel arrived. (5) **The card contradicted itself on row granularity** — "one per send attempt" versus "sent if at least one accepted". Resolved as one row per subscription (per-device is what answers a support question) with the aggregate surviving in the *return value*: throw unless one accepted, so partial success stamps `notified_at` and total failure leaves it null. (6) **Two SSRF-shaped findings the tests would never have surfaced**: the endpoint is a caller-supplied URL the API later POSTs to, and nothing capped how many a user could register. Now a host allow-list (real values, not placeholders — these are the browsers' own services) and a per-user cap, because the sender walks them serially on the ingestion path. (7) **`Base64Url.TryDecodeFromChars` throws.** Despite the name, it returns false only for a small destination and raises `FormatException` on a bad character — so key validation was a 500 instead of the 400 it exists to produce. `Base64Url.IsValid` first. (8) **A fixture can be well-formed and still not real.** The test `p256dh` was 65 correctly-shaped bytes and not a point on the curve, so the library's ECDH import threw a platform exception that looked exactly like a broken sender for twenty minutes. Real keys are generated per run now — for the browser side *and* the VAPID pair — and never committed, which is the same rule the application follows. (9) **Two tests asserted an ordering the query never promised**, and one of them passed alone and failed in the full suite. Both now assert the property (one sent, one failed; exactly one revoked) rather than which device drew which response. (10) **Minimal APIs will not infer a body on DELETE** — without `[FromBody]` the entire application fails at startup, which is how a finding the reviewer had ranked "low" turned out to be a hard blocker. Also: **`sw.js` is linted after all** (a small flat-config block for the service-worker globals; `/* eslint-env */` is gone in flat config) and it immediately caught a redundant initializer — the parse error that revealed it was a `*/` hidden inside a glob pattern in the file's own header comment. **Recorded gap:** a browser that rotates its endpoint 410s and is revoked, and nothing re-registers it, because `pushsubscriptionchange` would need a token the service worker does not have — on the **week-6 device checklist**. **The manual Chrome pass then earned its keep for the fifth slice running, and found two bugs the whole suite was blind to.** (a) **User secrets were being silently overridden by the placeholder file.** `CreateBuilder` adds them, and `Program.cs` then appended `appsettings.Placeholders.json` *after* — so the API refused to boot on VAPID keys that were demonstrably set, and would have done the same for `Auth:Jwt:SigningKey` or NEXT3's credentials. Fixed by re-adding user secrets after the placeholders; that in turn made the test host read the developer's own secrets, so `ApiFixture` now runs as **Production** and the suite is hermetic (the app has exactly one `IsDevelopment()` branch — the line that was just added). (b) **The panel offered "Enable notifications" on every page load to an expert who had already enabled them**, because the hook never read the subscription that already existed. Now it does, on mount, via `getRegistration()` rather than `ready` (which never resolves on a browser that has never registered a worker) — reading prompts for nothing, so it is safe where subscribing is not. **Verified live end to end against a real push service:** service worker registered at `/sw.js`; two successive subscribes produced exactly **one** row and **zero** UPDATEs (EF saw identical values, so the upsert issued no SQL at all); an injected assignment logged `1 of 1 subscriptions accepted` and Chrome displayed a real notification with `data.url = /expert/{assignmentId}`, `requireInteraction: true` and the url as its tag; that URL loads E2 for the right claim; and unsubscribing the browser — a genuine dead endpoint, not a stub — produced `410`, `Push subscription … revoked`, `0 of 1 subscriptions accepted`, and `Assignment … recorded, but notifying the expert failed`, which is 2.1's contract intact. **Both refusal branches also fired against real Chrome**: a dismissed prompt as `denied`, and Chrome's automatic block after that dismissal as `blocked` — the distinction earned its keep immediately, because pressing the button again genuinely could not fix the second and the panel said so instead of looping the expert.

**Week-3 Friday check:** the fake→real swap did **not** happen this week — the gate (#1) was closed. The status to the client must say: "RealNext3Client built and contract-tested, OpenAPI proposal sent, running on the fake; sandbox date unknown." If the sandbox is still absent at the week-4 demo, the day-for-day clock starts there.

## Week 4 — Declaration + DEMO

*Expanded 2026-08-22 from the week-3 Notes. Decisions at expansion: **push deferral is a third `push_status` value, `deferred`**, driven by a `PushTiming` field on `BucketRule` (§5.2's "nothing goes to NEXT3 before approval" has no mechanism today — every upload writes an outbox row); **the garage declaration buckets land in 4.1** (G2 attaches documents at Draft; `MediaBuckets`' comment deferring the garage row to 5.1 predates the week plan — 5.1 keeps the *repair* buckets); **approval is two calls** (the browser renders and uploads the comments PNG into an `approval_image` bucket, then `approve`, which refuses without it); **approve verifies the visa with `GetClaim`** (a visa NEXT3 does not know would only surface 26 h later as a `failed` push); **the officer needs a document-content endpoint** (`IBlobStore.Open` exists since 3.3; nothing serves bytes over HTTP); and **`state` is the EF concurrency token** — it is what every transition changes, unlike 2.4's `ArrivedAt`, so it is the once-only guard (sixth outing for 1.5's lesson).*

### ☑ 4.1 Declaration entity + state machine (server)
**Goal:** the §5.2 machine as an entity that owns its transitions, with the schema, buckets, endpoints, notifications and audit that make every row of the transition table executable. G4's repair submission is excluded (5.1).
**Refs:** design.md §5.2 (the transition table is the test plan), §4 (`declaration`, `declaration_comment`, `document.push_status`), §7.1 garage rows, §8 (officer + garage rows), §9 (resource-level checks; audit events), §12 #16/#18, Appendix A `Next3.DocTypes.*`.
**Prompt:**
> Slice 4.1 — the declaration state machine per design.md §5.2, test-first from its transition table.
> **Schema — one migration, db-reviewer mandatory.** `declaration` per §4 with a timestamp per transition; `state` check-constrained to the six values **and configured as the EF concurrency token** (2.4's note warned against a token on a column not every write changes — `state` is what every transition changes, and comments live in their own table); `declaration_comment` per §4. `document.push_status` gains **`deferred`** and both of its check constraints are rewritten (`deferred`/`n/a` ⇒ no outbox id; `queued` ⇒ outbox id). Add a `PushTiming { Immediate, OnApproval, Never }` field to `BucketRule` and three rows with `OwnerKind Declaration` and `Next3Folders.Survey`: `garage_documents` (Document, upload allowed, `DocTypeKey SurveyDocument`, OnApproval), `garage_car_photo` (Image, capture-only, `GarageCarPhoto`, OnApproval), `approval_image` (Image, `AllowUpload false` — produced in-app, `ApprovalImage`, OnApproval). `SurveyDocument` and `GarageCarPhoto` are new Appendix A placeholder keys under #12 (realized). `MediaUploadService.Record` branches on `PushTiming`: Immediate is today's path; OnApproval stores `deferred` with no outbox row; Never is reserved for 5.2. `MediaBucketTests.EveryExpertBucketLandsInTheExpertDocumentsFolder` must be scoped to assignment rows — say why in the diff (it was right about a guarantee that no longer holds). Retention needs no change: sweep 1 joins on outbox `sent`, sweep 2 spares any live row — a deferred document's bytes are kept until approval enqueues them (record that a *rejected* declaration therefore retains its blobs for ever — a 7.2 ticket, not this slice).
> **Entity.** `Declaration` exposes `Submit()`, `Approve(officerId, visaNo)`, `Reject(officerId)`, `StartRepairs()` — each validates the current state and throws `IllegalTransitionException`; nothing outside the entity sets `state`; no resubmit edge and no closure state (§1). A `DeclarationService` owns the transactions: **submit** → state + `submitted_at` + audit, then best-effort fan-out to **every active `claim_officer` user**: `IPushSender.Send(officer, PushMessage(…, "/officer/{id}"), NotificationTemplates.DeclarationSubmitted)` per officer, catching `PushNotDeliveredException` per officer and falling back to `IEmailSender` at the profile email (§8's "+ email fallback"); **approve** → `409 approval_image_required` without an `approval_image` document, then `INext3Client.GetClaim(visaNo)` (null → `422 visa_not_found`; unavailable → 503 — approval can wait, the garage's upload could not), then one transaction: state, `visa_no`, `officer_user_id`, `decided_at`, comment rows, **flip every `deferred` document to `queued` with `OutboxWriter.EnqueueDocument(visa, Survey, …)` and the approval image via `EnqueueApproval`**, audit; then push to the garage (`/garage/{id}`); **reject** → state, comment rows, audit, push garage, no NEXT3 push; **start repairs** → state, audit. New `AuditActions.declaration_*` and `AuditEntityKinds.declaration`. A lost concurrency race re-reads and answers `409 illegal_transition`.
> **Endpoints.** Garage group `/api/garage/declarations` under `AuthPolicies.Garage`, ownership on every read and write (404 for not-mine, the 2.x convention): `POST /` (Draft — plate required, insured name and note optional), `GET /` (worklist, newest first, state, media count), `GET /{id}` (state-dependent DTO: claim fields via `ClaimCache.Open` and comments **only when approved**), `POST` + `GET /{id}/documents` (the media pipeline with a `MediaUploadTarget(Declaration, id, …)` — the visa is unknown at Draft; the outbox rows are written at approval with the real one), `POST /{id}/submit`, `POST /{id}/start-repairs`. Officer group `/api/officer` under `AuthPolicies.ClaimOfficer`: `GET /declarations?state=submitted` (inbox, all garages), `GET /declarations/{id}` (full detail incl. garage contact), `GET` + `POST /declarations/{id}/documents` (POST accepts `approval_image` only), `GET /claims/search?plateNo=&visaNo=` → `INext3Client.SearchClaims` (arch rule 3 permits reads; blank both → 400; NEXT3 down → 503), `POST /declarations/{id}/approve` (`{visaNo, comment}`), `POST /declarations/{id}/reject` (`{comment}`). Keep every DTO at namespace level (3.3's arch-rule-1 lesson).
> **Tests, written first.** One test per transition-table row and one per illegal pair; four parallel submits and four parallel approves each yield exactly one transition — **verify by removing the concurrency token**; approval atomicity: comment rows, document flips and outbox rows commit or roll back together (`WithNext3Down` *after* the visa check); deferred documents survive `Sweep()`; approve without image → 409, unknown visa → 422; submit writes one `notification` row per officer and the email fallback fires on `PushNotDeliveredException`; garage A cannot read, upload to, or submit garage B's declaration. Generalize `MediaFlows.Upload` with a path parameter; add `DeclarationFlows` (`CreateGarage`/`CreateOfficer` over `CreateUser`, `CreateDraft`, `UploadSurvey`, `UploadApprovalImage`, `Approve`).
**DoD:** `dotnet test` green incl. architecture tests; migration db-reviewed; one integration test runs the whole chain: draft → upload (`deferred`, no outbox row) → submit (officers notified) → approval image → approve → outbox rows → `OutboxProcessor.RunOnce` → `sent` in the fake's *Survey* folder with `clientRef` = document id.
**Test focus:** the concurrency token is load-bearing; the flip to `queued` must satisfy the rewritten check constraint under RCSI; no new `INext3Client` path (so no new cancellation edge — but check it anyway, 3.4's lesson).
**Notes:** done 2026-08-22 (491 xUnit, +56, plus 1 skipped; web untouched at 155). One migration, db-reviewed. Nine things worth carrying forward. (1) **The transition table is a table, not a switch, and that is what made the illegal pairs testable.** `DeclarationTransitions` encodes §5.2 as data the way `MediaBuckets` encodes §7.1, and `Declaration` holds no second copy — every method routes through `Target()`. The legal edges are five rows anybody would write; the illegal ones are 6 × 5 − 5 = **25** combinations nobody enumerates by hand, and the suite sweeps them from the enums, so a seventh state cannot be added without it noticing. Verified by planting the resubmit edge §1 forbids: three tests go red. (2) **The generated check constraint was invalid T-SQL and would have failed at `database update`.** EF happily emitted `([state] IN ('a','b')) = (CASE …)`; T-SQL has no boolean *value*, so each side has to be its own `CASE … THEN 1 ELSE 0 END`. Then the db-reviewer found the *logic* was wrong too — testing the conjunction `officer IS NOT NULL AND decided_at IS NOT NULL` makes the undecided side true whenever *either* is null, so a draft carrying an officer id and no timestamp passed. **Three biconditionals, one per column**, and seven `[InlineData]` rows that insert the bad rows directly, because no row had ever been tested against it — the DDL runs on every `MigrateAsync`, and parsing is not refusing. (3) **The db-reviewer's CRITICAL finding was wrong, and checking cost five minutes.** It reported that `ContentDispositionHeaderValue.Name` keeps its quotes, so every real-browser upload had been dead since 2.3. Three recorded Chrome passes said otherwise; a throwaway probe test settled it — the getters *do* strip quotes. **But the same finding's second half was right and serious:** `Path.GetInvalidFileNameChars()` returns 41 characters on Windows and exactly ` ` and `/` on the Linux containers §10 deploys to, so the sanitiser did almost nothing where it runs and everything where it is tested. CR and LF surviving into the `Content-Disposition` `RealNext3Client` writes is a header-injection primitive chosen by whoever picked the file. Replaced with an explicit deny-list plus `char.IsControl`. **Check the reviewer's findings the way it checks yours.** (4) **`document.file_name` was not in the card and had to exist.** A deferred push is built at approval — a different request, hours later — and the name lived only in the upload's header, so a garage's `invoice.pdf` would have reached the *Survey* folder as `019ab….pdf`. Third column beyond §4's list for this table; design.md updated, not diverged from, seventh slice running. (5) **Two authorization holes the card did not describe, both from the reviewer's pass.** `approval_image` shares the `declaration` owner kind, so without a per-caller bucket allow-list a **garage could attach its own approval** and satisfy the gate that exists to make an officer sign the decision — and capture-only would not stop it, because `origin` is a claim the client makes. And an OnApproval bucket uploaded *after* the approve transition is a `deferred` row nothing will ever enqueue: never pushed, absent from A2, excluded from §7.3's sweep, blob retained for ever, nothing thrown. Both closed; the allow-list refuses before the file is read, so a rejection costs no blob. (6) **The card's atomicity test was unreachable, and saying so was better than writing it.** `WithNext3Down` sets `Fake:FailureRate = 1.0` and one `FakeBehavior` serves NEXT3 *and* all three senders, so it fails the visa check rather than the transaction — there is no seam over HTTP. Three tests instead: the 503 path writes nothing; **four parallel approvals** are the real seam (each loser stages a comment, three flips and four outbox rows, and every one must vanish); and a poison-row test against the mechanism. (7) **`state` as the concurrency token is load-bearing, verified by removing it** — all four concurrency tests go red. The nastiest of them is approve-versus-reject: an approval landing after a rejection would give a disregarded claim a visa and push its documents to AXA. (8) **`Next3PushKind` went on `BucketRule`** rather than a `bucket == "approval_image"` test in the service — the reviewer predicted that special case before it was written, and it is the rule-outside-the-table drift `MediaBuckets` exists to prevent. Two DTOs also moved out of `Api.Modules.Expert` (`DocumentDto`, `ClaimDto`) so the declaration module does not reference the expert module to describe a document or a claim. (9) **A test asserted an ordering the query never promised, for the second slice running** — the frozen `FakeTimeProvider` gives all three audit rows one timestamp, so `ORDER BY at` is a tie. Now asserts the set *with its actors*, which is the part a claims dispute turns on. **Recorded gap:** a rejected declaration (and an abandoned draft) retains its blobs for ever — design.md §7.3 rather than a code comment, carried as a **7.2 ticket**, because deleting a rejected claim's photographs needs a client answer (#4/#22). **No manual browser pass: this slice ships no UI.** 4.2 is where the screens and the Chrome pass land.

### ☑ 4.2 Garage G1–G3 + Officer O1–O2 (web) + the content endpoints
**Goal:** both views of the one state machine in the browser, the approval PNG rendered client-side, documents visible to the officer.
**Refs:** design.md §5.2 (both views; "approval unlocks detail"), §2 (G1–G3, O1–O2; E4 shared), §7.1 garage rows, §1 (rejection shows status only — will surprise users), §8.
**Prompt:**
> Slice 4.2 — the garage and officer screens per design.md §5.2 and §2.
> **Server.** `GET /api/garage/declarations/{id}/documents/{docId}/content` and the officer equivalent: stream `IBlobStore.Open` with the stored content type; 404 when not owned or `blob_deleted_at` is set; no SAS URLs. The officer endpoint covers any declaration.
> **Routing.** `homePathFor` gains `garage → /garage` and `claim_officer → /officer` (today both land on the admin screens). `GarageLayout` with `PushPanel` (garages get popups too) and `OfficerLayout` (desktop), mirroring `ExpertLayout`; `sw.js` already carries the url.
> **Garage module `src/Web/src/garage/`** mirroring `expert/`: `api.ts` (DTOs, `garageKeys {lists(), list(), detail, documents}`, `declarationDocumentsPath(id)`), hooks, pages. **G1** worklist table (state, plate, created, media count) with E1's shape (`keepPreviousData`; no search). **G2** create form (plate required) → navigate to G3 in `draft`. **G3** state-dependent: `draft` → `CapturePanel`s for `garage_documents` (upload + capture) and `garage_car_photo` (capture-only) on the declaration path plus a **Submit** button with the `inFlight` latch, disabled until at least one document exists (smaller interpretation — record); `submitted` → "waiting for AXA"; `rejected` → status only, **no comments** (§1); `approved` → `ClaimFields`-style table from the claim DTO, officer comments, **Start repairs**; `repairs_in_progress` → a placeholder naming 5.1. Invalidate `garageKeys.lists()` after every upload and transition (3.2's `lists()` lesson).
> **Officer module `src/Web/src/officer/`.** **O1** inbox table (newest first: garage contact, plate, submitted at, media count). **O2** review: declaration fields, documents with inline image previews via the content endpoint and named links for PDF/audio (2.5's broken-image lesson), a **visa search** box (plate or visa → results table → "Use this visa"), the #16 hint when nothing matches ("create the visa in NEXT3, then search again"), a comments textarea, **Approve** / **Reject** behind one `inFlight` latch. Approve = render the comments block to PNG → upload it to `approval_image` with `photographic: false` (3.1's lesson: the caller knows what the MIME type does not) → `POST approve`; a render or upload failure stops *before* the approve call — §5.2's clean abort.
> **The PNG.** Generalize `media/diagram/render.ts` into a shared `media/png.ts` (`renderSvgToFile(markup, size, fileName, render)`, `browserSvgRenderer`) used by the diagram **and** a new `media/approval/` builder that lays the decision out as SVG `<text>` lines — declaration id, visa, decision, officer, timestamp, wrapped comments — never `foreignObject` (serialized SVG carries no page CSS, and foreignObject taints the canvas), at the diagram's 1600×1200 so the floor guard applies. It lives in `media/` so `reusability.test.ts` keeps covering it (raise its non-vacuity count).
> **Tests (web).** G3 renders one assertion per state (comments absent on `rejected`, present on `approved`); the submit latch; the approve sequence order with an injected renderer (render → upload → approve; an upload failure makes no approve call); visa search → selection; previews point at the content endpoint; `homePathFor` for both roles; the reusability guard green with `approval/` added.
**DoD:** `npm test` + `dotnet test` green; **manual Chrome pass in two browser profiles** (garage, officer): create → capture → submit → officer notification → search visa → approve → outbox rows `sent` into the fake's *Survey* folder → garage notification → G3 unlocks; reject shows status only; every update arrives by invalidation, never by reload.
**Test focus:** state-dependent rendering is the transition table from the other side; the approve sequence's failure ordering; content-endpoint authorization (garage cannot read another garage's document; officer can read any).
**Notes:** done 2026-08-22 (500 xUnit, +9, plus 1 skipped; web 155 → 197, +42). No migration, so no `db-reviewer` pass — the third slice since 2.1 without one. Eight things worth carrying forward. (1) **The card asked for something a browser cannot do.** "Inline image previews via the content endpoint" and "previews point at the content endpoint" both imply an `<img src="/api/…">` — but the session is a bearer token in a header, an `<img>` sends none, and the 401 branch in `api()` hard-navigates to `/login`. An officer would have been **signed out by looking at a photograph**. So `apiBlob` was added beside `api<T>()`, sharing one extracted `send()` so the refresh-and-retry cannot drift; the *fetch* points at the content endpoint and the `src` is a `blob:` URL. **Read the card's mechanism against the transport it will actually run on.** (2) **A `fireEvent`-based latch test passes with the latch deleted**, and that was found by planting rather than by reasoning. `fireEvent` flushes React between events, so by the second click `isPending` is true and the `disabled` attribute swallows the rest — a real guard, but not the one under test. Three raw `.click()`s inside one `act` reproduce two taps in the same tick, and then the plant goes 1 → 3 requests. **A concurrency test that never puts two calls in one tick is testing the button, not the guard.** (3) **The content endpoint needed two checks, not one.** Resolving the *declaration* for the caller is not enough: matching the document by `docId` alone lets a garage read any document in the system by quoting its id under a declaration of its own — an authorization check that looks present and tests the wrong thing, which is worse than an absent one because nobody re-reads it. Pinned by a test that stays green when the *declaration* check is removed and goes red only for this one. (4) **`Results.File`/`Results.Stream` with a download name emit `attachment`**, which turns O2's inline preview into a download prompt per photograph; the response is an `IResult` type instead, which is also where `nosniff` lives. (5) **The generalization cost no test churn.** `media/png.ts` took everything mechanical and `diagram/render.ts` kept only what is about diagrams, aliasing the renamed types — `DiagramPanel` and its 15 tests passed untouched. (6) **The React lint rules reject `setState` in an effect body**, which is how the first preview hook was written; rebuilt on `useQuery` with the effect doing only revocation, which also caches bytes across a remount. (7) **`document.fileName` had never reached the web types** — 4.1 added the column and the server DTO, and `AssignmentDocument`/`UploadedDocument` still predated it; O2's named links need exactly that field. 4.1's two new refusal codes were also missing from `UPLOAD_EXPLANATIONS` and rendered as a bare "(400)"/"(409)". (8) **The reusability guard gained `garage/` and `officer/`** the moment those folders existed, plus an `/approval/` reach assertion and a raised floor — verified by planting an import. **Recorded:** the approval PNG is uploaded through `uploadDocument` rather than `useCapture`, so Approve stays one press (the card's `photographic: false` has no meaning outside `useCapture.select`; the fixed 1600×1200 satisfies the *server's* floor either way) — see scope-decisions.md. **The manual Chrome pass ran and found one real bug, sixth slice running.** **`PushPanel` was telling a garage the wrong thing about its own workflow** — it was written for `ExpertLayout` and promised a popup "the moment a claim is assigned to you", which then sat above every garage screen. A garage is never assigned a claim; §8's garage rows are decisions on work the garage filed. Invisible to the suite because every push test rendered the panel with no role in mind and asserted the button, never the sentence. The copy is a `PushCopy` parameter now (in `push/copy.ts`, because `react-refresh/only-export-components` forbids a `.tsx` exporting constants — `TestQueryProvider`'s reason), defaulted to the expert wording so nothing that existed changed, with two tests. **Verified live, end to end, against Azurite and the fake NEXT3:** `homePathFor` landed both new roles on their own screens; the garage's `Car photos` panel rendered **no file picker** while `Survey documents` did (§7.1 in a real browser); the clarity gate ran on the capture at **1600×1200, sharpness 14984**, and a PDF went to the confirm screen **named, with no sharpness line** (2.5's lesson holding); both documents stored `deferred` with **no outbox row** and their file names kept; **three rapid taps on Submit produced exactly one UPDATE, one fan-out and zero 409s** — the latch, in a real browser; the officer's inbox carried the garage contact; **the image preview decoded at `naturalWidth: 1600, naturalHeight: 1200` from a `blob:` URL** — the finding that made this slice, since the card's literal `<img src="/api/…">` would have been 0 and a logout; the PDF stayed a lazy button until clicked, then became a `download` link; blank search was refused client-side and an unknown visa produced #16's hint verbatim; **Approve rendered a real 128 KB PNG, uploaded it, then approved — in that order** — with a comment containing `<` and `&`, which proves the XML escaping against a real canvas; all three documents flipped to `queued` and reached **`sent` in the *Survey* folder**, the approval under the distinct `push_approval` operation, with `invoice.pdf` surviving from the garage's upload hours earlier into the push built at approval (which is the entire argument for `file_name`); G3 then unlocked with the claim detail and the comment; **rejection left `visa_no` NULL, 0 pushed documents, 1 stored comment — and the garage saw status only while the officer saw the reason** (§1, and it will surprise users). **Two things deliberately not done and recorded rather than claimed:** real browser *push* was not re-tested — permission was `default` and Chrome's permission bubble is browser UI this harness cannot click without risking a blocked session, and slice 3.4 already proved the mechanism end to end against a real push service; both notifications were instead observed taking §8's **email fallback**, which is itself the first live proof of 4.1's broad per-officer catch. And both roles shared one browser profile, so the sessions were sequential rather than simultaneous. **Also noticed, not fixed:** G1's media count includes the officer's approval image (3, not the 2 the garage uploaded) — defensible, since it is the declaration's media, but worth a decision in 4.3.

### ☑ 4.3 Demo prep — the milestone session
**Goal:** a rehearsed, resettable, 15-minute demo of §5.1 + §5.2 on the fakes with real browser push — and the fix list it produces.
**Refs:** design.md §11 (week-4 milestone → 30%), §6.2 (the fake is selectable and failure-injectable), Appendix A `Fake.*` and `Push.*`; HANDOFF §8 (the scope letter precedes client exposure).
**Prompt:**
> Slice 4.3 — demo preparation; verification, not feature code.
> Write `docs/demo-week4.md` (internal) as beats: (1) admin creates an expert, a garage and an officer — invites read from the fake SMS log; (2) inject an assignment (`POST /api/admin/dev/assignments`) → a real Chrome notification → E2 → Arrived → the four buckets, voice note, diagram, report → a blurry photo refused client-side → outbox rows `sent`; say plainly that the failed-push screen is 6.2; (3) **kill NEXT3**: set `Fake:FailureRate` to `1.0` in the placeholder file (it reloads live), upload more photos — they land, the queue grows, the expert never notices; restore it → the queue drains: the outbox's sales pitch, and the §6.3 retry schedule in one sentence; (4) garage declaration → officer notification → visa search → approve → PNG and documents pushed to *Survey* → garage notification → unlock; reject a second declaration; (5) what on screen is a placeholder and why — doc-type codes, insurance types, the NEXT3 URL — so nothing looks like invented client data.
> Write `scripts/demo-reset.ps1`: drop and recreate the `AxaMotorClaims` LocalDB, `dotnet ef database update`, seed the three demo users and two of the fake's seeded visas **through the admin API** (never SQL), start Azurite (npm) and the API with `Blob__Mode=azure` and `Push__Mode=webpush` using the user-secrets VAPID pair from CLAUDE.md; idempotent; under two minutes. Dry-run the whole script twice end to end in Chrome; every friction point goes on a fix list; fix only what breaks a beat — everything else becomes a 7.2 ticket.
> **The client-comms gate.** The scope letter (Option 2 in, one UAT round, §6.3's duplicate caveat: without NEXT3-side dedupe, timed-out retries can duplicate documents) and `docs/next3-openapi.yaml` go out **before** the demo; the Friday status says the sandbox gate is still closed and that the day-for-day clock starts at the demo if it stays closed.
**DoD:** two clean dry runs from `demo-reset.ps1`; `docs/demo-week4.md` followed without improvisation; the fix list triaged into "before demo" and "7.2"; scope letter and OpenAPI file sent — or the reason they were not, written into the status.
**Test focus:** none new. Any fix that is code follows the slice rules (tests; db-reviewer if schema).
**Notes:** done 2026-08-22. **No feature code and no test change** — 500 xUnit + 1 skipped and 199 web, both untouched, which is what a verification slice should look like. Seven things worth carrying forward. (1) **Rehearsing the document *is* the deliverable.** Ten items on `docs/demo-fix-list.md` were fixed before the demo, and three of them would have broken a beat in front of the client: beat 2's assignment injection was written as an inline `Invoke-RestMethod` against an `$adminToken` that exists nowhere (now `scripts/demo-assign.ps1`, which refreshes the stored admin session so it still works at minute 20); the pre-flight named only notification permission, when **Arrived** needs location and the voice note needs the microphone — three Chrome bubbles, in front of a client, mid-beat; and every button label in the first draft was wrong (*"Use this photo"* is **Confirm**, "submitted" is **Waiting for AXA**, "rejected" is **Not accepted**). **A demo script written from the source is a script for a different application.** (2) **The outage beat's timing was wrong, and only a dry run could have said so.** The card promises a queue that drains; the draft promised ~90 s. If a queued row fails a *second* time before the failure rate is restored, its next retry is **five minutes** — §6.3's schedule doing exactly what it says, and far too long for a fifteen-minute demo. The beat now says restore within ~40 s of the last upload, and carries a recovery line for when it happens anyway (name the five-minute step, go to beat 4, show the drained queue after). Verified both ways: dry run 1 hit the five-minute step, dry run 2 restored inside the window and drained in ~90 s with both rows recovering on attempt 2. (3) **The demo needed a queue viewer, because A2 is 6.2 and nothing in the product shows the outbox.** `scripts/demo-outbox.ps1` + `.cs` is read-only — CLAUDE.md's never-SQL rule is about *seeding*, and observing is the one thing SQL is allowed to do here. It is a **.NET 10 file-based app** (`#:package`) rather than PowerShell, because `Microsoft.Data.SqlClient` will not load into pwsh 7.6 ("not supported on this platform" — the netstandard facade resolves there), and rather than a project so it adds nothing to the solution. It also earned its keep by being wrong twice: it showed a future `retry_at` on `sent` rows (the worker's lease, which on a projector reads as "this will be sent twice"), and it failed CA1305 while the reset's warm-up swallowed the error. (4) **The blurry photo had to be blurry *by the gate's own arithmetic*, not by eye.** `scripts/demo-media.mjs` generates a hard checkerboard and a smooth gradient at 1600×1200, and both were scored through the real `assessClarity` before either reached a browser: 49761 and 0.84 against a threshold of 100. Nothing binary enters the repo. (5) **The browser pass found a real bug for the seventh slice running, and this one is silent.** A browser has **one push subscription per profile, not per user**: `usePushSubscription` reads `pushManager.getSubscription()` on mount, and when the browser already has one it sets `enabled`, shows *"Notifications are on"*, and `enable()` early-returns — so **no POST is ever made for the second user to sign in on that profile** and the server holds no row for them. Reproduced exactly: the garage signed in after the expert, was told notifications were on, and got the email fallback while `push_subscription` held one row. Unsubscribing and pressing again produced a proper garage row and a real popup. **design.md §4 anticipated precisely this** when it scoped the unique index to `(user_id, endpoint_hash)` "so two people sharing a browser profile each keep their own row" — the schema allows for it and the client defeats it, which is the inverse of the bug 3.4 fixed. **Carried to 7.2 rather than fixed:** it breaks no beat (one Chrome profile per role), and the fix is a mount-time behaviour change in a component 5.2, 5.3 and 6.1 all reuse. The pre-flight now says one-profile-per-role is load-bearing rather than tidy. (6) **The three permission-gated steps were the whole reason for two dry runs.** Dry run 1 ran beats 1–5 with notifications, location and microphone all at `prompt`, stubbing geolocation and skipping the voice note; dry run 2 ran them for real once the developer granted the three. **Both §8 push rows are now proven end to end against a real push service** — the expert's assignment popup (confirmed on the desktop by screenshot as well as in the service worker) and the garage's rejection popup, each with the right deep link — which closes the gap 4.2 recorded and did not close. Also live: **Arrived against real GPS** (the app's own `enableHighAccuracy` options resolve in ~2.3 s on this machine), and a real `MediaRecorder` note, 384,764 bytes, `audio/webm`, `not_applicable`, `sent` on attempt 1. (7) **`demo-reset.ps1` is 18 seconds and idempotent**, and it seeds **through the admin API only** — create → read the invite token out of the fake SMS log → accept → read the OTP → verify. That is not ceremony: it means a broken onboarding path fails the reset rather than the demo. Its one sharp edge is recorded rather than engineered away — activation has just sent every user a code, so sign-in is throttled for 60 s (`Auth:OtpResendSeconds`), which the closing card now says out loud; shortening a §9 control to suit a rehearsal was not on the table. **The client-comms gate is NOT met:** `docs/scope-letter.md` and `docs/status-2026-08-22.md` are drafted and **unsent**, and `docs/next3-openapi.yaml` has been ready since 3.3 and is **unsent**. Sending is the developer's act and nothing was sent from a build session. **Until all three go out, the demo does not happen** — that is the gate, and it is now the only thing standing between this slice and the milestone.

### ☑ 4.4 Presentable pass — implement the design system (one session)
**Goal:** the real app looks like the three design canvases. The canvases are mockups; nothing in `src/Web` changed when they were made. This slice is the *how*: tokens as a stylesheet, the existing components restyled to the component sheet, the net-new shell pieces, and the one shipped defect pass 3 found — **without changing the behaviour of anything 5.1–6.1 must reuse**.
**Refs:** `docs/design-prompts/pass-1-summary.md` (the contract — tokens, names, strings), `pass-2-review.md` and `pass-3-review.md` (decisions), the three canvases for pixels; design.md §2 (screens), §7.1 (bucket rules are unchanged).
**Prompt:**
> Slice 4.4 — make the app look like the design system in `docs/design-prompts/pass-1-summary.md`. Work the priorities **in order** and stop at the line if the session runs short; everything above the line must be whole. No behaviour change to `media/`, `push/`, `expert/`, `garage/`, `officer/` hooks — styling, markup and copy only, plus the named additions below.
> **P1 — foundations.** One stylesheet (`src/Web/src/styles/`): the pass-1 tokens as CSS custom properties (slate neutrals, accent `#2E6BB8` with its 700/100 derivatives as the *only* brand-swappable slot, semantic chip pairs, radii 4/6/10/999, shadows sm/md, focus halo), the type scale, 48 px touch / 36 px desktop control sizes, 16 px inputs. **Self-host IBM Plex Sans + Mono** (`@fontsource` or woff2 in `public/fonts`; system stack fallback) — a Capacitor app at a crash site must not depend on a font CDN. Light only; dark tokens defined, not applied.
> **P1 — shells.** `AppHeader` (plain-type wordmark, role name, phone, **Sign out** — net-new: clears tokens, navigates `/login`), `DesktopNav` for officer/broker/admin (admin gains a "Failed pushes" tab whose count is wired in 6.2 — render without the count until then), phone shells for expert/garage **without a bottom nav** (pass-2 decision 1: the Notifications tab had no screen; one destination needs no bar). `homePathFor` already routes all roles.
> **P1 — components, restyled in place.** Buttons (primary / secondary / destructive-outlined, pending label swaps, disabled), TextField / PhoneField / SearchField / TextArea with visible labels and the error-sentence convention, `StatusChip` with the six declaration labels and the three profile statuses, `AlertBanner` vs `StatusBanner` (role=alert vs role=status, distinct at arm's length), `DetailTable` (desktop two-column / phone label-above-value, em dash for missing, tap-to-dial on insured phone), worklist rows/cards, `DocumentRow` with the `PushIndicator` strings ("Queued, will send" / "Sent to AXA" / "Sent to AXA; the local copy has been removed"), `CapturePanel` (camera-only variant renders **no** file control; per-bucket accessible names — "Take a photo — insured car" — this is the week-6 a11y fix pulled forward), `ClarityConfirm` (image / audio / file), `VoicePanel`, `DiagramPanel` (lighter mark fill, panel names drawn into the export — pass-1 decision 1), `PushPanel` (four states, per-role copy — expert vs garage sentences).
> **P1 — the one defect.** A1's save refusals print the raw response body; map the three API codes to the pass-3 sentences (duplicate phone, duplicate NEXT3 ID, invalid number).
> ——— stop line: P1 complete = a coherent, demo-ready app ———
> **P2 — screen polish from the canvases.** E1/E2 per pass 2 (search above every branch — already true, now styled; stale vs not-found banners), G1–G3 per their five states, O1/O2 three-column review with the approve-sequence labels ("Rendering decision… Uploading… Approving…"), A1 list with the three row treatments and the deactivate confirmation, login as two styled steps with a single OTP field + `autocomplete="one-time-code"` + numeric keypad, S1 invite screens (the invite SMS gains a URL + route — pass-2 decision 2).
> **P3 — if time remains.** Broker display name on the public page via a snapshot column written at link creation (pass-2 decision 3; migration + db-reviewer); exclude `approval_image` from garage-facing media counts (pass-2 decision 4).
> **Rules.** Every string is the shipped/pass-1 string — no new copy invented. Placeholder discipline unchanged. Keep `reusability.test.ts` green: new shared components live in `media/` or a new `ui/` folder that imports nothing role-specific. Run `npm test`, `npm run build` (zero lint warnings), `dotnet test` (A1 mapping and any P3 server change). Record every design-introduced product change in scope-decisions (Sign out, no bottom nav, per-bucket names, OTP field, diagram export).
**DoD:** `npm run build` + `npm test` + `dotnet test` green; **manual Chrome pass at 390 and 1440** comparing each screen to its canvas — expert, garage, officer, admin shells; fonts load offline (DevTools → Network → Offline after first load); `demo-reset.ps1` + `docs/demo-week4.md` still run unchanged; a11y spot-check: every capture control has a distinct accessible name (DevTools accessibility tree).
**Test focus:** no behaviour tests should change — if one does, the slice crossed its line; new tests only for Sign out, the A1 mapping, the OTP field, and (P3) the snapshot column.
**Notes:** **P1 landed 2026-08-22; P2 and P3 are NOT done and the box stays unticked.** 249 web tests (+50) and 501 xUnit (499 passed, 2 skipped — Azurite and the NEXT3 sandbox both absent; nothing server-side changed). **No existing test was edited**, which is what the card asked for and was the main thing at risk. Eleven things worth carrying forward.

(1) **One card item was dropped rather than built, because it contradicted a later decision.** P3's "exclude `approval_image` from garage-facing media counts" (pass-2 decision 4) is answered the other way in `scope-decisions.md` by slice 4.3, and 4.3's argument is the better one — the approval image *is* the declaration's media under the same visa, and filtering it would put a §7.1 rule outside the table §7.1 is encoded in. Recorded as considered-and-not-taken so the next reader of pass 2 does not re-open it. **Two review documents written the same day can disagree; the playbook card inherited one of them.**

(2) **The per-bucket accessible name is an `aria-label`, not a new visible label, and that is what let the suite stay untouched.** The canvas draws "Take a photo" and *quotes* "Take a photo — insured car" as the accessible name — read the artboard carefully and the two are different things. The visible label stays the shipped string (the demo script names it), the input carries the qualifier, and WCAG 2.5.3 wants exactly that containment. The predicted risk was that `getByLabelText` would double-count once both a `<label>` and an `aria-label` matched; it does not — testing-library dedupes the union, so `findAllByLabelText(/^Take a photo/)` still returns 5 with qualifiers live.

(3) **Three of the four test breakages this slice caused were markup shape, not behaviour, and every one was worth understanding rather than patching.** A count in its own `<span>` broke `getByText('Insured car photos (3)')`, because that query reads an element's *direct* text children — reverted to one text node. Making the whole worklist card an `<a>` broke `links.map(l => l.textContent)`, and the test was right: the link's accessible name became the entire card, so a screen reader would read six label/value pairs before naming the claim. It is a stretched-link overlay now — the card is tappable edge to edge and the announced name is the visa number. **A test that goes red on a restyle is sometimes telling you the restyle is wrong.**

(4) **The fourth was a genuine bug I introduced: two components named `DocumentRow` in one module.** `OfficerDeclarationPage` had a local one and imported the new shared one, and the build did not complain — it resolved to the import, so the rows rendered with empty names and no children. Renamed to `ReviewDocument`. **A name collision between a local function and an import is not a compile error in this toolchain**, and the symptom was three tests failing for what looked like a query problem.

(5) **The browser pass found four things, for the eighth slice running, and one of them was invisible to every test.** At 390 px the header measured 443 against 386 of viewport and pushed **Sign out** off the right edge — on the two shells that have no other way out, in a slice whose whole point was adding Sign out. The phone number gives way, keyed on the shell class rather than a media query, because pass 1's distinction is field-versus-office and because a viewport media query is untestable in a harness that pins the viewport.

(6) **The other three, all of them things a component test cannot see.** O2 inherited E4's `max-height: 60vh` on document previews, so the officer got one screen-height photograph per row — right for E4, where a person must look at the photo before it is sent, wrong for a list of every document on the claim. The "Failed pushes" tab was pixel-identical to the four real tabs beside it and did nothing. And the diagram's narrow wing columns ran their labels over the border into the bonnet, which was only visible once real text was in a real SVG at a real size.

(7) **The diagram change is the one that alters what AXA receives, so it was proven end to end rather than asserted.** Panel names drawn into the export, mark lightened from `#cc0000` to `#f7c8c4` so the name of the marked panel survives, one word per line on the 30 px columns. Confirmed live: fifteen names in a 1600×1200 PNG, Front bumper and Front left wing marked and still readable, `passed` / `PLACEHOLDER-DOC-07` / 98,516 bytes, outbox `sent` on attempt 1. Inline attributes and **never `foreignObject`** — a serialized SVG carries no page CSS and a foreignObject taints the canvas outright.

(8) **`media/` importing `ui/` needed a rule, and a ban would have been the wrong one.** Most of `ui/` is presentational and safe for the Option 2 public page; `AppShell`, `AppHeader` and `useSignOut` read the session, the token store and the router, which is precisely what `reusability.test.ts` exists to keep out. So: an **allow-list** of the safe modules, plus a second `describe` asserting `ui/` imports no role module at all — the router is deliberately *not* forbidden there, because `DesktopNav` navigating is the job. Both halves **verified by planting**, one violation each, and both went red.

(9) **The licence check paid off a second time.** `@fontsource/ibm-plex-sans` and `-mono` declare **OFL-1.1**, which permits embedding and puts no obligation on this codebase's own source — checked from the package metadata before installing, because HANDOFF §5's IP conversation is still open and 3.4 is the slice that learned this.

(10) **A pre-existing flake surfaced twice and then stopped.** `GarageDeclarationsPage`'s `findAllByRole` (1 s default) timed out under 26 parallel test files on a loaded machine, in two runs out of five; four consecutive full runs since are green. Not fixed, because fixing it means editing a test and it is a timeout rather than a wrong assertion — but the new `ui/` module graph plausibly added first-import latency to that worker, so it is written down rather than shrugged off.

(11) **`demo-reset.ps1` and `docs/demo-week4.md` still hold.** The reset ran in 22 s, and the whole §5.2 chain was re-driven behind the restyle: garage draft → capture (§7.1's no-file-picker on Car photos, live) → E4 at 1600×1200 sharpness 33707 → Submit → officer inbox → visa search → Approve → approval PNG rendered and uploaded and *then* approved → both rows `sent` with `file_name` intact → both notifications on §8's email fallback. Every button label in the demo script is unchanged.

**Notes (P2), 2026-08-22.** **P2 is complete, so 4.4 is ticked; P3 is one item, not two** — the broker display-name snapshot column (pass-2 decision 3), since the other P3 item was dropped in P1. **279 web tests (+30) and 503 xUnit (+2, 501 passed / 2 environment-skipped).** Again **no existing test was edited**. Nine things worth carrying forward.

(1) **The browser pass found a bug my own testability hack had planted, and it blanked the page.** `ConfirmDialog` rendered `<dialog open>` so that jsdom — which has no `showModal` — would still show it to a test. In a real browser `open` puts the element into the **non-modal** state, `showModal()` then throws `InvalidStateError`, and because that happens inside an effect React unmounts the whole tree: pressing **Deactivate** cleared the screen to bare canvas. **All seven tests passed.** The fallback is now the jsdom branch only (`showModal` if it exists, `open` otherwise, never both), and Chrome takes the modal path — verified with `:modal` and with focus landing on Cancel. **Writing markup to suit the test environment is how you ship a bug the test environment cannot see.**

(2) **The second browser finding was pure layout arithmetic, and no test has a width.** O2's three columns give the left one about a third of 1180 px; the detail table's label column is up to 200 px, so the *value* got 75 px and every field wrapped mid-word — "PLACEHOLDER Insured 88" came out as PLAC/EHO/LDER/Insur/ed/88 and a plate number broke across three lines. A review column is phone-width, so it now gets the phone treatment: label above value, at every width.

(3) **Pass 2's question about the 429 has an answer, and it is yes.** `AuthEndpoints.TooManyRequests` already sets `Retry-After`, so the resend countdown is read rather than guessed — which is what keeps it right across a reload, since the browser has no idea when the code was sent. It is also why S1 and S2 keep raw `fetch`: `ApiError` carries a status and a body and **no headers**. Proven live at 43 s ticking to 40 s, with the throttle rendered as a countdown and not as an error.

(4) **The five-attempt message is the screen counting its own tries, and that is the only honest version available.** §9 answers wrong, expired, used, exhausted and unknown-number with one identical empty 401 so that nothing can be probed. So the screen says what it can defend — this code was not accepted, and separately that a code only gets five — and never which of the five just happened.

(5) **The invite SMS keeps its old sentence and gains a link, and that choice is why no test moved.** `CapturingSmsSender` reads the token with `invite token is ([A-Za-z0-9_-]+)`, which stops at the full stop; `demo-reset.ps1` used the looser `token is (\S+)`, which *would* have swallowed the URL, and is tightened to the same class. **`demo-reset.ps1` seeds through the real onboarding path, so a broken invite SMS fails the reset rather than the demo** — which is exactly what it is for, and it still runs in 18 s.

(6) **`--no-launch-profile` bites twice.** `Auth__AppBaseUrl` in `launchSettings.json` never reached the demo, because `demo-reset.ps1` starts the API with that flag — the same reason `Blob__Mode` and `Push__Mode` are already set as env vars there. Caught by reading the seeded SMS and finding `https://PLACEHOLDER-app.example/invite/…`: correct for a placeholder and useless to anyone who taps it.

(7) **The link's origin is configuration and must never be `Request.Host`.** The header is attacker-controlled and this URL carries a live credential into an SMS in AXA's name. Two server tests pin it — one that the link carries the configured origin, one that it does **not** carry the test server's host — and the second is the one that would go red if somebody "simplified" it later.

(8) **One artboard word was deliberately not shipped.** `G3Waiting` draws *Queued* beside a garage's document, but at that point §5.2 holds it `deferred` with no outbox row, and pass 1's string is "Queued, **will send**" — a promise the app cannot keep, since a rejected declaration never pushes anything. A fourth indicator string would have been invented copy. The panel's heading and the paragraph above it already carry the meaning; recorded rather than quietly rendered.

(9) **The approve sequence needed a test that holds each step open.** The three labels have no observable symptom from outside once the calls have finished, so the test gates render, upload and approve on promises it resolves one at a time and reads the label *while* each runs. **Verified by planting the flat "Working…" — two of the three go red.** One trap inside it: each gate is created inside the step it guards, so a step must be observed before it can be released; releasing blind resolves nothing and the test hangs on the label it just left.

**Week-4 milestone checklist (client demo → 30% payment):**
- [x] 4.4 presentable pass landed (P1 **and** P2; only P3's broker snapshot column remains) — the demo shows a product, not a prototype
- [x] Expert chain live on real push: popup → Arrived → capture (incl. voice, diagram, report) → clarity refusal → outbox `sent`
- [x] NEXT3 outage beat: `Fake:FailureRate=1.0` live → uploads still land → restore → queue drains; retry schedule explained; A2 (6.2) named as the human safety net
- [x] Declaration chain live: garage create → upload → submit → officer popup → visa search → approve → PNG + documents in the fake's Survey folder → garage popup → G3 unlock; reject path shows status only
- [x] Nothing invented on screen; placeholders explained; no real client values anywhere
- [x] `demo-reset.ps1` proven twice; `docs/demo-week4.md` followed without improvisation
- [ ] Scope letter + `docs/next3-openapi.yaml` sent **before** the demo; the Friday status states the sandbox gate and the day-for-day clock
- [ ] 30% milestone invoked per the guardrails: deployed and demoed, not AXA's rollout

**Gate check, 2026-08-24 (PO session):** the demo ran with the letter unsent — recorded as a **gate breach**, not left ambiguous. Evidence: all three client documents (`docs/scope-letter.md`, `docs/status-2026-08-22.md`, `docs/client-email-2026-08-18.md`) still carry *DRAFT — not sent*; Gmail could not be searched (connector lacks read scope); the developer did not assert a send. Client exposure preceded the written scope, so the demo formed AXA's expectations with nothing agreed — sending the package is now the top priority, ahead of slice 5.1. Cover email drafted at `docs/client-email-2026-08-24.md` (attaches the letter + the week-4 status + `docs/next3-openapi.yaml`; folds in #46, #47, #28 per the po-handoff rule). The 30% box stays open: the developer holds the invoice pending demo follow-up (their answer, 2026-08-24), and there is no demo debrief from AXA yet — both boxes tick only on the developer's confirmation.

## Week 5 — Repairs, Broker Option 1, the public form

*Expanded 2026-08-23 from the week-4 + 6.3a Notes and the three design canvases (`docs/design/`, reviews in `docs/design-prompts/`). The canvases are the screen specs: G3 Repairs / G4 for 5.1, B1–B3 + BrokerStates for 5.2, P1 ×4 + B4 for 5.3. Decisions at expansion:*
- *Repair uploads push **immediately** (`PushTiming.Immediate`) under the declaration's visa — §5.2's G4 row says so, and at `repairs_in_progress` the visa is guaranteed by `CK_declaration_decision`. Submit-repair-docs requires **at least one repair document of any kind** — not specifically an invoice (pass-2 decision 5; invoice-required is an invented rule).*
- *Broker and public media carry `PushTiming.Never` and a **null doc type** — `ResolveDocType` becomes timing-aware instead of inventing NEXT3 codes for documents NEXT3 never sees.*
- *The Option 1 email carries the six fields in the body **and the documents as attachments** read through `IBlobStore.Open` — "an email with all info" is the BRD's terminal act; `IEmailSender.Send` gains an attachments parameter, the fake logs their names.*
- *The public API stays at `/public/{token}`; **`/p/{token}` is the web route** in front of it (scope row from 1.5). The broker's display name reaches the public page through a **snapshot column written at link creation** (arch rule 2 forbids the public module from reading `Users`). The broker's "ready to send" notification is issued from `Api.Modules.Broker`, which the public module may reference.*
- *Public uploads are **one file per request** through the existing streamed service, so the 1.5 body cap stays at one file's worth and `PublicUploadCaps` counts against the documents already stored. The five mandatory car shots are **6.1**; 5.3 enforces at least one supporting document.*
- *B3 (create link) is a broker desk screen and lands in **5.2**; B4 (review + Send Email) needs customer submissions and lands in **5.3**. The lazy `expired` state is computed in B1's projection (token expiry), never written.*

### ☑ 5.1 Repair flow — G4 (server + web)
**Goal:** the last two rows of §5.2's table executable: repair photos (capture-only) + discharge/invoice (upload allowed) pushed to NEXT3's *Survey* folder immediately, then the terminal `repair_docs_submitted` transition.
**Refs:** design.md §5.2 (start repairs / submit repair docs rows; "no officer notification — recorded gap"), §7.1 garage rows, §4 (`document.push_status`), `docs/design/pass-2` G3Repairs + G4Submitted artboards, `pass-2-review.md` decision 5.
**Prompt:**
> Slice 5.1 — the garage repair flow per design.md §5.2, G4.
> **Buckets (one migration, db-reviewer mandatory).** Three `MediaBuckets` rows, `OwnerKind Declaration`, `Next3Folders.Survey`, **`PushTiming.Immediate`**: `repair_photo` (Image, capture-only, `DocTypeKey RepairPhoto`), `discharge` (Document, upload allowed, `Discharge`), `invoice` (Document, upload allowed, `Invoice`) — the three `Next3:DocTypes` keys already exist unused. Update `MediaBucketTests` deliberately: the `[InlineData]` matrix, `EveryDeclarationBucketLandsInTheSurveyFolderAndWaitsForApproval` (three buckets wait for approval, three do not — rename it to say what is now true), `EveryBucketIsAccountedForByOwnerKind`.
> **Upload gating by state.** `POST /api/garage/declarations/{id}/documents` today 409s `declaration_already_decided` after any decision. Split it: `draft` → `allowedBuckets [GarageDocuments, GarageCarPhoto]`, `VisaNo: null`; `repairs_in_progress` → `allowedBuckets [RepairPhoto, Discharge, Invoice]`, **`VisaNo: declaration.VisaNo`** (`RequireVisa` then passes and the outbox row is written in the same `SaveChanges` — the §4 one-transaction rule holds for free); every other state → 409 `declaration_already_decided` for declaration buckets or 409 `repairs_not_in_progress` for repair buckets. Repair uploads reach NEXT3 on the next worker tick without waiting for the submit.
> **Transition.** `DeclarationService.SubmitRepairDocs(id, garageUserId, ct)` through the existing `Transition` helper (the entity's `SubmitRepairDocs` and the transition-table row already exist); refuse with 409 `repair_documents_required` when no document in the three repair buckets exists; audit `declaration_repair_docs_submitted`; **no officer notification** (§5.2 records the BRD gap — do not add one quietly). `POST /{id}/submit-repair-docs` → `{ state }`. `DeclarationDetailDto` gains `RepairDocsSubmittedAt`. Add `garage.UploadRepairPhoto/UploadDischarge/UploadInvoice` and `garage.SubmitRepairDocs` to `DeclarationFlows`.
> **Web.** In `GarageDeclarationPage.tsx`'s `repairs_in_progress` panel, replace the `StatusBanner` placeholder with three `CapturePanel`s (`repair_photo` qualifier "the repaired car", `discharge`, `invoice`) on the declaration path and a **Submit repair documents** button (`useDeclarationAction(id, 'submit-repair-docs')`, latch, disabled until at least one repair document exists — as drawn on G3Repairs, minus the invoice-only rule). The `repair_docs_submitted` panel shows the four timestamps per G4Submitted ("This declaration is complete… Nothing further is needed from the garage"). `garage/api.ts` gains `submitRepairDocs`; `PushIndicator` shows "Queued, will send" / "Sent to AXA" on repair rows because they carry outbox rows.
> **Tests.** Transition + audit; `ConcurrentSubmitRepairDocs_ProduceExactlyOneTransition` on the `ConcurrentStartRepairs` template (the `state` concurrency token — verify by removing it); bucket gating per state (a repair photo on a draft → 409; a survey document on repairs_in_progress → 409); a repair upload produces an outbox row under the declaration's visa and reaches `sent` via `RunOnce` into *Survey*; submit without repair documents → 409; the G4 terminal state has no way out. Web: the repairs panel renders three panels and the latch; terminal panel renders the timestamps.
**DoD:** `dotnet test` + `npm test` green; migration db-reviewed; **manual Chrome pass**: approve a declaration → Start repairs → capture a repair photo + upload an invoice → outbox rows `sent` under the visa before pressing anything else → Submit repair documents → terminal screen; the officer sees the repair documents on O2.
**Test focus:** the visa flows from the declaration row into `MediaUploadTarget` for repair buckets only; state-gated bucket sets; the terminal transition's once-only guard.
**Notes:** done 2026-08-24 (521 xUnit: 519 pass, +18, 2 environment-skipped; web 279 → 283). One migration, db-reviewed.

1. **The card's "draft →" would have shipped a lying error code.** Taken literally, a `submitted` declaration's upload answers `declaration_already_decided` — of a declaration nobody has decided — and a garage loses the ability to add a forgotten photograph while an officer reviews. The gate tests `DecidedAt`, which is today's behaviour unchanged, and an integration test pins it so a later tidy-up to draft-only goes red. Confirmed with the developer before implementing.
2. **The refusal depends on the bucket, and the bucket arrives inside the streamed body.** So `allowedBuckets` became a `BucketGate` callback: the endpoint that owns the policy answers "may this bucket, here, now?" and the pipeline consults it at the same point, before the file is read. Returning the attempted bucket to the endpoint and re-deciding there was the alternative and puts the same rule in two places — 4.1's note (8) about `Next3PushKind`, from the other direction.
3. **The db-reviewer's HIGH finding was real, its mechanism was wrong, and checking cost an hour.** It said the upload gate is a read-then-write that the `state` token would close. The token *does* close the wide half — the seconds a file spends streaming between the gate and the commit — and the guard is in, on both upload routes (CLAUDE.md's "check the siblings"). But the interleaving it described survives it: approve reads the deferred set, the upload commits legitimately (the token's *value* has not changed), then approve commits. Proven by writing the test and watching it stay green with the guard removed. What actually shrank that window was moving approve's `Deferred` read to **after** the NEXT3 verification — it used to span a call to a legacy core with a 30-second timeout. **Read the reviewer's mechanism, not just its verdict.**
4. **Neither window is testable through `WebApplicationFactory`, and saying so beat writing a test that passes for the wrong reason** (4.1's note 6, second outing). TestServer buffers the whole request body before invoking the endpoint, so an upload cannot be held open mid-request; the fake's `LatencyMs` runs on the frozen `FakeTimeProvider` and never elapses, so approve cannot be held either. A first attempt asserted a 409 that both interleavings produce — it passed with the guard deleted. Deleted rather than kept. The permanent fix is a **re-queue sweep** (any `deferred` document on a declaration that already has a visa is stranded by definition): carried as a ticket, with 7.2's retention gap.
5. **`EveryDeclarationBucketLandsInTheSurveyFolderAndWaitsForApproval` was renamed, not weakened.** Half of what it asserted stopped being true — G4's buckets are `Immediate` — and the folder half is now unconditional across all six. The timing split is pinned against `MediaBuckets.Repair` and `GarageDeclaration` rather than against a count, so a seventh declaration bucket has to be classified rather than inherit whichever timing its author typed. `EveryBucketIsAccountedForByOwnerKind` needed no change and that was checked rather than assumed.
6. **Two more the reviewer found in the adjacent code, both taken:** the officer's upload target hard-coded `VisaNo: null` while its new sibling read the row, which is a trap for the first officer-writable bucket that pushes immediately; and §7.1's table had gained no rows for the three buckets, while `MediaBuckets` claims that table is the source of truth. **One recorded and not taken:** `CK_declaration_decision` guarantees `visa_no` is non-*null*, and `RequireVisa` needs non-*empty* — unreachable through the entity (`Approve` calls `ThrowIfNullOrWhiteSpace`), and tightening a constraint 4.1 built with three biconditionals and seven raw-INSERT tests deserves its own slice.
7. **The manual pass found the real bug, seventh slice running: "Sent to AXA" was a string nothing could produce.** The card promised the repair rows would show it, and they showed "Queued, will send" for ever instead — `PushIndicator` tests `pushStatus === 'sent'`, and §4 deliberately never writes that value, because live push state belongs to the outbox row. The row went straight from "Queued, will send" to "the local copy has been removed" a week later. Fixed by computing `pushConfirmed` at read time through `OutboxSentQuery` — the join §7.3 already uses, so architecture rule 4 is untouched and there is still one copy of the fact. Every document list gained it, the expert's included: an expert on a bad connection is asking the same question.
8. **`repairs_not_in_progress` and `declaration_changed` went into `UPLOAD_EXPLANATIONS` in the same commit as the server** — 4.2's note (7) was the same omission a slice earlier, where 4.1's two new codes rendered as a bare "(400)"/"(409)" for a week.


### ☑ 5.2 Broker Option 1 — B1, B2, B3 (server + web)
**Goal:** a broker files a quotation request with documents and the app emails it, with attachments, to the AXA recipient for that insurance type — the broker module's terminal act, with no NEXT3 anywhere in it.
**Refs:** design.md §5.3 (Option 1 row, states), §8 (broker email row), §7.1 (Broker Option 1 row: upload **or** capture + kill-switch), §7.3 (`Retention.BrokerBlobDays`), Appendix A `Broker.*`; `docs/design/pass-3` B1List, B2New, B3Link, BrokerStates + `pass-3-review.md` decisions 4 and the "Not built" notes.
**Prompt:**
> Slice 5.2 — Broker Option 1 per design.md §5.3, and the broker's three desk screens.
> **Config.** A `BrokerOptions` class bound to `Broker` (today the section is read by nobody): `AllowUpload`, `InsuranceTypes`, `EmailRouting`. Validate **always** (`IValidateOptions` + `ValidateOnStart`, the 3.3 house pattern): the type list non-empty, every type has a routing entry, every recipient is a syntactically valid address — placeholders pass, a misconfiguration fails the boot rather than the first broker. A `MutableOptionsMonitor<BrokerOptions>` on `ApiFixture` so tests flip the kill-switch.
> **Media.** One bucket, `broker_document` (`OwnerKind BrokerRequest`, Document kind — jpeg/png/pdf, `AllowUpload true`, **`PushTiming.Never`**, **no doc type**): make `ResolveDocType` timing-aware so a `Never` bucket stores `doc_type = null` (documented on `Document`) instead of demanding a NEXT3 code for a file NEXT3 never sees. Migration (`CK_document_bucket`), db-reviewer mandatory; flip `EveryBucketsTimingIsOneThePipelineHandles` deliberately. **The kill-switch is dynamic, the bucket rule is static**: the broker upload endpoint refuses `origin=uploaded` with `upload_not_allowed_for_bucket` when `Broker:AllowUpload` is false, and `GET /api/config/media` projects the *effective* `allowUpload` for the broker bucket so the file control disappears rather than greys (the B2 artboard's two variants). Retention: implement the §7.3 broker branch — a third cleanup sweep deleting `broker_request` documents' blobs `Retention:BrokerBlobDays` after the request reached `sent` or `expired`; `RetentionOptions` says it is unenforced today.
> **Endpoints** under `/api/broker` (`AuthPolicies.Broker`, ownership on every read/write — 404 for not-mine): `GET /requests` (the B1 list, newest first — add `created_at` to the `(broker_user_id, state)` index; project **`expired`** for Option 2 rows whose token `expires_at` has passed without a lock — computed, never written), `POST /requests` (Option 1 draft with the six fields; insurance type must be in `BrokerOptions.InsuranceTypes` → 400 `unknown_insurance_type`; premium and car value > 0; state `draft`), `GET /requests/{id}`, `POST` + `GET /requests/{id}/documents` (the media pipeline, `allowedBuckets [BrokerDocument]`, `VisaNo null`, actor = broker), `POST /requests/{id}/submit` → resolve the recipient from `EmailRouting`, send via `IEmailSender` with the six fields in the body **and every document attached** (extend `IEmailSender.Send` with `IReadOnlyList<EmailAttachment>` — name, content type, bytes streamed from `IBlobStore.Open`; `FakeEmailSender` logs attachment names and sizes, never bytes), then state → `submitted`, `submitted_at`, `emailed_at`, `email_recipient`, audit, new `NotificationTemplates.BrokerRequestSubmitted`. **Once-only:** add `IsConcurrencyToken()` on `BrokerRequest.State` (4.1's pattern; regenerate rather than stack the migration while uncommitted — 2.2's snapshot lesson) and answer a lost race with 409 `illegal_transition`. **Ordering:** commit the state first, then send; on send failure leave `emailed_at` null with a `failed` notification row so B1 shows "submitted, email not yet sent" rather than lying, and let B1 offer a **Resend** that only sends (no state change). Record this ordering in scope-decisions.
> **B3 moves here.** `POST /api/broker/link-requests` already exists; give it a screen: customer mobile (optional — pass-3 decision 4) + optional insurance type, the link shown **once** with Copy (the issued `Url` is `/public/{token}`; render it as `{Auth:AppBaseUrl}/p/{token}` — the 5.3 web route), "Send by SMS" disabled with the delivery-channel explanation, and the "Afterwards" state (link issued, expires, Create a new link). Store the **broker display name** on the request at issue time (`broker_display_name`, a snapshot — 5.3 reads it; write it in this slice so the migration is one).
> **Web.** `BrokerLayout` (desktop, no PushPanel) + `homePathFor` `broker → /broker` + remove `/broker` from `UNBUILT_TABS`; `src/broker/` mirroring `garage/` (`brokerKeys {lists, list, detail, documents}`, DTOs, hooks, latches). Three new field components in `ui/fields.tsx` — `SelectField`, `DateField`, `MoneyField` (no currency symbol: none is specified — show the amount, raise #47 currency) — and a `broker/` ban added to the ui block of `reusability.test.ts`. **B1** worklist with the seven `StatusChip` labels in `tones.ts` (Draft, Submitted, Link issued, Customer in progress, Ready to send, Sent, Expired; green only on Ready to send) and one action per row (Continue / Review / Reissue). **B2** the six-field form, `CapturePanel` on `broker_document` with the provenance tag shown on each row ("captured" / "uploaded" from `origin`), Submit disabled until the six fields are filled; the Option 1 outcome state names the recipient address ("Sent to AXA — Recipient PLACEHOLDER-recipient-1@example.invalid"). **B3** as above.
> **Tests.** Options validation (boot fails on a type with no route); kill-switch flips `allowUpload` in `/api/config/media` and refuses an `uploaded` origin; submit resolves the recipient and the fake email logs the attachments; unknown type 400; four parallel submits → one email, one transition (verify by removing the token); ownership isolation between two brokers; retention sweep deletes a `sent` request's blobs after `BrokerBlobDays` and leaves a `draft`'s. Web: the seven chips, the form's disabled/enabled rule, the upload control absent when the switch is off, the link shown once.
**DoD:** `dotnet test` + `npm test` green; two migrations folded into one and db-reviewed; **manual Chrome pass**: broker signs in → B2 fills six fields, captures one document, uploads one → Submit → the fake email in the API log shows the fields, the routed recipient, two attachments by name → B1 shows *Submitted* with the recipient; B3 issues a link and shows it once; the kill-switch off removes the file control live (reloadOnChange).
**Test focus:** the email's attachments come from blobs (not the upload stream), and a send failure leaves state `submitted` with `emailed_at` null — visible, not silent; `Never` timing + null doc type through the whole pipeline; no broker code path references `INext3Client` (arch rule 3 is untouched, but say so).
**Notes:** done 2026-08-24 (564 xUnit: 562 pass, +39, 2 environment-skipped; web 284 → 310). One migration, db-reviewed.

1. **The db-reviewer found the 5.1 lesson had not been carried across, one slice after 5.1 recorded it.** The broker upload read its request `AsNoTracking` and gated on an `if` — exactly the read-then-write `GarageDeclarationEndpoints` had closed the day before with a tracked read, `IsModified` on `state` and a `DbUpdateConcurrencyException` catch. The consequence here is worse than the declaration's: a document that lands after the submit is in no email, refused by Resend (`already_emailed`), and then **deleted by retention** on the send's clock — bytes the broker believes AXA holds, destroyed silently. CLAUDE.md's "a lesson learned in one adapter is not learned until it is checked in the others", proven again by not doing it.
2. **The concurrency token cannot guard a Resend, and the comment claiming it did was the giveaway.** A Resend changes no state, so both racers carry the same token value and both UPDATEs match; two presses meant two emails at AXA's desk for one request. It is claimed on `emailed_at` now, in the `WHERE` of a conditional update issued *before* the sender is called, with a compensating release when the send fails. Both guards were verified by removal — the token, and the claim — and each turns exactly one test red.
3. **`Declaration`'s justification for the token idiom was copied and is not true here.** 4.1 could say "there is no innocent write to this row"; `broker_request` has two — `MarkEmailed`, and the public page's field write — which is precisely 2.4's objection to putting a token on a column not every write changes. Each is handled where it happens rather than by weakening the token, and the entity now says so instead of repeating 4.1's sentence. The public **first-open** was the live consequence: two simultaneous opens of one link would have thrown an unhandled `DbUpdateConcurrencyException` on §9.1's uniform-404 surface. It is swallowed there, because the row already says what the losing write was trying to say.
4. **The card's index would not have served the query it was added for.** `(broker_user_id, state, created_at)` puts `state` between the seek column and the sort, and B1 has no state predicate anywhere in the module — so SQL Server sorts the whole partition regardless. It is `(broker_user_id, created_at)`, and the migration was regenerated rather than corrected by a second one (2.2's snapshot lesson).
5. **The card's retention rule was unimplementable as written, and the fix is a better rule.** "After the request reached `sent` or `expired`" names two states this slice cannot test: Option 1's terminal state is `submitted`, and `expired` is computed in B1's projection and never written. The sweep keys on **`emailed_at`** — the moment the documents left as attachments — which covers both options in one predicate and gets the failure case right for free, because a failed send leaves it null and Resend needs those bytes. Abandoned Option 2 requests are recorded as out of scope (#4/#22) rather than swept quietly.
6. **`BrokerOptionsValidator` is the first validator registered unconditionally**, and that is the difference between these placeholders and the `Next3`/`Push` ones: three well-formed types, three routes, three `@example.invalid` addresses all pass, so an always-on check costs nothing today and turns a mistyped insurance type into a container that will not start. Two existing boot harnesses had to be given a `Broker` section — the validator working, not the validator being wrong.
7. **Two `MediaBucketTests` flipped, deliberately, and one of them is the guard doing its job.** `EveryBucketsTimingIsOneThePipelineHandles` asserted `PushTiming.Never` had *no* bucket; `broker_document` now carries it, so the negative was simply false. Replaced by the positive form — the `Never` set equals `MediaBuckets.BrokerRequest`, and `Timing == Never` ⇔ null doc type ⇔ null folder, which is the biconditional `ResolveDocType` now relies on. And `EveryBucketIsAccountedForByOwnerKind` failed as written because `broker_request` is a third owner kind: 5.1 checked it and correctly left it alone; 5.2 had to widen it, and it arrived with a broker-scoped test of its own.
8. **B2 is two screens because a document needs a request to attach to.** The card's endpoint set has no `PATCH`, so the details go in once and a mistake means a new request — pass 3's B4 rule applied to the broker's own form. The artboard is not violated by this: its two Documents cards are the kill-switch's two variants drawn side by side, not two panels on one route.
9. **One thing the card did not name and B2 could not work without: the insurance types have to reach the browser.** They are `Broker:InsuranceTypes` (#14), so a list in TypeScript would be a placeholder violation *and* a second source of truth from the one the server validates against. `GET /api/broker/config` serves them, behind the broker policy — `/api/config/media`'s reasoning, minus the anonymity 5.3's public page will need.

10. **One flake seen once, written down rather than shrugged off.** `PublicLinkFlowTests.ConcurrentSubmits_OnlyOneIsAccepted` failed on a single full run and then passed **17 consecutive times** (5 full suites, 12 runs of that class alone). It is in the same serialized `[Collection("api")]`, so this slice's `Fake:FailureRate = 1.0` windows cannot reach it — that explanation is ruled out rather than assumed. The mechanism I can point at and have not proven: the public submit now commits **two** concurrency-checked rows in one transaction (`public_link_token`'s rowversion and, new in 5.2, `broker_request.state`) under four concurrent submissions, and a database-level deadlock would surface as an unhandled `DbUpdateException` → 500, which is what would break its `Assert.All(NotFound)`. Not fixed, because the fix candidates are either swallowing a genuine `DbUpdateException` as a 404 — which would hide a real fault on §9.1's surface — or editing a test that is asserting the right thing. Carried as a ticket for 7.1's public-surface security pass, which is where that endpoint gets its load work.
11. **The manual Chrome pass ran on 2026-08-24, after this card was otherwise finished, and it is the first browser exposure any broker screen has had.** Both DoD lines walk end to end — the routed email with both attachments by name, two insurance types reaching two different desks, the failure path leaving the request filed with `emailed_at` null and Resend delivering it, and **the kill-switch removing the file input from the DOM live**, no restart. It also found **six defects in this slice**, none fixed (recorded in `docs/browser-pass-week5.md` by the developer's instruction, so the diff under review stayed put). The two that matter: **B2 renders its attached-document list *below* the Submit button** — a broker sends before seeing what is attached, and two adjacent panels are both headed "Documents"; and **an Option 2 request nobody has touched is described as "filed" and offered a Send email button**, because `OutcomePanel` branches only on `emailedAt`. The second is half of B4 and belongs in 5.3.


### ☑ 5.3 Option 2 public form — P1 + B4 (server + web)
**Goal:** the customer completes the six fields and uploads supporting documents from a link, with no account; the broker is told, reviews, and sends the email. The five car-side captures are 6.1.
**Refs:** design.md §5.3 (Option 2 rows), §9.1 (what the page exposes; abuse controls), §7.1 public rows (supporting documents: upload + capture), §4 (`broker_request`, `public_link_token`), §3 (public-surface boundary), `docs/design/pass-2` P1Intro/P1Capture/P1Success/P1Expired + `docs/design/pass-3` B4Review/BrokerStates; `pass-2-review.md` decision 3; scope-decisions rows from 1.5 (one-file ceiling, no broker identity, lazy expiry).
**Prompt:**
> Slice 5.3 — Option 2's public form per design.md §5.3 and §9.1, plus the broker's review.
> **What the page may learn.** `GET /public/{token}` gains `brokerDisplayName` from the **snapshot column 5.2 wrote at link creation** — never from `Users` (arch rule 2) — and nothing else: amend `PublicSurface_NeverRevealsTheBrokerOrTheCustomerMobile` **deliberately** to "reveals the broker's display name and never the customer mobile", and say in the diff why (an anonymous page asking for identity documents looks like phishing). Existing issued links have a null snapshot; the page copes (no name shown).
> **Public uploads.** `POST /public/{token}/documents` — **one file per request** through `MediaUploadService` with `MediaUploadTarget(BrokerRequest, request.Id, VisaNo null, ActorUserId null)`, `allowedBuckets [PublicDocument]`: a new bucket `public_document` (`OwnerKind BrokerRequest`, Document kind, upload **and** capture, `PushTiming.Never`, null doc type — same shape as 5.2's broker bucket; migration + db-reviewer). Before storing, `PublicUploadCaps.Validate` against the request's existing document count + 1 and the file size → 400 `too_many_files` / 413 `file_too_large` — its first production caller. The 1.5 body-size ceiling stays at one file's worth because each request carries one file: record that the 1.5 caveat is resolved by design, not raised. `GET /public/{token}/documents` returns names/buckets/sizes only. The global limiter and the `UseWhen` middleware cover both routes for free — add a test that proves the limiter partitions them.
> **Submit.** `POST /public/{token}/submit` now also requires the insurance type to be in `BrokerOptions.InsuranceTypes` (400 `unknown_insurance_type`) and **at least one `public_document`** (400 `documents_required`); the five car shots are 6.1 and are not enforced yet — record it. An incomplete submit still does not lock the token (1.5). On success — in the same transaction as the lock — state `ready_to_send`; then notify the broker **from `Api.Modules.Broker`** (a `BrokerRequestNotifier` the public endpoint calls; it may read `BrokerProfile.Email`): `IPushSender` with `PushMessage(…, "/broker/{id}")`, email fallback, template `broker_request_ready`.
> **Web — the public page.** Route **`/p/:token`** in `App.tsx` with no shell, no token guard, no `PushPanel`; `vite.config.ts` proxies `/public` to the API (today it does not — the page would 404 in dev). `src/public/`: its own plain-`fetch` client (**never `api()`/`apiBlob`** — they attach a bearer and hard-navigate to `/login` on 401); `useCapture`/`CapturePanel` gain an `auth: 'bearer' | 'none'` option on `UploadRequest` so the shared component uploads without a token (the anonymous `/api/config/media` already serves the bucket rows). **P1Intro**: the broker's name ("PLACEHOLDER Broker One asked you to complete this"), the six fields using 5.2's `SelectField`/`DateField`/`MoneyField`, Continue. **P1Capture (documents only this slice)**: supporting documents panel (upload + capture), the car-photo section rendered as a "photographs come next" placeholder naming 6.1 — not drawn as working. **Send to AXA** disabled until six fields + one document. **P1Success** (counts, "this link has now closed"). **P1Expired** for every non-valid token — one screen, uniform. Mobile-first at 390; no `viewport-fit=cover` (6.3a found it puts the header under the status bar).
> **Web — B4.** `src/broker/` review page per B4Review: the customer's six fields read-only, the documents with provenance tags, the car-photo grid rendered as "not collected yet" until 6.1, **Send email** → `POST /api/broker/requests/{id}/send` (reuse 5.2's sender with attachments; state `ready_to_send → sent`, `emailed_at`, recipient; once-only via the `State` token) → the sent state naming the recipient; the "nothing to review yet" state for `customer_in_progress`. B1's Review action links here.
> **Tests.** Public: uniform 404 still byte-identical across invalid/expired/locked for the new routes; upload caps; submit requires a document and a known type; concurrent submits (existing test) still one; the broker is notified from the Broker module (notification row + `/broker/{id}` url); the snapshot name appears, the customer mobile never does; rate limiter partitions the upload route. Broker: send once-only under four parallel presses; attachments include the public documents. Web: the public page never reads `localStorage` tokens (assert `getTokens` is never imported — add `api/tokens` to a `public/` ban in `reusability.test.ts`'s style); the `auth: 'none'` upload path sends no `Authorization` header; the expired page shows nothing identifying.
**Before you start — what the week-5 browser pass left you** (`docs/browser-pass-week5.md`, findings unfixed by instruction):
- **Finding 2 is half of B4 and is this card's to fix.** An Option 2 request in `link_issued` currently falls into `OutcomePanel`'s not-yet-sent branch, so a request nobody has touched reads *"This request is filed, but the email has not gone yet"* over a **Send email** button — which the server then refuses with `409 not_submitted`, leaving two contradictory sentences on one card. B4's "nothing to review yet" state and the `expired` branch are the fix.
- **Finding 9 is this card's route.** `/p/:token` does not exist and `vite.config.ts` does not proxy `/public`, so the link B3 hands a customer today paints a **blank page** — there is no catch-all route either. Both are named in the prompt above; the blankness is why it matters.
- **Already in place from 5.2:** `broker_request.broker_display_name` is written at link-issue time (so the public page can name the broker without `Api.Modules.PublicSurface` touching `Users`), `BrokerOptions.InsuranceTypes` is bound and validated, and `BrokerRequestService.Resend` is guarded to Option 1 `submitted` only — so B4's send needs its own path, not Resend.
- **Do not re-report:** the `/p/{token}` blank page, and "Notifications are on" with no server subscription (`demo-fix-list.md` #16, widened).

**DoD:** `dotnet test` + `npm test` green; migration db-reviewed; **manual Chrome pass on a phone-width window + your iPhone over the 6.3a HTTPS setup**: broker creates a link (B3) → open `/p/{token}` on the phone → broker's name shown → six fields → capture one document, upload one → Send → success page → the link now shows the closed page → broker gets the notification → B4 shows the submission → Send email → fake email with attachments → B1 shows *Sent*.
**Test focus:** arch rule 2 stays green with the notifier living in `Api.Modules.Broker`; the public page's fetch layer carries no credentials; one-file-per-request is the reason the body cap holds.
**Notes:** done 2026-08-24 (588 xUnit: +25, 1 environment-skipped with Azurite up; web 310 → 337). One migration, db-reviewed.

1. **The 5.2 "flake" was a real 500 on §9.1's uniform-404 surface, and finding its mechanism was this slice's best work.** 5.2's note 10 recorded `ConcurrentSubmits_OnlyOneIsAccepted` failing once in eighteen runs and guessed at a database deadlock surfacing as `DbUpdateException`. It reproduced **five times in ten** here, and the message was not a deadlock: `InvalidOperationException: A broker request in state 'ready_to_send' cannot become ready to send`. `PublicLinkTokenService.Resolve` read the *token* and then the *request* in two statements, so a concurrent submission committing between them left the loser holding an unlocked token and an already-submitted request — `Lock` then called a transition the entity forbids, unhandled, answered 500. That is a new way to tell one token's state from another's, on the one surface that owes every caller the same answer. It is one joined statement now: a single snapshot cannot observe "unlocked" and "already submitted" together, and anything committing after it is still caught by the concurrency token on the way out. **A torn read across two statements is invisible in either one** — 3.3's "read the producer against the consumer", in a new place. Twelve consecutive green runs of the class where it had been failing half the time.
2. **The browser pass found the slice's real bug, eighth slice running, and no test could have seen it.** P1Success counted the documents query — and the submit had just locked the token, so the refetch was the uniform 404, the list was empty, and a customer who had watched two files upload was told their broker had **0 documents**. On the one screen whose entire job is to say what left, to somebody with no account, no receipt and no way to check. Nothing threw. The count is captured at the moment of sending now, and the documents query stops once the link is spent. **The first test written for it passed against the bug**, which is the part worth keeping: the fetch stub kept serving documents after the submit, so the count could not be wrong in jsdom. The stub models the token dying now, and the plant goes red. *A test whose fake is kinder than the server cannot see the bug the server causes.*
3. **The browser pass measured a second one: 36 px controls on the most phone-only screen in the product.** `--control: 48px` comes from `app-shell--touch`, and `shellFor` assigns that class **by role**. The public page has no role — that is its defining property — so it silently inherited the desk size for a member of the public standing on the pavement with their own handset. One class; all seven controls measured 48 px afterwards, header `scrollWidth` 390 in a 390 px shell, nothing wider than the shell. Pinned by asserting the class rather than a computed height, because jsdom applies no stylesheet and the pixels are the browser pass's job.
4. **The card's B4 could not name the recipient it was told to name.** `broker_request.email_recipient` is written *by* the send, so on a `ready_to_send` request it is null — and the B4 artboard's whole argument for showing the address is that it is visible **before** the press, "if the routing table is wrong, this is the screen where somebody notices". #13's routing table therefore joins the type list on `/api/broker/config`. Proven live on the second desk: `MOTOR TOTAL LOSS` → `PLACEHOLDER-recipient-2`, shown before the press and again after it.
5. **A failed B4 send had nowhere to go, and the card's own once-only rule is what created the hole.** State commits before the send (§5.3's ordering), so a mail server being down leaves `sent` with `emailed_at` null — and `Resend` was guarded to Option 1 `submitted` only, deliberately, by 5.2. Confirmed with the developer before implementing: `Resend` is widened to Option 2 `sent`, both arms only while `emailed_at` is null. 5.2's argument survives intact because it exists to stop a submission being mailed *past* B4's review, and `sent` means the review happened. The alternative was telling a broker to ask a customer whose link is locked and who has gone home to start again.
6. **`public_document` shares `broker_request` as its owner kind rather than taking a fourth**, and that is what makes the customer's files reach both B4's list and the email with no new code: `BrokerRequestEmail.Attachments` selects on the owner alone. The cost is that the §7.1 rules alone would let the public page file a document as the broker's own — `origin` is a claim the client makes, so capture-only would not stop it either — and the `BucketGate` is the only thing between them. **Verified by removing it**: the forged `broker_document` is `201 Created`.
7. **`PublicUploadCaps` got its first caller after four slices as a tested pure guard, and only half of it is load-bearing.** `MaxFiles` is a cap *per submission* while each request carries one file, so only a count of the rows already stored can enforce it. The size branch tests the same predicate `PublicBodySizeMiddleware` already rejected on the way in — said plainly in the code rather than left to read as two independent checks.
8. **The 1.5 body-ceiling caveat was resolved by design rather than raised.** `scope-decisions` predicted the `/public/*` request cap would have to rise to ~`MaxFiles × MaxFileMb` here. One file per request means it does not, and the smaller request is also the cheaper failure: a customer on a bad connection loses one photograph rather than fifteen.
9. **The db-reviewer's HIGH findings were all about code that did not exist yet** — it ran against the tree before the endpoints landed, and reported the notifier as unreachable and the bucket as unwritable, which the same commit fixed. What survived was worth having: `public_document` blobs fall outside all three retention sweeps for an **abandoned** Option 2 request, and 5.2's comment saying that case "carries no documents at all today" became false in this commit. The sweep itself needs no change — it keys on the owner kind, so those rows join its set the moment `emailed_at` lands — what is missing is a rule for the requests where it never will. Corrected in `BrokerMediaCleanupTask` and design.md §7.3 rather than left as an absence, and carried to 7.2 beside the rejected-declaration gap it now matches in weight. **Check the reviewer's findings against the tree it actually read.**
10. **`PublicSurface_NeverRevealsTheBrokerOrTheCustomerMobile` was renamed and amended, not weakened.** It asserted the string "broker" appeared nowhere in the response, which the *property name* alone now trips. The answer genuinely changed — pass-2 review decision 3, and §9.1's original intent — and the half that mattered did not: the customer's own mobile is still absent. Two tests were added beside it for the null snapshot and the type list.
11. **Every test that submits now attaches a document first**, because `documents_required` is a new precondition. That is the rule being enforced, not an assertion relaxed — `ConcurrentSubmits_OnlyOneIsAccepted` still ends at exactly one acceptance, and `BodyWithinTheCap_PassesTheFilter` needed it or it would have passed the filter for the wrong reason and stopped being about body size at all.
12. **What the Chrome pass proved end to end**, at 390 px against Azurite and the real outbox worker: B3's link → P1 naming the broker from the snapshot → the three server-served insurance types → Continue disabled until all six fields → **the clarity gate refusing a blurry PNG with no request leaving the browser** and passing the sharp one at `1600×1200, sharpness 30724` → both files stored `public_document` with sizes matching the disk **byte for byte** (615 and 22088) → Send → **"2 documents"** → the same link then showing the closed page with **no name, no dates and no controls** → the broker notified, taking §8's **email fallback** → B1 *Ready to send* → B4 read-only with both provenance chips → three taps on **Send email producing one email** to `PLACEHOLDER-recipient-2` **with both attachments by name and size** → *Sent*. And **an untouched `link_issued` request reading "Nothing to review yet" with Sign out as the only button on the page** — browser-pass finding 2, closed. §9.1's limiter measured live: 20 of 25 views allowed, and the **upload route 429'd on its first attempt afterwards**, sharing the token's budget. Console clean throughout.

## Week 6 — Option 2 capture + device checkpoint · *expand when reached*

*Triage from the week-5 browser pass (recorded 2026-08-24, PO session — `docs/browser-pass-week5.md`): findings **1, 3, 4, 6** (B2's document list below Submit + duplicate "Documents" headings, the full-width provenance chip, B1's caption wrapping around Resend, no row navigation to `/broker/{id}` after send) are broker-screen defects and must be absorbed into **6.1's expansion** — they are fixes to what 5.2 shipped, not new scope. Finding **5** (B2 submits with no documents under a caption promising documents) is half product question: **#48** asks AXA whether Option 1 requires a document; whatever the answer, 6.1 reconciles the caption with the behaviour. The four older findings (7, 8, 9's missing catch-all, 10) stay on 7.2's buffer list.*

*Expanded 2026-08-25 from the week-5 Notes, the browser-pass triage above, the design canvases (P1Capture, A2Queue, B4Review), and `research-capacitor.md` §11–12. Fact-briefs run against the code as committed (`30b1bda`). Decisions at expansion:*
- *The five car shots are **five bucket rows** — `public_car_front` / `public_car_rear` / `public_car_left` / `public_car_right` / `public_car_roof` — `OwnerKind BrokerRequest`, capture-only, `PushTiming.Never`, null doc type, null folder (the `MediaBucketTests` biconditional holds). `MediaUploadTarget` has no side slot and the multipart contract reads only `bucket`+`origin`, so the side IS the bucket; five distinct buckets also keep `CapturePanel`'s input ids unique. No new wire field, no `document` column.*
- *The side selector is the expert diagram's SVG car reused as a **picker** (tap a region → that side's capture panel), never a drawing surface — `regions.ts` records this reuse as intended.*
- *Public submit additionally refuses until **each** of the five buckets holds a document (`car_photos_required`); `documents_required` (supporting docs) is unchanged and disjoint. The five count against `PublicLink:MaxFiles` by design — 5 + 10 supporting.*
- *Browser-pass findings **1, 3, 4, 6** are 6.1's to fix; finding **5** is caption-only ("…and any documents above") — **no submit gate** until #48 answers (developer decision 2026-08-24).*
- *6.2's endpoints live in **`Api.Outbox`** — architecture rule 4 permits no other namespace to reference `Next3OutboxMessage`. Long-pending per pass-3 decision 2 (`pending` with `next_retry_at` more than one `Outbox:PollSeconds` away); "Retry now" never touches `processing`.*
- *6.3 is **Android only** (platforms split); FCM runs against a **developer-owned Firebase project**, `google-services.json` an Appendix-A placeholder swapped at deployment (developer decision 2026-08-24); native tokens get their **own table** — `push_subscription` requires web-push keys an FCM token does not have. iOS native stays gated on #29/#30.*
- *Slice order 6.1 → 6.2 → 6.3; 6.3 runs on a branch (device work, release-config changes).*

### ☑ 6.1 Option 2 capture — five car shots + side selector (P1), B4 photo review, week-5 broker fixes (server + web)
**Goal:** §5.3's P1 submit rule executable — five mandatory capture-only car shots chosen on the diagram's car, each through the clarity gate; the broker sees them on B4; the four week-5 browser-pass defects on B1/B2 fixed.
**Refs:** design.md §5.3 (P1 upload/submit rows), §7.1 (public car-photo row), §9.1 (the upload surface), `docs/design/pass-2` P1Capture artboard, `docs/design/pass-3` B4Review, `docs/browser-pass-week5.md` findings 1/3/4/5/6, `docs/open-questions.md` #48.
**Prompt:**
> Slice 6.1 — the Option 2 car shots per design.md §5.3 and §7.1, plus the broker's photo review and the week-5 fixes.
> **Buckets (one migration, db-reviewer mandatory).** Five `MediaBuckets` rows — `public_car_front` / `public_car_rear` / `public_car_left` / `public_car_right` / `public_car_roof` — each `DocumentOwnerKinds.BrokerRequest`, `AllowUpload: false`, `DocTypeKey: null`, `MediaKind.Image`, `Next3Folder: null`, `PushTiming.Never` (the `MediaBucketTests` biconditional holds untouched). Add `MediaBuckets.PublicCarShots` (that order) and widen `MediaBuckets.BrokerRequest`. Migration `AddPublicCarShotBuckets` rewrites the document bucket check constraint to 20 values on the `AddPublicDocumentBucket` pattern, carrying its `Down()` and `--no-transaction` warnings. Update `MediaBucketTests` deliberately: the §7.1 matrix rows, `EveryBucketIsAccountedForByOwnerKind`, and a new test that the five are capture-only images under the broker_request owner.
> **Public surface.** Widen the upload gate to `BucketGates.Only(MediaBuckets.PublicDocument, …the five)` — `broker_document` stays out. `POST /public/{token}/submit` gains `400 {error:"car_photos_required"}` checked after `documents_required`, refusing unless **each** of the five buckets holds a row for the owner; the refusal leaves the token alive (1.5's rule) and does not enumerate missing sides (the page computes them from its own list). `documents_required` stays scoped to `public_document` — a car shot never satisfies it. The five count against `PublicLink:MaxFiles` (15) by design — 5 shots + 10 supporting; do not raise the cap. **Record in scope-decisions:** the Option 2 email now carries the five shots (`BrokerRequestEmail.Attachments` selects on the owner alone) — up to ~15 files per mail, accepted and said out loud rather than filtered.
> **Broker content read.** Lift `DeclarationDocumentContent.Serve` into a shared `Api.Modules.Media.DocumentContent.Serve(db, blobs, ownerKind, ownerId, documentId, ct)` (third caller — the "lesson learned in one adapter" rule) and delegate the Declarations copy. New `GET /api/broker/requests/{id}/documents/{docId}/content` inside `MapDocuments`, ownership-scoped through the existing `Find(db, id, brokerUserId)` first; a blob already swept by `BrokerMediaCleanupTask` answers 404 — expected, not a bug.
> **Web — P1.** Replace the placeholder panel (`PublicRequestPage.tsx` "Photographs come next…" — its pinned test moves deliberately) with a `CarShotsSection`: a `CarSideSelector` in `src/media/carshots/` reusing `CAR_VIEWBOX`/`CAR_REGIONS` from `media/diagram/regions.ts` grouped into five zones (`CAR_SHOT_ZONES` in `carshots/sides.ts` — geometry only; the component takes generic `{id, label, done}` sides so `media/` stays bucket-agnostic and the reusability guards hold), `role="radio"`/`aria-checked`, done-fill + dot per P1Capture; plus **one** `CapturePanel` whose `bucket`/`label`/`qualifier` swap with the selected side — `CAR_SHOT_PANELS` in `src/public/carShots.ts` binds side → bucket ("front of the car", …). Distinct buckets keep the capture-input ids unique. Done marks and "N of 5" come from the existing documents query filtered by bucket; Submit stays disabled until all five and at least one supporting document; `describeSubmitError` gains the `car_photos_required` sentence. No auto-advance after a shot (smaller interpretation; the done-fill is the feedback).
> **Web — B4.** Move `useDocumentBlobUrl` from `officer/` to `media/` (the move `GarageDeclarationPage.tsx` already records as intended; it touches only `apiBlob`). `broker/api.ts` gains `requestDocumentContentPath(requestId, docId)`. `ReviewPanel`'s "The car" placeholder (pinned test moves) becomes `CarPhotosPanel`: five labelled slots (Front / Rear / Left / Right / Roof) rendering each shot via the hook; a side with no document says **"Not provided"** — `ready_to_send` rows submitted before this slice legitimately have none.
> **Week-5 fixes (triaged into this card).** (1) B2 order: render the attached list **above** Submit and retitle it **"Attached"** (`Documents` gains a `title` prop; matches P1's own heading). (3) `.chip` gains `width: fit-content` (the 707 px stretch is the flex-column `.doc-row` default). (4) B1's Resend cell wraps button + caption in a new column-flex `.cell-stack` (align-items flex-start). (6) B1's insured-name cell becomes a `Link` to `/broker/{id}` on **every** row — O1's plate pattern (`OfficerInboxPage.tsx:58`). (5) Caption only: "This sends the details and **any documents** above to AXA, in one email." — **no submit gate** until #48 answers; record the interpretation in `scope-decisions.md`, dated.
> **Tests.** Server: a car shot stores under its side bucket; an uploaded file into a shot bucket is refused; submit with documents but missing shots → 400 + token still usable; all five + one document succeeds; a car shot does not satisfy `documents_required`; the file-count cap counts shots and documents together; the broker content read is scoped to the caller's own requests. Web: the selector renders five sides as a radio group, reports taps, fills done sides; the page collects five shots, disables Send until then, and explains a `car_photos_required` refusal; B4 shows the five by side and says "Not provided" for an absent one; B2's capture → Attached → Submit order pinned via `compareDocumentPosition`; the "any documents" caption; B1 links the insured name on every row.
**DoD:** `dotnet test` + `npm test` green; migration db-reviewed; **manual Chrome pass at 390 px**: B3 link → P1 → pick each side on the car → five captures with the clarity gate refusing a blurry one → supporting document → Send → B1 *Ready to send* → B4 shows the five by side → Send email carries six attachments by name; the four B1/B2 fixes verified visually (chip width, caption stack, name links, list above Submit).
**Test focus:** the widened gate never opens `broker_document`; `car_photos_required` and `documents_required` stay disjoint; `media/carshots/` carries no bucket name and no role import (the reusability suites are the pin).
**Notes:** done 2026-08-25 (622 xUnit: 621 pass, +33, 1 environment-skipped; web 345 → 387). One migration, db-reviewed. Every week-5 browser-pass defect is now closed.

1. **The db-reviewer found the thing the card had not thought about, and it is CLAUDE.md's first bug class again: nothing stopped a *second* photograph of one side.** No unique index, no `WHERE`, not even an `if` — and P1 offers "Take a photo" on a side that is already done, so a retake was one tap away rather than a corner case. Two `public_car_front` rows would both reach AXA as attachments (`BrokerRequestEmail.Attachments` selects on the owner alone) with nothing on the email to say which was current, both would consume §9.1's `MaxFiles`, and there is **no delete route on `/public/*`** — so enough retakes left a request nobody could send and nobody could repair, under an error message (`upload.ts`'s "Remove one before adding another") telling the customer to do something the API makes impossible. Decided with the developer: **a retake replaces**, on a filtered unique index over the five, with the delete staged into the same transaction as the insert. The **row** goes and the blob does not — §7.3's safe failure order, so the old bytes are an orphan for the sweep rather than something deleted inside a transaction that might roll back. Verified by removing `IsUnique()`: the schema test goes red **while the replacement test stays green**, which is the asymmetry that matters — the happy path alone proves nothing.
2. **Three of the reviewer's other five findings were real and taken; one was about code that did not exist yet.** Its HIGH #1 said the server enforced a precondition no client could satisfy — true of the tree it read, and closed by the same commit (5.3's note 9, second outing: *check the findings against the tree it actually saw*). Taken: **the five arrived at AXA nameless** (every iOS capture is `image.jpg` — 6.3a — so five attachments would share one name; they now carry their bucket, which is §4's `file_name` argument one module over); **the per-token rate limit had become too tight** (a clean session is now ~15 calls, so two retakes tripped 20/min) and, worse, **a 429 rendered as the dead-link page** — telling a customer holding a live link to ask their broker for a new one, which would have issued one and killed the old one for real. Recorded and not taken: the `MaxFiles` count is a read-then-write the token cannot arbitrate (the cap is 15, the harm is 16), and the widened queries filter on a column the index does not carry (six to fifteen rows per request).
3. **The manual pass found two defects, ninth slice running, and neither was reachable from jsdom.** The side labels were drawn horizontally: **"Right" measured 38 user units against a 30-unit door strip** — four over the edge each side — and "Left" landed exactly on the seam between the two door rects, struck through by its own outline. And **the five B4 slots rendered at their natural 1600×1200**, because the class names were in the JSX and the CSS was never written — `bodyScrollWidth` 1683 against a 1265 viewport. Every web test passed both times, and would have: **jsdom applies no stylesheet and lays nothing out**, which is exactly what CLAUDE.md means by testing the risky parts by hand. The rotation is pinned by asserting the `transform` attribute — what a test here can honestly check — and the geometry was measured in the browser instead.
4. **`minmax(0, 1fr)`, not `1fr`.** A grid track's default minimum is `auto`, which is the *content* size — and the content was a 1600 px photograph, so the plain version blew the column out and took the page's horizontal scrollbar with it. The one-character difference between a grid that contains its images and a grid its images contain.
5. **The side *is* the bucket, and that decision paid for itself three times.** `MediaUploadTarget` has no side slot and the multipart contract reads only `bucket`+`origin`, so five rows meant no wire field, no `document` column, and no migration beyond the check constraint. It also keeps `CapturePanel`'s `${bucket}-capture` DOM id unique while one panel swaps between sides — a global id, so a single shared bucket would have bound five labels to one input — and it is what let the unique index be written at all.
6. **One `Distinct()` answers both preconditions, and that is what makes them provably disjoint.** `documents_required` and `car_photos_required` are existence questions over different buckets; asking them as two counts would have been two chances to point at the same set. The refusal deliberately **does not name the missing side** — the page holds the list and computes it — which keeps §9.1's surface saying as little as it can.
7. **`media/` gained a bucket-name ban, because the import bans could not express this one.** A component that *knew* the roof goes to `public_car_roof` would be an Option 2 component sitting in the shared folder, and the garage flow that wants the same picker next year would copy it. All twenty names are listed rather than the five new ones, matching on the **full** name so `regions.ts`'s panel ids and `carshots/`'s zone ids are untouched. Verified by planting one.
8. **Five existing P1 tests went red the moment the server started requiring photographs, and that is the rule being enforced rather than an assertion relaxed** — 5.3's note 11, verbatim situation. `OpenAndAttach` is the single choke point on the server side, so widening it covered every suite at once; on the web side the fixture gained a `COMPLETE` list. The "photographs come next" test **moved rather than being deleted**: it now pins that the five sides render and are collectable.
9. **`DeclarationDocumentContent` became `Api.Modules.Media.DocumentContent` before the second copy existed rather than after** — the "lesson learned in one adapter" rule applied ahead of the mistake for once. The owner **kind** joined the predicate with the owner id, because `owner_id` is polymorphic and carries no FK (§4): the id spaces of `declaration` and `broker_request` are separated by nothing the database enforces, and a test now proves the broker route refuses a declaration's document.
### ☑ 6.2 Failed-push admin screen A2 (server + web)
**Goal:** §5.4's A2 executable — failed and long-pending `next3_outbox` rows listed with their story, retryable singly and together; the admin nav tab goes live with a count.
**Refs:** design.md §5.4 (A2 row), §6.3 (outbox retry semantics), `docs/design/pass-3` A2Queue artboard + `pass-3-review.md` decisions 2–3; architecture rule 4.
**Prompt:**
> Slice 6.2 — the A2 failed-push queue per design.md §5.4 and the pass-3 A2Queue artboard.
> **Placement.** Everything server-side lives in **`Api.Outbox`** — architecture rule 4 lets no other namespace reference `Next3OutboxMessage`, and the rule is right, so the admin surface moves to the row rather than the row leaking out. New `OutboxAdminEndpoints.MapOutboxAdminEndpoints`, group `/api/admin/outbox`, `RequireAuthorization(AuthPolicies.Admin)`, mapped from Program.cs.
> **"Last tried" is a migration** (`AddOutboxLastAttemptAt`, db-reviewer mandatory): nullable `last_attempt_at` on `next3_outbox`, stamped inside the dequeue **procedure's** claim UPDATE (the claim is the attempt — the same reasoning that put the attempts increment there); the abandoned-row retire does **not** stamp it (retiring is not attempting). `Up`/`Down` use `CREATE OR ALTER`; `Down` restores the `AddOutboxTable` proc body **byte-for-byte** — a drifted restore silently changes dequeue semantics. Historical rows stay NULL; the screen shows an em dash.
> **Endpoints.** `GET /` → `OutboxAdminRowDto(Id, Operation, VisaNo, Status, Attempts, LastError, CreatedAt, LastAttemptAt, NextRetryAt)` for `failed` ∪ long-pending — **`pending` whose `NextRetryAt` is more than one `Outbox:PollSeconds` away** (pass-3 decision 2 verbatim; `processing` is never listed) — failed first, oldest `CreatedAt` first within each (a queue you work). `GET /count` → `{ failed }` — **failed only**: long-pending self-heals, and a badge that breathes with the backoff schedule is an alarm nobody trusts. `POST /{id}/retry` → one `ExecuteUpdateAsync` with the status inside the WHERE (`id` ∧ status ∈ {failed, pending}) SET status=pending, `next_retry_at`=now — it can never touch `processing` and never races the claim (both are single atomic UPDATEs; whichever wins, the other's predicate no longer matches; `Attempts` untouched so a live worker's outcome write is unaffected); 1 row → 200, 0 rows → `409 not_retryable`; `LastError` is kept until the next outcome overwrites it. `POST /retry-all` → the same SET over exactly the list predicate → `{ retried: n }` (0 is a fine answer; no ConfirmDialog — retries are idempotent via clientRef, the artboard's own argument). **Audit:** new `AuditActions.OutboxPushRetried` / `OutboxPushesRetried` + `AuditEntityKinds.Next3Outbox`; `AuditWriter.Append` after a successful ExecuteUpdate needs its own `SaveChangesAsync` (the writer adds, never saves; `entityId` is nullable — null for retry-all with `{ Count }` detail).
> **Web.** The admin module's first api file `src/admin/outbox.ts` (react-query — the house norm; A1's raw effect is the outlier, not the pattern): types, fetchers, `outboxKeys`, `useOutboxList` + `useOutboxFailedCount`, both `refetchInterval: 30_000` (mirrors `Outbox:PollSeconds` — an engineering constant, not client data); retry mutations invalidate both keys. `AdminFailedPushesPage` in `pages/`: `Worklist` with `Operation | Claim / visa | Attempts | Last error | Created | Last tried |` action; operation and visa **raw in `.mono`** (an admin reads them to a developer); inline chips — failed → danger "Failed", long-pending → warn "Still trying" with "next attempt in ~…" from `NextRetryAt`; **Retry** on failed rows, **Retry now** on still-trying rows (same endpoint); **Retry all** above the table; the empty state is reassurance + "Last checked {time}" from the query's `dataUpdatedAt` ("nothing has failed" must be distinguishable from "this page has not loaded").
> **Route + nav.** `/admin/failed-pushes` registered **before** `/admin/:kind` — the param route eats it otherwise (today it renders "Unknown profile type."). Remove the path from `UNBUILT_TABS` (keep the mechanism); `DesktopNav` gains optional `counts` and renders `.desktop-nav__count` (the CSS has been waiting since 4.4) when > 0; plumb via a new optional `tabCounts` on `AppShell` → `AppHeader` with the count hook called in `AdminLayout` (App.tsx) — `ui/` must not import `admin/`, so the count is **passed in**. The two pinned `AppHeader.test.tsx` assertions move deliberately: live link + shows-count-when-provided + hides-at-zero.
> **Tests.** Server (`OutboxAdminTests`): the list shows failed rows and pending rows more than one poll away; a pending row due within one poll is not listed; a `processing` row is never listed; retry resets a failed row to pending-and-due; Retry now pulls a long-pending row forward; retry refuses a processing row and leaves it untouched; retry-all resets every listed row and nothing else; the count counts failed only; every other role is refused; a retry writes an audit row. Plus in the dequeue tests: the claim stamps `last_attempt_at`; a never-claimed row has none. Web: list with raw mono operations; the two chip tones; Retry/Retry now; a retry refetches; retry-all is one call; the reassuring empty state with a last-checked time; the header tests moved.
**DoD:** `dotnet test` + `npm test` green; migration db-reviewed with the proc body diffed against `AddOutboxTable`; **manual Chrome pass**: `Fake:FailureRate=1.0` → rows walk to `failed` (`Outbox:MaxAttempts` down for the demo) → the screen lists them with raw operation names → restore the fake → Retry one → the worker resends it → Retry all drains the rest → the nav count falls to zero → the empty state shows with its timestamp.
**Test focus:** the retry UPDATE's predicate (never `processing`, never racing the claim); the route-order trap; the count staying failed-only.
**Notes:** done 2026-08-25 (641 xUnit: 639 pass, +19; 2 environment-skipped — the Azurite half of `BlobStoreContractTests` skips with nothing on port 10000, which 6.1's run had up; web 387 → 399). One migration, db-reviewed. A2 is live and the admin nav carries the first count in the chrome.

1. **The db-reviewer found the blind spot in the screen built to remove blind spots, and it is recorded rather than closed.** The predicate lists `pending` rows scheduled *far ahead*; a row that is **overdue** — due in the past and not being claimed — is on neither the list nor the badge. That is the signature of a stopped worker job, of a backlog draining slower than it fills, and of the seconds after a Retry. So A2 can show a clean queue while nothing at all is reaching NEXT3, which is the one thing it exists to contradict. Not fixed here because pass-3 decision 2 is quoted verbatim in the card and the fix is a third predicate arm *plus* a grace period — every freshly enqueued row is briefly overdue-looking — so it is a decision to take deliberately, not to fold into the slice implementing the card. **Carried as a 7.2 ticket**, in `scope-decisions.md`, in §5.4, and on the PO handoff's buffer list.
2. **`Fake:FailureRate` is global, and the browser pass discovered it by locking itself out.** Setting it to 1.0 to make pushes fail also makes `FakeSmsSender` throw, so the OTP could not be sent and nobody could sign in. The order that works is *sign in first, then break NEXT3* — the knob is one setting for every fake, which is right for "kill the integrations mid-demo" and a trap for "kill one of them". Worth knowing before a demo, not during one: `docs/demo-week4.md`'s flow raises the rate **after** everyone is authenticated, and now there is a reason written down for why it has to.
3. **A helper method inside a LINQ tree compiles and then throws at the first press.** `Retryable(m)` read well and would have failed at runtime on an endpoint whose whole job is to be there when something has gone wrong — the worst place to put a latent translation error. It is an `Expression<Func<…>>` composed as its own `Where`, which ANDs with the id and translates. Caught by reading the code before running it; nothing in the type system was ever going to.
4. **`Down()`'s "byte-for-byte" was made true by construction, not by care.** The restore body is *extracted from* `AddOutboxTable.cs` by the script that wrote the migration, and a second script asserts the two are identical strings. Retyping forty lines of a stored procedure that holds the lease semantics, the `READPAST` hints and the retire rule is exactly the task a human does slightly wrong, and the failure mode is a rollback that succeeds with different dequeue behaviour — silent, and days later.
5. **The first version of the badge-invalidation test would have passed whether or not the code invalidated anything.** The count hook is mounted by `AdminLayout`, not by the page, so on a page-only render there is no observer and `invalidateQueries` just marks the key stale — no refetch, no call to assert on. The test now renders a probe component that uses the hook, and it goes red when the count invalidation is removed. Verified by removing it. A test that cannot fail is the same bug class as 2.4's arithmetic-back-to-itself, one layer up.
6. **Emptying `UNBUILT_TABS` would have retired a guard silently.** With the set empty, `DesktopNav`'s disabled branch is unreachable and nothing covers it, so the next tab drawn ahead of its screen would have found out in a browser. The set is a mutable `Set`, so the test adds a path, asserts the muted non-link, and removes it in a `finally` — CLAUDE.md's "pair every guard with a non-vacuity assertion", answered by keeping the assertion rather than the guard alone.
7. **The browser pass found one defect, and it was in the column the screen exists for.** A 21-character visa number in a 173 px cell wrapped to `PLACEHOLDER-` / `VISA-0002` — a broken identifier in the one column an admin reads aloud to a developer. `white-space: nowrap` on `.worklist td.mono`, which `.worklist__scroll` was already there to absorb; measured after, and the table still does not overflow the page. Tenth slice running that the browser pass earned its place, and jsdom lays nothing out, so nothing else could have caught it.
8. **A migration can be safe in isolation and unsafe in a deployment order.** `Up()` adds a column before the procedure that writes it (deferred name resolution would otherwise let it install and fail at the first dequeue); `Down()` restores the procedure *before* dropping the column, for the mirror reason. And rolling the **database** back under a **new** app image breaks every claim, because `FromSql` demands a column the restored `OUTPUT inserted.*` no longer returns — so the order is image first, then migration, and it is written in the migration's own doc comment, where somebody will be standing when it matters. §10 says nothing about ordering.
9. **The shared frozen clock moves when you sign in.** `Login` advances it 61 seconds to clear the OTP resend throttle, so a timestamp captured during arrange is not the one the handler writes. Every A2 test signs in **once** and reads the clock again *after* the call it is asserting on. The first draft compared a value captured before the request against one written after it, and it was the fixture, not the endpoint, that would have been blamed.

### ☑ 6.3a Early device spike — Android build (everything but push) + iPhone PWA pass + `research-capacitor.md` from the results
*Pulled forward from week 6 on 2026-08-22 because the styled web app is stable and the camera half of the device checkpoint can be de-risked three weeks early. Runs on a branch, one session, hard stop. It is not 6.3: native push on both platforms stays there, gated on AXA's FCM/APNs accounts (#29/#30).*
**Goal:** a debug APK running on the developer's Samsung over USB proving capture-only camera, geolocation and layout; the web app running on the developer's iPhone over HTTPS proving Safari capture, `MediaRecorder`, Add-to-Home-Screen and iOS web push; and HANDOFF §7A's research brief answered from what was observed rather than from the web.
**Refs:** HANDOFF §4 (decision: PWA + Capacitor, provisional) and §7A (the ten research questions); design.md §7.1 (native enforcement is the provisional part), §8 (push rows), §10 (Codemagic for iOS); CLAUDE.md (Azurite, `Push__Mode=webpush` + user-secrets); `docs/demo-week4.md` pre-flight (permissions).
**Prompt:**
> Slice 6.3a — early device spike. **Pre-flight, then stop if it fails:** `git status` must be clean and 4.4 committed; create branch `spike/device-6.3a`. Nothing in this slice changes behaviour in `src/Api` or in the React components — it adds a native shell, dev-only config, and a research document.
> **Part A — Android, everything but push.** Install the toolchain without Android Studio: a JDK (17 or 21, via winget), the Android SDK **command-line tools** (`sdkmanager`: platform-tools, the current platform, build-tools; accept licences), and record the exact versions in CLAUDE.md's commands section. Add Capacitor to `src/Web` (`@capacitor/core`, `@capacitor/cli`, `@capacitor/android`; `npx cap init` with app id placeholder `PLACEHOLDER.axa.motorclaims`, web dir `dist`), `npx cap add android`, and **`server.url` in `capacitor.config` pointing at the dev machine's LAN API over HTTPS** (see Part B for the cert) so the shell talks to the running API without a deploy. Build a **debug** APK with Gradle from the CLI; install over USB with `adb` (USB debugging on the Samsung). Then **hand-test and record, in a `docs/device-spike-2026-08-22.md` table, with screenshots named by check:** (1) every capture-only bucket opens the camera with **no** gallery route — and note whether `capture="environment"` is honoured by the WebView or whether `@capacitor/camera` is needed (if needed, wire it behind the existing `CapturePanel` without changing its props); (2) Arrived — geolocation permission flow, accuracy, time to fix, the denied path; (3) `MediaRecorder` — which `audio/*` type the WebView produces and whether the server accepts it; (4) the diagram PNG export; (5) the 390 px layout on real glass and the 48 px targets with a thumb; (6) the clarity gate's speed on a real 12–48 MP photo; (7) that web push does **not** fire inside the WebView — confirm and record rather than assume; this is why 6.3 exists. No FCM, no Firebase, no signing keys in this slice.
> **Part B — iPhone, the web app as a PWA.** Safari needs a secure context for camera, location and push, and it will not trust the ASP.NET dev cert. Use `mkcert` (winget): generate a cert for the machine's LAN IP/hostname, serve the API and the Vite dev server with it (or `vite preview` behind the API), and install the mkcert root CA on the iPhone as a profile (Settings → General → VPN & Device Management → enable full trust). Record the exact steps in CLAUDE.md. Then on the iPhone, hand-test and record in the same table: (1) Safari capture input — camera vs gallery behaviour for capture-only buckets (Safari offers a sheet; note what it offers); (2) `MediaRecorder` support and the type produced (`audio/mp4` expected) and server acceptance; (3) Arrived geolocation; (4) **Add to Home Screen**, then from the installed PWA: **Enable notifications → a real iOS web push** from an injected assignment (`Push__Mode=webpush` with the user-secrets VAPID pair; iOS 16.4+; push only works from the installed app, never from the Safari tab — record both); (5) the diagram export; (6) layout and targets. Add a minimal `manifest.webmanifest` + `<link rel="manifest">` if Add to Home Screen needs it for standalone display — this is the one allowed `src/Web` addition; keep `sw.js` untouched.
> **Part C — write `docs/research-capacitor.md`** answering HANDOFF §7A's ten questions, **from Parts A and B first** and from the web only where observation cannot answer (iOS native push via Capacitor, Codemagic free-tier iOS build, store vs MDM distribution, OEM battery-saver behaviour — cite sources, dated). End with the go / no-go on PWA + Capacitor, the revised estimate for 6.3 (what is left: native push on both platforms, signing, Codemagic pipeline, store/MDM packaging), and the concrete build-and-distribution plan for week 6. Flag which answers are blocked on #28–#30.
> **Rules.** Branch only; no commit (the developer reviews the spike diff). The Android shell and any `capacitor.config` live in `src/Web`; nothing client-specific goes anywhere but the placeholder file. Every observation in the table is a fact with a screenshot, not an expectation. If the toolchain fights you for more than an hour on either platform, record where it stuck and move on — the research document is the deliverable; a half-working shell is acceptable output for a spike.
**DoD:** a debug APK installed on the Samsung from the CLI, with the seven Android checks recorded; the PWA installed on the iPhone over HTTPS with the six iOS checks recorded, including a real iOS web push received in the installed app; `docs/device-spike-2026-08-22.md` with screenshots; `docs/research-capacitor.md` written with a go/no-go and the 6.3 estimate; CLAUDE.md updated with the toolchain and mkcert steps; `npm run build`, `npm test`, `dotnet test` unchanged and green.
**Test focus:** none new — this is observation. Any code change beyond the shell, the manifest and config is a sign the spike has become 6.3; stop and record instead.
**Notes:** Ran 2026-08-23 on branch `spike/device-6.3a`, merged to `main` (five commits). **Thirteen checks, two handsets, every row filled** (`docs/device-spike-2026-08-22.md`); `docs/research-capacitor.md` written with a **GO, platforms split**. The headline is that the platforms swapped roles: **Android's WebView ignores `capture="environment"` and opens the gallery** (fraud surface open, `@capacitor/camera` required), while **Safari honours it** and **iOS web push works from the installed PWA** (B4 PASS, observed twice) — so the recommendation is **ship Android as the Capacitor app, ship iOS as the installed PWA, and treat the iOS Capacitor build as a separate deliverable gated on #29/#30**, which takes #30's multi-week lead time off the critical path. Five findings no browser pass could reach: Apple **403s** a VAPID subject of `mailto:…@example.invalid` while FCM accepts it (a silent iOS-only outage waiting in Appendix A — the subject must be a real AXA mailbox at deployment); `MediaRecorder` emits **`audio/webm` on both** platforms, not the `audio/mp4` slice 3.1 anticipated for Safari; every iOS capture is named **`image.jpg`**, so four photos land under one visa with identical names — a question for AXA (#46), not a defect; the WebView reports **location-off as `TIMEOUT`**, so the expert is told to wait for a signal that will never help; and an installed iOS PWA has its **own storage context**, so Add-to-Home-Screen costs a fresh OTP sign-in. Three defects were introduced and caught on the device: `viewport-fit=cover` put the header under the status bar, an env-gated HTTPS dev config **silently downgraded to http** on a Vite restart, and `adb reverse` (IPv4) could not reach Vite (`::1`). Five code-change tickets raised and deliberately **not** built — the spike's rule was shell, config, manifest and docs only. **Process deviation recorded:** the card said branch-only/no-commit; the work was committed to `main` after review with all gates green (279 web / 501 xUnit, no test moved). **Propagation done 2026-08-23 by the PO session:** scope-decisions rows, open-question #46, HANDOFF §8 item 3a, 6.3 re-scoped below.

### ☐ 6.3 Capacitor — Android native app (camera, FCM push, location copy, release build)
**Goal:** Android ships as the Capacitor app per the 6.3a decision: capture-only enforced by the native camera, FCM push to native device tokens **beside** web push, the location-off trap answered in copy, and a signed release build with every piece of dev wiring off the path. iOS stays the installed PWA; the iOS native build remains a separate deliverable gated on #29/#30 (if AXA mandates MDM on iOS it becomes mandatory with #30 on the critical path); Huawei/HMS push is unbudgeted and depends on #28.
**Refs:** `docs/research-capacitor.md` §11–12 (the plan and estimates — Android ≈5–7 d), `docs/device-spike-2026-08-22.md` (toolchain versions, traps), design.md §7.1 (native enforcement), §8 (push contract), CLAUDE.md "Device testing"; the week-6 milestone checklist below; open questions #28–#30.
**Prompt:**
> Slice 6.3 — the Android Capacitor app per `research-capacitor.md` §12. Branch `slice/6.3-android`; server first, device pass last.
> **Validator first (independent — ships even if the rest slips).** `PushOptionsValidator` gains a routable-subject rule: after the scheme check, extract the host (`mailto:` → after the `@`; `https://` → `Uri.Host`) and fail on `localhost`, any `*.invalid` / `*.example` / `*.test`, and `example.com/org/net` (the RFC 2606 set). The failure text names the consequence: **Apple answers 403 to an unroutable subject, silently, iOS-only** (6.3a's finding). Update Appendix A's comment on `Push:Vapid:Subject`.
> **Device tokens (one migration `AddDeviceTokenTable`, db-reviewer mandatory).** A native FCM token has no https endpoint, no P256dh, no auth — it fits none of `push_subscription`'s required columns, so it gets its own entity: `DeviceToken` in `Api.Integrations.Push` — `Id`, `UserId`, `Token` (512), `TokenHash`, `Platform` (check-constrained `'android'`), `CreatedAt`, `LastUsedAt?`, `RevokedAt?`; unique `(user_id, token_hash)`; FK mirroring `PushSubscriptionConfiguration`. Endpoints as siblings in `PushEndpoints.cs` (`ActiveUser`): `POST /api/push/device-tokens` `{token, platform}` → `{id}` with the subscriptions ladder (`token_required` / `token_too_long` / `platform_not_supported` / `too_many_device_tokens` against new `Push:MaxDeviceTokensPerUser`; upsert un-revokes; the `DbUpdateException` race-catch); `DELETE` always 204 with explicit `[FromBody]` (the DELETE-body startup trap). Revoke-don't-delete on FCM `UNREGISTERED`; explicit unregister deletes.
> **FCM sender.** Config `Push:Fcm:{Enabled: false, ProjectId, ServiceAccountJsonPath, TimeoutSeconds}` — placeholders, `Enabled:false` keeps every environment booting; `FcmOptionsValidator` runs only when `webpush` and `Enabled`. `FcmAccessTokens` builds the RS256 service-account JWT (`System.IdentityModel.Tokens.Jwt` rides transitively via JwtBearer — verify at first build; the fallback is a hand-assembled JWT, still **no new package, no licence question**), exchanges it at Google's token endpoint through named client `"fcm"`, caches to ~5 min before expiry. `FcmPushSender : IPushSender` mirrors `WebPushSender` **exactly**: no live tokens → one `NotificationLog.Failed` row + `PushNotDeliveredException`; one notification row per token (address = `DeviceToken.Id`); `LastUsedAt` on success; **revoke only on a 404 whose error body carries `UNREGISTERED`** — a bare 404 is also a mistyped ProjectId, and revoking on it would silence every device at once; timeout discrimination (the 3.3 `TaskCanceledException` lesson); throw when zero accepted. Wire: `POST /v1/projects/{ProjectId}/messages:send`, payload `{message: {token, notification: {title, body}, data: {url}, android: {priority: "high"}}}` — `data.url` is the same tap contract `sw.js` reads.
> **Composite.** `CompositePushSender : IPushSender` replaces the bare sender under `Push:Mode=webpush`: it wraps `WebPushSender` plus, when `Fcm:Enabled`, `FcmPushSender` — a user legitimately has a browser **and** a phone, so the channels are additive, never modal. Partial success returns; it throws `PushNotDeliveredException` only when **zero devices across all channels** accepted; an inner channel's unexpected exception counts as "delivered nothing there" and never silences the other channel. §8's contract is the pin: `AssignmentHandler` stamps `notified_at` iff `Send` returns. FCM disabled means behaviourally identical to today; `fake` mode untouched.
> **Web seams (house idiom — injectable, jsdom never imports Capacitor).** `media/nativeShell.ts`: `NativeShell {hasCamera, takePhoto}`, `detectNativeShell()` probing `window.Capacitor?.isNativePlatform?.()` (property probe, no import), the real `takePhoto` via **dynamic** `import('@capacitor/camera')` with `source: CAMERA` → `File` — camera-only is what closes the WebView gallery hole. `CapturePanel` gains `shell?: NativeShell` (default `detectNativeShell()`): in the native shell the capture `<input>` becomes a `<button>` with the same visible label and accessible name calling `takePhoto()` → `capture.select(file, 'captured')`; upload inputs for `allowUpload` buckets are untouched (a picker is *allowed* there); the clarity gate runs unchanged on the returned file. `push/nativeToken.ts`: `AcquirePushToken` + `acquireNativePushToken` (dynamic `import('@capacitor/push-notifications')`: permissions → register → resolve on `'registration'`). `usePushSubscription` gains `{shellIsNative?, acquireToken?}` — native enable calls new `registerDeviceToken(token, 'android')` in `push/api.ts` instead of the service-worker path. `push/nativeTaps.ts`: `wireNativeNotificationTaps(navigate)` reads `notification.data.url` (the very field `sw.js` reads) — wired once in `main.tsx` behind the probe. Foreground-received notifications are **not** re-displayed in-app — record the smaller interpretation. Confirm the dynamic imports land in async chunks and execute nothing at module load in a browser build.
> **Shell + release path.** Install `@capacitor/camera` + `@capacitor/push-notifications` (^8, matching core 8.5.0; **check both licences from the package on install** — 3.4's rule, third outing). Convert `capacitor.config.json` → `capacitor.config.ts` in which `server.url`, `cleartext` and `webContentsDebuggingEnabled` exist **only when `CAP_SERVER_URL` is set** — unset means the clean release shape (`webDir: 'dist'`). Replace the placeholder appId with the real one **now** — FCM binds to the applicationId and the Android package paths regenerate, so it must precede the Firebase app. Commit `android/app/google-services.json.placeholder` with a **conditional** gradle apply (the plugin hard-fails on a placeholder file; dev builds must compile without the real one — test both branches); the real file comes from the **developer-owned Firebase project** (decision 2026-08-24) and swaps to AXA's at deployment like every Appendix A value. `signingConfigs.release` + gitignored `keystore.properties`; `scripts/android-release.ps1` asserts `CAP_SERVER_URL` unset and google-services non-placeholder → `npm run build` → `npx cap sync android` → `gradlew assembleRelease`. **Never commit:** the keystore, `keystore.properties`, the real `google-services.json`, the service-account JSON (mounted-secret custody, same as the VAPID private key).
> **Location + battery (copy only — a community settings plugin means a licence review the half-day doesn't cover).** The `timeout` sentence in `expert/geolocation.ts`'s failure copy becomes shell-aware: in the native shell it says the location toggle looks off and where to turn it on, instead of advising a wait that cannot help. Native-shell-only battery-optimisation guidance beside the PushPanel copy, plus `docs/oem-push-guidance.md` (Samsung/Xiaomi/Huawei specifics) for the rollout notes. Record both as scope decisions.
> **Tests.** `FcmPushSenderTests` via `StubHttpMessageHandler`: a tokenless user fails loudly with one row; one row per token + `LastUsedAt`; `UNREGISTERED` revokes and the others are still attempted; a rate-limited token stays live; it throws only when no token accepted; the access token is cached across sends; a timeout is recorded, not thrown out of the loop. `CompositePushSenderTests`: browser-only accepted succeeds; native-only accepted succeeds; throws only when both channels deliver nothing; a channel's unexpected failure does not silence the other; FCM is absent when disabled. `DeviceTokenEndpointTests`: upsert by (user, token); the 11th live token refused; unknown platform refused; re-registration un-revokes; delete removes only the caller's row; two simultaneous registrations upsert to one row. `PushOptionsValidatorTests` theories (unroutable hosts fail; a routable one passes). `FcmOptionsValidatorTests` (placeholder ProjectId fails when enabled; missing file fails when enabled; disabled validates nothing). Web: nativeShell browser fallback; CapturePanel — native camera button with no capture input, file picker kept for upload buckets, capture input kept in a plain browser, a native photo walks the clarity confirm; usePushSubscription — native token registered instead of a web subscription, a native failure surfaces without touching the service worker; nativeTaps — navigates to the url, falls back to root without one.
**DoD:** `dotnet test` + `npm test` green; migration db-reviewed; **device pass on the Samsung recorded against the week-6 milestone checklist**: a capture-only bucket cannot reach the gallery; an FCM push arrives with the app killed **and** backgrounded and its tap deep-links to `/expert/{id}`; location-off shows the new copy, not a timeout; the battery-saver check; clarity-gate timing on a real 12–48 MP photo (the A6 backfill); the release APK inspected — no cleartext, no `server.url`, debugging off; an iPhone PWA regression pass confirming iOS is untouched.
**Test focus:** the composite's throw semantics (§8's `notified_at` depends on them); `UNREGISTERED` versus a bare 404; the conditional google-services apply in both branches; jsdom never imports a Capacitor module.
**Notes:** _

**Week-6 milestone checklist (real-device checkpoint — updated 2026-08-23 after 6.3a; Flutter is NOT reconsidered: the camera failure's fix is a plugin, not a stack):**
- [x] Samsung via USB: Arrived geolocation accurate (A2); layout/targets (A5); voice note + diagram (A3/A4); web push confirmed absent in the WebView (A7) → native push is 6.3
- [ ] Samsung via USB, after 6.3: capture-only bucket cannot reach the gallery (`@capacitor/camera`); FCM push arrives app-killed and backgrounded; location-off is detected, not reported as a timeout
- [x] iPhone as installed PWA: capture honoured by Safari (B1), voice (B2), geolocation (B3), **real iOS web push received** (B4), diagram (B5), layout on a wide device (B6)
- [ ] iPhone on a *narrow* device (B6 was an iPhone 17 Pro Max); the Add-to-Home-Screen + fresh-OTP install path written into the rollout/training notes
- [ ] Clarity gate timing measured on a real 12–48 MP Android photo (A6 was not run on Android)
- [ ] Battery-saver test on the Samsung (MENA OEM push-killing — §7A item 3), after FCM exists
- [x] Verdict recorded in research-capacitor.md: **GO, platforms split** (Android native / iOS PWA; iOS native build gated on #29/#30)

## Week 7 · *expand when reached*

### ☐ 7.1 Public-surface security pass — §9.1 as tests: token lifecycle, rate limits under load, caps, enumeration resistance. Run `/security-review` on the diff.
### ☐ 7.2 Hardening + spillover buffer — error states, size limits, audit completeness (§9 event list); Option 2 spillover lands here by design.
### ☐ 7.3 UAT prep — seed data, test environment live (§10), runbook draft, `/code-review ultra` on the whole tree.

## Week 8 · *expand when reached*

### ☐ 8.1 UAT round — single round, capped fix window (§11 trade-off; scope letter defines acceptance).
### ☐ 8.2 Deploy + handover — prod deploy per §10, handover docs, account transfer, training.

**Week-8 milestone checklist (handover → 30% payment):**
- [ ] Source in AXA's repo; deployment runbook delivered; accounts transferred (AXA name, AXA card)
- [ ] Fake NEXT3 still config-selectable (support/debugging tool per §6.2)
- [ ] Training session held; hypercare window dates in writing
- [ ] "Handover complete" per scope-decisions.md guardrails — deployed and demoed, not AXA's internal rollout


---

## Maintenance rules

- **Client answer arrives** → update `design.md` + placeholder config; playbook untouched (prompts only point).
- **Slice done** → tick + one-line Note; Notes feed the next week-upgrade session.
- **Week starts (3+)** → upgrade that week's cards to verbatim prompts in one short session.
- **Design change** (scope, interpretation) → design.md and scope-decisions.md first; then check whether any un-started slice's refs moved.
