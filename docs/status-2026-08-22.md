# Weekly status — week 4 (to 2026-08-22)

**To:** HADDAD Ramy, AXA Middle East
**From:** Ali Sleiman
**Status:** DRAFT — not sent. Slice 4.3.

Two things in this status must not be softened in editing, because both are dated commitments that
protect the delivery: **the NEXT3 sandbox gate is still closed**, and **the day-for-day clock starts
at the demonstration if it is still closed then**. They are stated once each, plainly, near the top.

---

**Subject: Motor Claim App — week 4 status, and the demonstration**

Dear Ramy,

## Where the build is

Weeks one to four are complete. Working today, end to end:

- **Administration** — all four profile types, invitation by SMS, login by phone and one-time code.
- **The expert module, complete** — assignment notification on the phone, claim detail, *Arrived*
  writing date, time and position back, all four photo buckets, voice note, damage diagram, expert
  report, and the image quality check that refuses a photograph before it is ever sent.
- **The garage and claim-officer flow, complete** — declaration, notification, visa search,
  approval or rejection with comments, the approval rendered as an image, and everything filed into
  the *Survey* folder under the visa the officer chose.
- **The delivery queue behind all of it** — the mechanism that makes a NEXT3 outage invisible to an
  expert standing at an accident. I will demonstrate this by switching NEXT3 off mid-flow.

Next: the garage's post-repair uploads, then the broker module — both options, including the
customer link.

## The sandbox — the one thing that decides the date

**The NEXT3 sandbox was due in week three. It has not arrived, and I have had no date for it.**

I have not let that stop the build. The real NEXT3 client is written and tested against the
interface specification I sent you (`next3-openapi.yaml`); switching from the simulator to the real
system is a configuration setting, not a rebuild. Everything in the demonstration runs on the
simulator, and I will say so on screen rather than let it be assumed.

**To be direct about the consequence: if the sandbox is still absent at the demonstration, the
delivery date moves day for day from that point.** Not because of the switch-over itself — that is
an afternoon — but because the integration cannot be proven against anything real until it exists,
and proving it is the part that finds problems.

The two questions I need answered are in the scope letter: who is building the NEXT3 endpoints, and
on what date can I have a sandbox URL with credentials.

## The demonstration

Fifteen minutes, then questions. I will show onboarding, the full expert chain including a
photograph refused for being out of focus, a simulated NEXT3 outage and recovery, and the garage and
claim-officer flow through both approval and rejection.

I will also spend a minute on what is deliberately fake on screen — document-type codes, insurance
types, the NEXT3 address. **No client data has been invented anywhere in the build.** Everything
unanswered is an obvious placeholder in one configuration file, because a plausible guess is worse
than a visible blank: it gets believed.

## Also sent today

- **The scope letter**, for written acknowledgement before the demonstration. It confirms that
  Broker Option 2 is included, sets out what is excluded, and names the dates I need things by.
- **`next3-openapi.yaml`** — the proposed interface specification for NEXT3. Your team or the NEXT3
  vendor can review, amend and build to it rather than both sides guessing. It also asks NEXT3 to
  ignore a repeated document reference, which is what makes a retry after a timeout safe; the scope
  letter explains what happens if it does not.

Best regards,
Ali Sleiman

---

## Internal notes (not part of the message)

- **Why the scope letter and the OpenAPI file are listed as "also sent today":** they must go out
  **before** the demonstration (HANDOFF §8, and slice 4.3's client-comms gate). If they have not, the
  demonstration does not happen — this status must not go out claiming otherwise.
- **What is not said, and why:** the two things traded to fit Option 2 in (diagram polish, second UAT
  round) are in the scope letter, not here. A status report is not the place to negotiate scope.
- **Also true and unsaid here:** `docs/research-capacitor.md` is still unwritten, and the week-6
  device checkpoint depends on it. It is not a client-facing item yet; it becomes one if the
  checkpoint fails.
